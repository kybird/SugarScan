# plan.md — 2026-09-21 접수 요구사항 실행 계획

사용자 실행 명령(2026-09-21)에 따라 작성. 요구사항 #1~#10 + 전제 P1 을
작업으로 분해하고 칸반 보드에 올린다. 판단의 정본은 이 문서와 각 카드.

## 전제 (사람 결정, 고정 — 재론하지 않는다)

- **P1** 라이브 카메라 스캔은 아직 시작하지 않았다. 현재 구현 목표는
  **사용자가 카메라로 찍어 저장해 둔 사진(갤러리) 인식**이다. 가이드 박스
  유도는 없다. 임의 구도·품질의 사진이 배포 분포이며, "조연 구도"(기기가
  프레임에서 작게 박힌 사진) 커버리지는 제품 요구사항이다.
- **P2 (#10, 사람 결정)** 합성기를 **우리가 실패하는 이미지와 비슷하게**
  만들어 학습에 반영한다. 실패 이미지의 정체는 로보플로우 실사진 검증
  결과(완전 미검출 포함)다.

## 요구사항 → 작업 대응

| 요구 | 내용 | 작업 |
|---|---|---|
| #1 | 아틀라스 서버를 웹툴 서버 하나로 합친다 | A1 |
| #2 | 기종 화면에서 상단 메뉴가 중복 표시된다 | A2 |
| #3 | 기종/기종 라벨링이 같은 메뉴로 보인다 — 왜 나눴나 | A2·A3 |
| #4 | 합성 코퍼스 필터 이미지 대부분 "Failed to load resource" | A4 |
| #5 | 프로파일 라벨이 기울어져 있다 — 축정렬 라벨만 쓰기로 했다 | A5 |
| #6~#9 | 질문·의견 — 답변 완료, 후속 과제는 B 계열로 | B1~B4 |
| #10 | 합성기 개선(실패 유사 이미지)을 넣는다 | B1 → B3·B4 |

## 조사로 밝혀진 원인 (2026-09-21, 증거)

- **#1** 웹툴(8777)은 `/atlas`·`/imgs/*`·`/api/cases`·`/api/review` 를
  8790 의 `make_failure_atlas.py --serve` 에 HTTP 프록시한다
  (`webtool.py:2209` `_atlas_proxy`). 아틀라스 서버는 요청 처리가 가볍다 —
  상태(페이지 HTML·케이스 JSON·리뷰 저장소·이미지 디렉터리)를 시작 전에
  클래스 속성으로 묶고 `AtlasHandler`(`make_failure_atlas.py:939`)가
  정적 서빙만 한다. 경로 충돌 없음(2026-09-11 확인 주석). 합칠 수 있다.
- **#2** 공유 상단 메뉴(`caf1072`)는 `_send_page` 가 모든 페이지에
  `_topnav` 를 주입한다(`webtool.py:2296`). 그런데 작업대의 기종·아틀라스
  탭은 **iframe** 으로 `/devices`·`/atlas` 를 싣는다(`webtool.html:312,326`).
  iframe 안에 상단 메뉴가 또 주입되어 작업대 탭 머리글과 **두 겹**이 된다.
- **#3** "기종"(`_NAV_TABS`, `/#devices` → 작업대 탭 iframe)과 "기종
  라벨링"(`_NAV_PAGES`, `/devices` 독립 화면)은 **같은 화면의 두 경로**.
  iframe 탭 방식이 독립 화면+공유 메뉴 시대보다 앞서 있어 남은 중복이다.
- **#4** 2026-09-20 폐기 모델 계열 코퍼스(synth_coco/A·A2·A3·B·C·P00·P50·S·Z1·
  S_scale)의 **이미지 파일을 지우고 manifest 만 남겼다**(train2017/README.md
  "이미지를 지웠다"). 웹툴은 manifest 존재만 보고 코퍼스로 등록한다
  (`webtool.py:906` `_synth_dirs`) → 죽은 코퍼스 타일 전부 404. 기본
  코퍼스가 `cs[0]` = 죽은 synth_coco/A 라 무필터 첫 페이지도 깨진다.
  필터는 무죄(이미지 URL 을 바꾸지 않는다 — 선택 효과로 보였을 뿐).
- **#5** 정답 축정렬 전환 커밋 `be7bc93`(2026-09-17, SPEC §9.5)은
  `synth_panel.py`·`build_synth_coco.py`·`synth_view.html` 을 고쳤지만
  **`webtool.py`·`prof_edit.html` 프리뷰는 안 고쳤다**. 프로파일 프리뷰는
  아직 회전 쿼드 `quad` 를 "정답"으로 초록 오버레이하고
  (`webtool.py:1205`·`prof_edit.html:489`), 낡은 주석(`webtool.py:1198`,
  매니페스트 `quad`·구형 `train_band_detector.py` 인용)이 규약과 어긋난다.
  부수: `/api/synth/list`(`webtool.py:1073`)가 `r["quad"]` 직접 인덱싱 —
  box-only 코퍼스에서 KeyError 잠재 위험.

## 작업 분해

### A 묶음 — 웹툴 정비 (새 마일스톤: "웹툴을 한 서버·한 메뉴로 정비한다")

- **A1. 아틀라스를 웹툴 서버로 흡수한다 (#1)**
  - `make_failure_atlas.py` 의 serve 상태 초기화·요청 처리를 함수로 뽑아
    (standalone `--serve` 는 그대로 동작 유지), `webtool.py` 가
    `_atlas_proxy` HTTP 중계 대신 **in-process 로 위임**한다.
  - TF import 는 첫 `/atlas` 요청 시 lazy — 웹툴 기동을 늦추지 않는다.
    초기화(케이스 빌드)는 백그라운드 스레드, 진행 중 안내 응답.
  - 아틀라스 미빌드 시 안내 문구는 "빌드 명령" 을 알려 준다(서버 띄우라는
    옛 503 문구는 폐기). `WEBTOOL.md` 실행 안내 갱신.
  - iframe 내(`/atlas` in 작업대 탭) 주입 규칙은 A2 와 맞춘다.
- **A2. iframe 이중 메뉴를 없앤다 (#2)**
  - `_send_page` 가 iframe 요청에는 `_topnav` 를 주입하지 않게 한다
    (`Sec-Fetch-Dest: iframe` 판별, 폴백으로 `?embed=1`). 기종·아틀라스
    작업대 탭에서 메뉴가 한 겹만 보인다.
- **A3. 같은 화면의 메뉴 항목을 하나로 만든다 (#3)**
  - 기종: 작업대 iframe 탭(`webtool.html` 기종 탭·`fr-devices`)과 독립
    화면 `/devices` 가 같은 화면이다. **독립 화면이 정본** — 작업대 탭의
    iframe 중복을 걷어내고 상단 메뉴에는 한 항목만 남긴다. 아틀라스 탭도
    같은 패턴이므로 같은 규칙을 적용한다(A1 결과물에 맞춤).
- **A4. 죽은 코퍼스를 웹툴이 숨긴다 (#4)**
  - `_synth_dirs`/`_corpus_kind`: 이미지 폴더(images/·train2017/·val2017/)
    에 파일이 있는 코퍼스만 등록(또는 "이미지 없음" 표시). 기본 코퍼스는
    이미지 있는 첫 코퍼스. 타일 `onerror` 로 "이미지 없음(09-20 폐기)" 안내.
  - 폐기 코퍼스의 manifest 자체는 보존한다(지우지 않는다).
- **A5. 프로파일 프리뷰의 정답을 축정렬 상자로 바꾼다 (#5)**
  - `_prof_render`·`/api/prof/preview`·`prof_edit.html`: 정답 오버레이를
    `box`(쿼드 외접 사각형)로. `quad_panel` 점선 오버레이는 SPEC §9.5 가
    매니페스트에서 뺀 필드 — 사람 검증 화면에서도 뺀다. 낡은 주석 정리.
  - `/api/synth/list` 가 `box` 를 전달하고 box-only 코퍼스에서 안 죽게.

### B 묶음 — 검증 체계·합성기 개선

소속: **B1·B2 는 기존 마일스톤 "사진용 밴드 검출기의 장면 불일치를
검증한다"**, **B3·B4 는 새 마일스톤 "실패 사진과 닮은 합성기 — 열화·구도"**.

- **B1. 로보플로우 완전 미검출을 3분할로 잰다 (구도/열화/이물) (#9·#10 근거)**
  - 완전 미검출 장을 ①조연 구도(기기 상대 크기·위치) ②열화(블러·잡음·
    압축) ③실사진 아닌 이물 로 분류해 건수·비율을 낸다. 자는 기존 것을
    재사용(`measure_panel_stats.py`·`report_fail_mix.py` 분류 틀) — 새 자를
    만들면 커밋한다(수치 인용 규칙). 표본 n 을 반드시 적는다.
  - 산출물이 B3·B4 의 예산 배분 근거다(게이트).
- **B2. 이물층(실사진 아닌 사진)에 태그를 단다 (#9)**
  - 로보플로우 개발 분할에서 실사용자가 주지 않을 사진(광고·스크린샷 등)에
    태그를 남겨 게이트 수치가 실제보다 나쁘게 왜곡되지 않게 한다.
    사람이 보고 판정해야 하는 작업 — 웹툴에 최소 조작의 태깅 UI 가 필요하면
    A 묶음 완료 뒤 붙인다.
- **B3. 합성기에 열화 사슬 축을 넣는다 (#10, 게이트: B1)**
  - 블러 → ISO 잡음 → JPEG 사슬을 **이미지 전체(패널 포함)** 에 적용하는
    축으로 `synth_panel`/프로파일 예산에 추가. 축은 분리(per-element 예산 —
    `coupled-budget-loop-defeats-per-element-tuning` 회피). "노이즈 대폭"
    이 아니라 사실의 열화 사슬. 검증은 축별 A/B, `scene-component-split`.
- **B4. 합성기 구도 축을 넓힌다 (#10, 게이트: B1)**
  - 기기 상대 크기·프레임 내 위치·배경 문맥 축을 `device_scene` 계열 위에
    확장. 목표 분포는 B1 의 구도 측정값. 세션 단위 분할 유지
    (`image-level-split-on-session-corpus` 회피).
- **B5. (사람 작업, 대기) rfband rand 층 라벨링 (#7)**
  - 무작위 50장, 우리 규약. fail 층은 새 검출기 실패 장으로 재구성 뒤로
    미룬다. 사람이 여유 있을 때 하는 카드 — 자동 루프가 집지 않게 REVIEW 로.

### 게이트·판단 규칙

- B3·B4 는 B1 의 측정이 나오기 전에 시작하지 않는다(예산 근거 없는
  증강은 `proxy-metric-moves-against-the-goal` 위험).
- B 계열의 채택 판정(게이트 합격선, 커버리지 목표선)은 사람 자리다 —
  카드에 "사람 판정 대기" 로 세운다.
- A 계열은 검증이 서버 기동 + 화면 확인으로 끝나는 위임 가능 작업이지만,
  A1(구조 변경)과 A3(메뉴 정본 결정)은 지시서에 근거를 그대로 싣는다.
- 웹툴은 파이썬 도구다 — `flutter analyze`/`flutter test` 기준이 아니라
  각 카드의 확인 절차(서버 기동, 화면 스크린샷/응답 확인)로 검증한다.
  리포지토리 커밋 기준은 그대로 지킨다.

## 칸반 배치

- 새 마일스톤 1: **"웹툴을 한 서버·한 메뉴로 정비한다"** — A1~A5.
- 기존 마일스톤 "사진용 밴드 검출기의 장면 불일치를 검증한다" 에 B1·B2 추가.
- 새 마일스톤 2: **"실패 사진과 닮은 합성기 — 열화·구도"** — B3·B4(게이트:
  B1). B5 는 마일스톤 밖 사람 대기 카드.
