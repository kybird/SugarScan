---
status: active
version_context: "sugarScan doc/raw"
tags: [process, anti-pattern]
aliases: [무엇을 검증했는지 안 적기, 눈검증 기록, 검증 주어 누락]
created: 2026-09-02
confidence: 4
---
# 무엇을 검증했는지 없이 "통과했다"만 적는 것

`눈검증: glucose_batch1/10 쿼드가 표시 이미지에서 유리 정확히 감쌈`

읽으면 "변환본을 검증했고 통과했다"로 읽힌다. 그런데 **어느 파일의 쿼드였는지가
적혀 있지 않다.**

## 실패 모드

2026-09-02: 위 기록을 근거로 `gmscreen_quads_oriented.jsonl`(변환본)이 검증된
것으로 취급했다. 나중에 재현해 보니 **원본** 쿼드가 유리를 감싸고 변환본은
어긋난다. 즉 당시 통과한 것은 원본이었을 가능성이 높은데, 기록만으로는
**복원이 불가능**하다.

그 결과 이중 회전된 힌트가 "검증됨" 딱지를 달고 살아남았고, 라벨링 재개 직전까지
아무도 의심하지 않았다.

## 점검 체크리스트

- [ ] 검증 기록에 **입력이 무엇이었는지**가 적혀 있는가 (파일명·경로·커밋)
- [ ] 비교 대상이 둘이면 **둘 다** 결과를 적었는가 ("A 는 맞고 B 는 틀렸다")
- [ ] 산출물(크롭 이미지 등)이 남아 있는가, 어디에
- [ ] 재현 명령이 적혀 있는가
- [ ] "통과"가 아니라 **관측한 것**을 적었는가 (유리를 감쌌다 / 오른쪽으로 밀렸다)

## 올바른 대안

```
검증: glucose_batch1/10 (ori=6)
  gmscreen_quads.jsonl 쿼드 → 표시 이미지에 그대로: 유리 정확히 감쌈  ✓
  같은 쿼드에 ori=6 회전 적용   → 오른쪽 밀림, 배터리 아이콘 잘림    ✗
  산출물: _diag/verdict_glucose_batch1__10_{a_asis,b_rotated}.png
```

## 같은 모양의 두 번째 사례 — 실행하지 않은 수정을 "반영됨"으로 적기 (2026-09-04)

2026-09-02 Case 2 는 GT 패딩 버그를 발견하고 이렇게 기록했다:

> `ctc_reader_v2.py` gt_strs: `... if int(v) != NUM_CLASSES` — **이미 반영돼
> 다음 실행부터 자동 정상.**

**그 수정 자체가 틀렸다.** blank 는 `NUM_CLASSES - 1`(10)인데 `NUM_CLASSES`(11)와
비교해서 아무것도 걸러지지 않는다. 재실행해서 숫자를 보지 않았기 때문에
**no-op 이 "완료"로 기록됐고**, 그 거짓 안전 신호가 3일간 유지되며 완전일치를
18.3pp 낮게 찍었다.

주어가 빠진 것과 같은 뿌리다: **관측하지 않은 것을 관측한 것처럼 적었다.**
"고쳤다"는 코드를 고쳤다는 뜻이지 **고쳐졌다**는 뜻이 아니다. 둘을 가르는 것은
수정 후의 숫자 한 줄뿐이다.

- [ ] 수정을 기록할 때 **수정 후 실행 결과**를 함께 적었는가
- [ ] 필터/가드를 고쳤다면 **실제로 걸러진 개수**를 세어 봤는가

## Related

- [[padding-as-content]] — 그 no-op 수정의 대상
- [[metric-path-not-under-test]] — 이 거짓 안전 신호가 계측 버그를 3일 더 살렸다

## Grounding (References)

- `doc/raw/2026-09-02.md#case-1` (원 기록) · `#case-13` (정정)
- `doc/raw/2026-09-04.md#case-2` (`hash:b015830`) — no-op 수정이 "반영됨"으로 기록된 사례
