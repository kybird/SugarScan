---
title: 에러 코드 상태 지원
status: review
ordinal: 8000
created: 2026-09-09
---

## Goal
<!-- kanban:goal:begin -->
혈당계의 E-1 등 에러 표시를 앱이 상태로 다룰지, 인식 실패로 처리할지 정하고 그에 맞는 경로를 구현한다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 제품 방침이 사람 결정으로 확정되고 구현 범위가 정해진다
- [ ] #2 E-1 등 에러 코드가 NormalizedNumber 로 나오지 않는다 — mmol/L 에서 E-1~E-9 가 전부 값이 되지 않음을 테스트가 고정한다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-10T11:06-07:00 — 사람 판정 2026-09-10 (Claude): (b) 인식 실패로 처리한다. 단 '조용한 실패'가 아니라 ReadingNormalizer 가 명시적으로 잡아 값 생산을 거부한다. 강화 프롬프트 §12 의 '유효 상태' 권고는 채택하지 않는다 — 근거 셋. ① HI/LO 는 혈당 정보를 담는다(측정 범위 밖). 에러 코드는 기기 고장이라 혈당 정보가 0 이다. 기록하면 통계·일별 집계가 오염된다. ② E-1 의 의미가 제조사마다 다르다. 상태로 기록하는 것은 우리가 그 뜻을 안다는 주장인데 모른다 — 진단성 문구 금지 규칙에도 걸린다. ③ 실측: 지금 이미 뚫려 있다. E-1 -> 정규화 '1'. mg/dL 은 하한 10 이라 거절되지만 mmol/L 은 하한 0.6 이라 1.0 이 통과하고, E-5 는 5.0 mmol/L 이라는 완전 정상값으로 저장된다. CTC 탑재 전이 아니라 지금 열려 있는 구멍이다(규칙 엔진은 소수점을 읽어 mmol 사용자에게 이미 적용된다).

## Handoff
- 2026-09-09T23:51-07:00 — QUESTION: 혈당계가 E-1 같은 에러 코드를 표시할 때 앱은 (a) 상태로 기록(ScanRejectionReason 확장+UI 문구+l10n) (b) 인식 실패로 처리해 수동 입력 유도 — 어느 쪽인가. 현재 ReadingNormalizer 는 E 를 떼어내 '1' 로 만들 수 있어(reading_normalizer.dart:87 — 글자 교정 후 숫자 외 제거), CTC 리더 탑재 전에 결정이 필요하다. 강화 프롬프트 §12 는 ERROR 를 유효 상태로 다루라고 권고한다.

## Result
