---
title: 합성 프로파일 세트가 기기 모집단을 대표하지 않는다 — 극성이 그 증상
status: doing
ordinal: 46000
created: 2026-09-12
claimed_by: zcode-overnight
claimed_at: 2026-09-24T22:15-07:00
---

## Goal
<!-- kanban:goal:begin -->
합성은 프로파일 12종을 균등 샘플링하는데 실사진 코퍼스는 51종이고 사진 수가 기기마다 크게 다르다. 그래서 어떤 축이든 '실사진 실측값' 에 맞추려 하면 구조적으로 어긋난다. 극성에서 처음 드러났다 — 기기별 실측 극성을 PANEL_ATTRS 에 성실히 반영하니 합성 50.7%(seed 31000)·51.7%(32000)가 나왔다. 100% 반전 기기 3종(ACCU-CHEK Instant·ACURA PLUS·CareSens N Premier, 각 실사진 n=4)이 프로파일 균등 샘플링에서 25% 가중치를 갖는 반면, 실사진 33.7% 는 사진 가중치라 전용 프로파일이 없는 202장(77%, 반전 29.7%)이 이끈다. 대규모 단말인 UltraMini(30장·3.3%)와 CareSens N(19장·0%)은 프로파일 세트에 아예 없다. 이 카드는 극성을 고치는 카드가 아니라 프로파일 세트를 무엇의 표본으로 볼 것인지 정하는 카드다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 사람이 먼저 정해야 한다 — 아래 셋 중 하나. 정해지기 전에는 착수하지 마라
- [x] #2 (가) 기기 충실 유지 + 극성 허용폭을 기기 가중 기준으로 재정의. 극성은 기기 속성이라는 것이 measure_polarity.py 의 전제이고, 검출기는 반전 화면에 더 강해진다. 코퍼스 사진 가중치는 촬영자 편향이지 시장 분포가 아니다 — 오늘 밴드 라벨링 큐를 기기 균등으로 짠 것도 같은 이유다. 구현·측정은 이미 끝나 있다(942e347)
- [ ] #3 (나) 프로파일 가중치를 코퍼스 사진 수로 맞춘다. 통계적으로는 정확하나 generic_v1 이 77% 를 차지해 레이아웃 다양성이 무너진다 — 재설계가 따라온다
- [ ] #4 (다) 프로파일 세트를 넓혀 대규모 단말을 넣는다(UltraMini·CareSens N 등). 근본 해결이지만 프로파일 신설은 근거 사진 실측이 필요하다
- [ ] #5 정해진 뒤: 그 결정을 극성뿐 아니라 다른 축에도 일관되게 적용한다. 지금은 밀도·기하 AC 가 사진 가중 실측값을, 극성은 기기 속성을 전제해 서로 다른 가정 위에 서 있다
- [x] #6 결정을 GLM_TASKS.md 3.8 과 real_baseline.json 의 주석에 적는다 — 다음 사람이 같은 질문을 다시 하지 않게
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-13T19:23-07:00 — 2026-09-13: 이 카드의 '증상' 진단을 정정해야 한다. 극성은 측정 오류였다 — measure_polarity 의 배경 기준이 GM 크롭 전체 중앙값이라 어두운 베젤 기기를 통째로 반전으로 셌다(사람 정답 30장 채점: 구판 19/30, 정상 25장 중 11장 오검). 자를 밴드 히스토그램 봉우리로 바꾸고(커밋 66a7941) 프로파일 극성은 사람 선언으로 뺐다(31d070d). 종횡비 괴리도 증상이 아니었다 — 검증기가 합성 캔버스를 실측 GM 쿼드와 비교하고 있었다(별도 카드). 유리 기준으로 재면 0.800 vs 실측 0.792 로 맞는다. 즉 이 카드가 근거로 든 두 증상이 둘 다 계측 문제였다. 대표성 문제 자체(프로파일 12종 vs 코퍼스 54종, UltraMini 30장·이름모를 가로형모델 18장이 프로파일에 없음)는 그대로 남는다 — 다만 새 근거로 다시 세워야 한다.
- 2026-09-16T22:51-07:00 — 근거 재수집 2026-09-16 (review 세션, 조회 전용). Goal 의 수치가 전부 옛것이고, 일부는 모집단이 뒤바뀌어 있다. 판정 전에 아래를 먼저 볼 것.

[모집단 정정 — 가장 중요] Goal 의 'UltraMini 30장 vs 대부분 4장', 'CareSens N 19장', '실사진 극성 33.7%' 는 코퍼스 사진 수가 아니라 그때의 밴드 라벨 세트(n=84) 수치다. 실제 코퍼스(device_labels.jsonl n=2,494 · 54종)에서는 UltraMini 30장, CareSens N 240장(코퍼스 최대)이다. 즉 '코퍼스 사진 가중치' 라는 카드의 논거가 실제로는 밴드 라벨 가중치였다. 모집단이 다른 두 수를 나란히 놓은 상태다([[comparison-across-different-denominators]]).

[현재 실측 — 2026-09-16, 자는 저장소 것 그대로]
- 코퍼스: device_labels.jsonl n=2,494 사진 / 54 기기(전부 status=identified). 상위: CareSens N 240 · SD CHECK GOLD2 189 · GluNEO plus 173 · 오토-첵 156 · HANDOK BAROZEN II 123 · CareSens N Voice 123. 최소 1장.
- 프로파일: synth_profiles.PROFILES 는 12종이 아니라 15항(기기 프로파일 14 + generic_v1). PROFILE_DEVICES 14키가 코퍼스 17 기기 정체성에 대응해 837장 = 33.6% 를 덮는다. generic_v1 이 나머지 1,657장 = 66.4% 에 대응한다(Goal 의 77% 아님).
- AC#4 의 대표 예시는 이미 절반 해소됐다: onetouch_ultramini 와 wide_unknown(이름모를 가로형모델) 프로파일이 2026-09-13 에 들어갔다. CareSens N(240장)은 여전히 프로파일 없음 — 지금 가장 큰 미커버 기기다.
- 샘플링은 여전히 균등이다(synth_panel.py:208 _PROFILE_WEIGHTS=None). set_wide_share(--wide-share) 만 가로형 비중을 손댄다.

[Goal 의 두 증상은 이미 철회됐다 — 2026-09-13 Note] 극성은 자 오류였고(66a7941 이후 real_baseline 재생성 dddb7e2), 종횡비는 검증기의 프레임 오류였다. 현재 real_baseline.json(생성 2026-09-13T22:18, 극성 자 수정 17:35 이후이므로 post-fix) 의 polarity.inverted_pct = 29.71%, n=276(밴드 라벨). 33.7% 는 더 이상 인용하지 말 것.

[카드가 근거로 든 '기기 편향' 도 크게 줄었다] 2026-09-13 Case 1 의 밴드 라벨은 84장 중 63장(83%)이 세 기기였다. 현재 band_boxes.jsonl 277행(고유 277) 중 코퍼스 내 259장이 54 기기 전부를 덮고, 상위 3기기 합계는 66/259 = 25.5% 다(UltraMini 30 · CareSens N 19 · 이름모를 가로형모델 17, 나머지는 대개 4). 기기 균등 큐가 실제로 작동했다.

[결정이 이미 코드에 반쯤 박혀 있다 — 사람이 볼 것] synth_panel.py:232 GENERIC_INVERTED_P = REAL_BASELINE['polarity']['inverted_pct']/100. 즉 지금 코드는 '기기가 알려진 프로파일 14종은 사람 선언, 기기 미상 잔여(generic_v1)는 실측 사진 가중 분포' 라는 혼합안을 이미 쓰고 있다. AC#5 가 '두 가정이 섞여 있다' 고 지적한 그 자리다. 이 혼합이 결함인지 정답인지가 실질적 질문이다.

[선택지를 다시 좁힌다 — 셋 다 되돌릴 수 있다. 코드 상수·프로파일 표 수정이고 굽은 데이터는 재생성 가능]
(가) 기기 충실 유지 + 허용폭을 기기 가중으로 재정의. 저장소의 기존 원칙과 일치한다 — patterns/no-real-photo-ruler-for-synth(2026-09-15, confidence 5, 사람 지침 '니가 기준을 주고 사람이 따라해야지')가 '합성 설계값을 실사진 통계로 정하지 않는다' 를 이미 못박았고 patterns/declare-what-the-human-knows 가 극성 12줄을 사람 선언으로 되돌린 근거다. 이 경우 AC#5 의 후속은 '밀도·기하 AC 도 실측 기준선에서 떼어낸다' 가 된다.
(나) 프로파일 가중치를 코퍼스 사진 수로 맞춘다. 위 패턴 페이지와 정면으로 충돌한다 — 채택하려면 그 페이지를 deprecated 로 내리고 superseded_by 를 걸어야 한다. 그리고 generic_v1 비중은 77% 가 아니라 66.4% 이므로 '레이아웃 다양성 붕괴' 의 크기도 다시 써야 한다.
(다) 프로파일을 넓힌다. UltraMini·가로형은 이미 들어갔으므로 남은 실질 표적은 CareSens N(240장·프로파일 없음)과 SD CHECK GOLD2(189)·GluNEO plus 계열 정도다. 프로파일 신설에는 근거 사진을 사람이 보고 선언하는 작업이 따른다(재는 것이 아니다 — 같은 패턴 페이지).

[재지 않은 것] 프로파일 대표성이 검출기·리더 성적에 실제로 얼마나 영향을 주는지는 재지 않았다. 상관도 인과도 없다. 재려면 A/B 굽기와 학습이 필요한데 이 세션은 그것을 돌리지 않는다 — 필요하면 별도 카드로 세울 것.
재현: assets_dev/train 에서 device_labels.jsonl 집계, synth_profiles.PROFILE_DEVICES 대조, band_boxes.jsonl × device_labels.jsonl 조인, real_baseline.json 읽기.
- 2026-09-24T21:26-07:00 — RESUMED: 사람 결정 2026-09-24(권고 채택): (가) 합성 프로파일 12종 균등을 기기 충실로 유지하고 허용폭을 재정의한다 — 코퍼스 사진 가중치는 촬영자 편향(UltraMini 30장 vs 대부분 4장), 라벨링 큐를 기기 균등로 짠 것과 같은 논리. 밀도·기하 AC 의 사진 가중 가정도 이 결정에 맞춰 재정의한다(두 가정이 섞여 있던 상태 정리).
- 2026-09-24T22:22-07:00 — (가) 적용 1차(2026-09-24 무인): AC#1 충족(사람 결정 (가) — 재개 노트에 기록). make_real_baseline.py 에 기기 균등 band_du 추가(기기별 중앙→기기 분포 p10/p90 — 세로 54기기·가로 9기기) + synth_panel BAND_GEOM 를 band_du 로 전환 + GLM_TASKS §3.8 가중 기준 블록록(AC#6). 실증: 가로형 사진가중 h p10 0.460→기기균등 0.418 · cx p10 0.304→0.456 — 사진 많은 가로 기기 하나가 만든 왼쪽 치우침이 사진가중 기준선에 과장돼 있었다. 세로형은 두 기준이 거의 동일(h 0.379~0.521 vs 0.387~0.515). 스모크 24장 위반 0. AC#5 잔여: 밀도·링 축 기기 균등화 — collect_density/collect_ring 이 id 를 안 돌려줘 자 리팩터(반환형 확장)가 선행 필요 — 다음 커밋 과제로 남김.

## Handoff
- 2026-09-12T22:14-07:00 — QUESTION: 사람 결정 대기. 합성 프로파일 세트(12종 균등)를 실사진 코퍼스(51종·사진수 불균등)의 무엇으로 볼 것인지 정해야 한다. GLM 은 (가) 기기 충실 유지 + 허용폭 재정의를 추천했고, 나도 (가) 쪽으로 기운다 — 코퍼스 사진 가중치는 촬영자 편향이고(UltraMini 30장 vs 대부분 4장) 오늘 라벨링 큐를 기기 균등으로 짠 것도 같은 이유다. 다만 이건 '합성이 무엇을 닮아야 하는가' 라는 제품 판단이라 내가 정하지 않는다. 근거 수치는 카드 Goal 에 전부 있다. 결정이 극성 하나로 끝나지 않는다는 점이 중요하다 — 지금 밀도·기하 AC 는 사진 가중 실측값을 쓰고 극성만 기기 속성을 전제해 두 가정이 섞여 있다.

## Result
