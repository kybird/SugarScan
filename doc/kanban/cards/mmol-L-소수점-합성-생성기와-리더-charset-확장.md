---
title: mmol/L 소수점 합성 생성기와 리더 charset 확장
status: review
ordinal: 13000
created: 2026-09-11
---

## Goal
<!-- kanban:goal:begin -->
소수점을 포함한 mmol/L 표시를 합성으로 만들고 리더 charset 에 소수점 클래스를 더해, 실촬 데이터가 없는 상태에서도 소수점 경로를 측정 가능하게 만든다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 synth_lcd.py 가 한 자리 소수(3.0~20.0) 표시를 렌더한다 — 소수점 위치가 고정이 아닌 표본을 포함한다
- [ ] #2 리더 charset 이 0~9 + 소수점으로 확장되고, 기존 캐시의 라벨이 그대로 유효함을 테스트가 고정한다
- [ ] #3 합성 홀드아웃에서 소수점 표시의 완전일치·자릿수 붕괴율이 보고된다
- [ ] #4 정수 표시 성적이 확장 전 대비 얼마나 변했는지 기존 기기 단절 홀드아웃 1,138장에서 짝비교로 보고된다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T11:16-07:00 — 가장 틀리기 쉬운 자리 — 클래스 인덱스. ctc_reader_v2.py:35 는 NUM_CLASSES=11, blank=10(마지막 인덱스)이고 라벨 패딩도 10 이다(ctc_reader_v2.py:368). 소수점을 인덱스 10 에 끼워 넣으면 기존 캐시 전부의 패딩 10 이 '소수점'으로 읽혀 라벨이 조용히 오염된다. 반드시 소수점 = 인덱스 11, blank = 10 을 유지하고 blank_index 를 명시적으로 10 으로 넘길 것(ctc_reader_v2.py:61 의 NUM_CLASSES-1 자동 계산을 쓰지 말 것). 기존 data_cache_v2.npz 를 그대로 읽어 학습이 되는지가 이 카드의 첫 검증이다.
- 2026-09-11T11:16-07:00 — 소수점 위치를 고정하지 말 것. 기기마다 소수점 자리가 다르고(예: 5.6 과 12.3), 자릿수 결합 로직이 정확히 거기서 갈린다. 합성 표본에 두 자리수+소수(12.3)와 한 자리수+소수(5.6)를 모두 넣는다. 값 범위 3.0~20.0, 한 자리 소수.
- 2026-09-11T11:16-07:00 — 실촬 데이터는 이 카드의 범위가 아니다. 카드 'mmol/L 실츬 데이터 확보'(사람 결정 대기, 2~3종 x 50~100장 기준이 Notes 에 있음)가 따로 있고, 합성만으로 성적을 주장하지 않는다 — 보고서에 '합성 전용 측정이며 실기기 전이는 미검증'을 명시한다. 강화 프롬프트 §21 우선순위(실데이터 > 포토메트릭 증강 > 복붙 > 완전합성)를 뒤집는 것이 아니라, 실데이터가 0건인 동안의 임시 측정 수단이다.
- 2026-09-11T11:16-07:00 — 건드리지 말 것: 앱 코드(lib/) 전부. 이 카드는 assets_dev/train 안에서만 끝난다. 리더는 아직 앱에 없다(ocr_bootstrap 에 등록된 것은 SegmentRuleEngine 과 7seg CNN 둘뿐). 앱의 GlucoseUnit.format 이나 검증기 하한(mmol 0.6)도 건드리지 않는다.
- 2026-09-11T18:28-07:00 — 범위 조정(2026-09-11 scratch 재구축): 리더를 처음부터 다시 만들므로 charset 은 설계 시점에 0~9 + 소수점으로 잡는다. 기존 캐시의 blank 인덱스 충돌 문제(이 카드 첫 Note)는 새 리더에서는 발생하지 않는다 — 다만 옛 캐시를 재사용하는 경로가 남아 있으면 그때는 그대로 유효하다. 합성 소수점 렌더는 여전히 필요하다.
- 2026-09-11T23:58-07:00 — 판정 2026-09-12 — (a) 새 리더 설계 시점으로 미룬다. GLM 이 낸 두 선택지 중 (a). 지금 charset 을 늘리면 아직 존재하지 않는 리더의 설계를 미리 박는 것이고, (b) 합성 소수점 렌더만 먼저 하는 길도 합성이 물리 패널 기준으로 재구성되는 중이라 충돌한다. AC#2(기존 캐시 라벨 유효성 고정)와 AC#4(옛 기기 단절 홀드아웃 짝비교)는 폐기된 옛 리더를 전제하므로 새 리더가 정해질 때 다시 쓴다. 무인 에이전트는 이 카드를 잡지 마라.
- 2026-09-13T10:20-07:00 — 2026-09-13 시기상조 판정 — 이 카드는 '리더' 단계를 전제한다. 옛 리더(checkpoints_v2/pre_best · reader_model)는 2026-09-11 scratch 재구축으로 폐기됐고, 새 리더를 만드는 카드가 보드에 없다. 지금 파이프라인은 검출기 단계다(밴드 쿼드 검출기 v0 → 합성기 정비 → 재학습). 리더가 없는 상태에서 이 카드를 열면 폐기된 산출물을 대상으로 작업하게 된다 — 2026-09-11 에 이미 그 사고가 났다. 새 리더 카드가 서기 전까지 REVIEW 에 둔다. 사람이 판정할 것은 '지금 하자/말자' 가 아니라 '리더 단계를 언제 시작하나' 하나뿐이고, 그건 이 카드가 아니라 로드맵 결정이다. 새 리더가 생기면 이 카드의 AC 를 그 리더 기준으로 다시 쓰고 resume 하라.
- 2026-09-16T22:51-07:00 — 근거 조회 2026-09-16 (review 세션, 조회 전용). 2026-09-13 Note 의 대기 조건('새 리더 카드가 서기 전까지 REVIEW')은 충족됐다 — 다만 그 새 카드가 이 카드를 명시적으로 배제한다.
- 「CRNN CTC 리더를 새로 만든다」 카드가 2026-09-16 에 섰다(status todo · ordinal 55000 · 마일스톤 '합성만으로 사진에서 값까지 한 번 통과시킨다').
- 그 카드 Goal 원문: 'charset 은 0~9 만 — HI/LO·소수점은 이 마일스톤 밖이다.' 즉 새 리더는 설계 단계에서 소수점을 뺐다.
- 마일스톤 6장 전체(합성 COCO 내보내기 52000 → YOLOX 파인튜닝 53000 → 세트 B 추론 54000 → CRNN 구현 55000 → 예측 상자로 리더 학습 56000 → 세트 C 끝단 평가 57000)에 소수점·charset 확장 단계가 없다.
따라서 사람이 답할 것은 둘 중 하나다 — (1) 이 카드를 이번 마일스톤 뒤(리더 v1 완성 후) 순서로 미루고 지금은 그대로 REVIEW 에 둔다. 되돌릴 수 있다. (2) 마일스톤 안으로 끌어와 새 리더 charset 을 처음부터 11클래스(0~9+소수점)로 잡는다 — 되돌리기 비싸다. 리더를 한 번 학습한 뒤 charset 을 늘리면 재학습이고, 이 카드 첫 Note 의 blank 인덱스 충돌이 새 리더에서 다시 살아난다.
AC 재작성은 아직 불가능하다. AC#2(기존 캐시 라벨 유효성)와 AC#4(옛 기기 단절 홀드아웃 1,138장 짝비교)는 새 리더의 캐시·홀드아웃이 정해진 뒤에야 다시 쓸 수 있고, 그 캐시는 마일스톤 첫 카드(COCO 내보내기, 현재 doing)가 끝나야 형태가 잡힌다.
참고로 옛 산출물은 디스크에 남아 있다(reader_model/ · checkpoints_v2/ · reader_model.onnx 2026-09-10). 2026-09-11 '폐기' 는 결정이지 삭제가 아니다 — 이 파일들을 새 리더의 기준선으로 쓰지 말 것.
- 2026-09-17T00:28-07:00 — 전제 하나가 바뀌었다(2026-09-17 무인 루프). 이 카드의 Handoff 는 '(a) 새 리더 설계 시점으로 미룰지 (b) 합성 소수점 렌더만 남겨 지금 할지' 를 묻고 그 판단이 '새 리더가 아직 없는 지금' 을 전제한다. 어젯밤 새 리더가 생겼다 — assets_dev/train/reader_crnn.py (torch CRNN+CTC, 50장 과적합 완전일치 100% 로 구현 확인). 다만 charset 이 0~9 뿐이라 소수점은 아직 없고, 그 상수는 파일 상단 한 곳(CHARSET·BLANK·NUM_CLASSES)에 모여 있어 확장 지점이 명확하다. (a)/(b) 선택 자체는 여전히 사람 몫이라 카드는 세워 둔 채로 두고 사실만 적는다.
- 2026-09-17T01:16-07:00 — 정정 보강 2026-09-17 (같은 세션, 뒤늦게 확인). 앞 Note 에서 '새 리더가 소수점을 명시적으로 뺐다' 고만 적었는데, **정본 계획은 반대로 적혀 있다.**
docs/OCR_REBUILD_PLAN.md:114-115 §4 리더 v1 원문: 'charset: 0~9 + 소수점. **처음부터 소수점을 넣는다** — 나중에 끼워 넣으면 기존 캐시의 blank 인덱스와 충돌한다.' 2026-09-11 재구축 결정의 정본 문서다.
마일스톤 카드 「CRNN CTC 리더를 새로 만든다」(2026-09-16)는 'charset 은 0~9 만 — HI/LO·소수점은 이 마일스톤 밖이다' 로 정했다. 두 문서가 이 축에서 갈라져 있고, 어느 쪽이 정본인지 아무 데도 적혀 있지 않다. 사람이 답할 질문에 이것이 먼저 온다 — 앞 Note 의 (1)/(2) 선택지는 그 다음이다.
다만 §4 가 든 사유는 새 리더에 그대로 적용되지 않는다. reader_crnn.py 는 npz 캐시가 아니라 COCO 세트에서 읽으므로(reader_crnn.py:15·87·220) '기존 캐시의 blank 인덱스 충돌' 이 일어날 자리가 없다. 나중에 charset 을 늘릴 때 드는 비용은 데이터 오염이 아니라 **재학습 1회**다. 즉 §4 의 결론('처음부터 넣는다')은 여전히 옳을 수 있지만 그 근거는 바뀌었다 — 근거가 바뀐 채 결론만 인용하지 말 것.
- 2026-09-24T21:26-07:00 — 사람 결정 2026-09-24: 미루기 — 새 리더 설계 시점으로 순서 이관(옛 리더·옛 홀드아웃을 전제한 AC#2·AC#4 는 실효 없음 — 카드 본문 참조).

## Handoff
- 2026-09-11T20:08-07:00 — QUESTION: scratch 재구축(2026-09-11) 뒤 AC 재조정 필요: AC#2(기존 캐시 라벨 유효성 고정)와 AC#4(기존 기기 단절 홀드아웃 1,138장 짝비교)가 폐기된 옛 리더·옛 홀드아웃을 전제한다. 새 리더가 아직 없는 지금 이 카드를 (a)새 리더 설계 시점으로 순서를 미룰지 (b)합성 소수점 렌더만 남겨 지금 할지 정해야 한다. 참고: 2026-09-11 무인 세션이 pick 을 돌렸을 때 ordinal 최저 카드라 자동으로 잡혔으나 킥오프의 수행 가능 카드 목록에 포함돼 있지 않아 반납한다.

## Result
