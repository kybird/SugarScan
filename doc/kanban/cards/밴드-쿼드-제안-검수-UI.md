---
title: 밴드 쿼드 제안 검수 UI
status: review
ordinal: 25000
created: 2026-09-11
depends_on: ["합성만으로 밴드 쿼드 검출기 v0"]
---

## Goal
<!-- kanban:goal:begin -->
사람이 쿼드를 그리는 대신 제안을 O/X 로 검수하게 만든다. 틀린 것만 모아 다음 학습의 hard example 로 쓴다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 웹툴에 검수 모드가 생긴다 — 제안 쿼드가 사진 위에 겹쳐 보이고 키 하나로 승인·거절이 된다
- [ ] #2 승인된 쿼드가 band_boxes.jsonl 에 source 를 구분해 기록된다 — 사람이 그린 것과 모델 제안을 승인한 것이 섞이지 않는다
- [ ] #3 거절된 장의 목록이 따로 쌓인다 — 다음 학습의 hard example
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T20:54-07:00 — 설계 원칙: 사람은 그리지 않는다. 기본 동작은 승인·거절 두 키뿐이고, 편집은 예외 경로다. 그리기를 기본으로 두면 2,494장을 손으로 그리는 원래 문제로 돌아간다.
- 2026-09-11T20:54-07:00 — source 구분이 중요하다. band_boxes.jsonl 은 사람이 그린 라벨의 정본이고 gitignore 예외로 추적되는 파일이다(노동이 들어갔고 다시 만들 수 없다). 모델 제안을 승인한 것을 human 으로 기록하면 둘을 영영 구분할 수 없게 된다. 승인 라벨은 별도 source 값으로 남기고, 필요하면 파일을 나눠라.
- 2026-09-11T20:54-07:00 — 거절 사유를 한 글자라도 남기게 하지 마라. 사유 입력을 넣는 순간 장당 1초가 10초가 되고 검수가 다시 노동이 된다. 거절은 거절로만 쌓고, 왜 틀렸는지는 거절 목록을 나중에 몽타주로 묶어 한 번에 본다.
- 2026-09-11T20:54-07:00 — 검수도 기기 단절 분할을 따른다. train 쪽 승인 라벨은 학습에, holdout 쪽은 평가에 쓴다 — 섞이면 검출기 성적이 무의미해진다.
- 2026-09-11T23:56-07:00 — 시점 정정(2026-09-12): 이 카드는 사람은 절대 그리지 않는다 설계에서 나왔는데 그 설계는 순환이라 접었다. 지금은 씨앗 라벨을 사람이 그리고 검출기는 합성으로 세운다. 검수 UI 는 검출기 v0 이 게이트를 통과해 2,494장에 제안을 뿌릴 수 있게 된 뒤에 의미가 있다. 그 전에 만들면 빈 화면이다.
- 2026-09-16T22:52-07:00 — 근거 조회 2026-09-16 (review 세션, 조회 전용 — 검출기 추론을 돌리지 않았다. GPU 를 다른 세션이 쓰고 있다).

[게이트 수치의 모집단이 그동안 3배로 커졌다 — 이것이 판정의 핵심이다]
v0 Result 의 'IoU 중앙 0.669' 는 사람 밴드 라벨 84장 기준이다(2026-09-12). 오늘 band_boxes.jsonl 은 277행(고유 277, 전량 source=human)이고 그중 코퍼스 내 259장이 54 기기를 전부 덮는다. 상위 3기기 집중도는 66/259 = 25.5% 로, 2026-09-13 Case 1 이 지적한 84장 시절의 83%(63/76)에서 크게 내려왔다. 즉 0.669 는 기기 편향 표본에서 나온 수치이고, 지금 다시 재면 달라질 수 있다 — 올라갈지 내려갈지는 모른다. v0 의 기기별 최하위가 OneTouch UltraMini 0.548 이었는데 그 기기가 84장 중 30장이었으므로 중앙값을 끌어내렸을 가능성이 있다.

[필요한 측정 — 이 세션은 돌리지 않았다]
자는 이미 있다: assets_dev/train/eval_band_detector.py gate. 이걸 현재 band_boxes.jsonl(277행)로 한 번 돌리면 '지금 검출기가 얼마나 좋은가' 가 새 모집단에서 나온다. 추론이 필요하므로 GPU 가 한가할 때 사람이나 다른 세션이 돌려야 한다. 주의 두 가지 — (1) 어느 체크포인트로 재는지 보고서에 적을 것. v0 게이트의 ckpt 와 현재 band_quads_pred.jsonl 의 ckpt 가 다르다(아래). (2) eval_band_detector.py 머리말 주석은 아직 '97장' 이라고 적혀 있다. 실제 n 은 band_exclusions.drop 과 gm_quads 조인 뒤 값이므로 출력에 찍힌 n 을 인용할 것 — 오늘 재지 않았다.

[현재 제안 파일 상태]
band_quads_pred.jsonl 2,511행(2026-09-16 21:42 갱신). 각 행에 source=detector · ckpt=band_det_m22_3k.pt · arch=fc · gm_source=human 이 붙어 있다. v0 Result 의 전량 제안은 2,497장이었으므로 지금 파일은 그때 것이 아니다 — 다른 세션이 갱신 중일 수 있으니 이 카드를 열기 전에 어느 ckpt 의 제안을 검수 대상으로 삼을지부터 고정해야 한다.

[AC#2 는 이미 절반 준비돼 있다]
band_boxes.jsonl 의 각 행에 source 필드가 이미 있고 현재 277행 전부 'human' 이다. 사람이 그린 것과 승인된 제안을 섞지 않는 장치가 파일 포맷 수준에서는 이미 존재한다 — 새 값(예: 'approved')을 쓰기만 하면 된다. 파일을 나눌 필요가 있는지는 별개 판단이다.

[선택지 — 둘 다 되돌릴 수 있는 정도가 다르다]
(가) 재학습 뒤로 미룬다. 되돌릴 수 있다(아무것도 쓰지 않는다). 근거: v0 약점 3개의 처방이 전부 '데이터만 바꾸고 재학습' 이고, 그 데이터를 바꾸는 카드들이 지금 보드 TODO 에 있다(합성 국소 광원 · 7-세그먼트 글리프 정밀화 · 밀도 최대 선택 · 실사진 밴드 라벨로 파인튜닝). 또 현재 지시된 우선순위는 마일스톤 '합성만으로 사진에서 값까지 한 번 통과시킨다' 소진이고 이 카드는 그 멤버가 아니다.
(나) 지금 연다. 되돌리기 어렵다 — 약한 v0 의 제안을 사람이 승인하면 그 라벨이 band_boxes.jsonl 에 정본으로 쌓인다. source 구분이 있어 사후 분리는 가능하지만, 승인에 들어간 사람 시간은 되돌릴 수 없다. 다만 UI 를 만드는 것과 승인을 시작하는 것은 분리할 수 있다 — AC#1~3 을 구현만 해 두고 검수 개시는 재학습 뒤로 미루는 절충이 있다. 이 경우 되돌림 비용은 (가) 와 같아진다.

[재지 않은 것] 현재 검출기의 IoU 를 재지 않았다. 검수 UI 가 라벨 확보 속도를 얼마나 올리는지도 재지 않았다 — 근거 없이 '빠르다' 고 말하지 말 것.

## Handoff
- 2026-09-16T22:49-07:00 — QUESTION: 무인 루프(2026-09-16)의 오픽으로 doing 이 됐다 — pick --claim 에 작업자 이름 대신 카드 제목을 넣어 ordinal 최저인 이 카드가 집혔다. 되돌려 세운다. 함께 판단이 필요한 것: 이 카드를 지금 열 것인가. 의존(합성만으로 밴드 쿼드 검출기 v0)은 done 이고 2,497장 전량 제안이 존재하므로 기술적으로는 막혀 있지 않다. 다만 그 카드의 Result 가 게이트 IoU 중앙 0.669(84장 사람 라벨 기준)로 '진행하되 약점 명시' 였고, 약점 3개(가로형 정보칼럼 부재·크롭 걸침·밴드 크롭 밖 13장)의 처방이 '데이터만 바꾸고 재학습' 이다. 약한 v0 의 제안을 지금 사람이 승인하면 그 라벨이 정본으로 굳는다. (가) 재학습 뒤로 미룬다 (나) 지금 연다 중 택일이 필요하다. 또한 현재 지시받은 우선순위는 마일스톤 '합성만으로 사진에서 값까지 한 번 통과시킨다' 소진이고 이 카드는 그 멤버가 아니다.

## Result
