---
title: 7-세그먼트 글리프 정밀화
status: review
ordinal: 34000
created: 2026-09-11
depends_on: ["합성만으로 밴드 쿼드 검출기 v0","합성 글리프 네 결함 — 콜론·글자 크기 종수·배터리·화살표 위치"]
---

## Goal
<!-- kanban:goal:begin -->
글리프가 이 프로젝트의 중심이다. DSEG 도입으로 형상은 잡혔지만 실물과의 잔차를 더 좁힌다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 실사진 글리프와 합성 글리프를 같은 크기로 겹쳐 놓은 대조 이미지가 자릿수별로 나온다
- [ ] #2 획 굵기·세그먼트 간격·기울기의 실사진 분포를 측정해 합성 파라미터 범위와 나란히 보고한다
- [ ] #3 꺼진 세그먼트 잔상의 농도가 실사진 측정값에 맞춰진다 — 현재 15% 확률은 근거 없이 정한 값이다
- [ ] #4 DSEG 변형(Classic/Modern, 굵기, 이탤릭) 중 어느 조합이 실사진에 가까운지 근거와 함께 고른다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T23:56-07:00 — 리더용 카드다(2026-09-12 구분). 폐기된 「합성 LCD 광학 재현」의 픽셀 격자·편광 항목을 여기서 가져간다. 검출기가 어떤 크롭을 내는지 정해진 뒤에 착수한다.
- 2026-09-13T19:23-07:00 — 2026-09-13: AC#3(잔상 15% 확률은 근거 없는 값) 이 더 급해졌다. 형질 분리 작업에서 잔상을 '기기 형질'로 고정했다 — 근거 없는 값을 기기별로 못박은 셈이라, 지금은 어떤 기기가 잔상을 갖는지가 crc32 씨앗이 정한다. 실측으로 대체하기 전까지는 임의값이다.
- 2026-09-13T19:23-07:00 — 자간이 눈으로 가장 먼저 걸린다 — seg_char_advance 가 글자마다 셀 폭 + 0.13h 간격을 무조건 붙여 'mg/dL' 이 'm g / d L' 로 벌어진다. 실사진(842·1058·267)의 단위 표기는 붙어 있다. AC#2 의 '세그먼트 간격' 항목이 이걸 포함해야 한다.
- 2026-09-17T00:07-07:00 — AC#1 산출: assets_dev/train/make_glyph_compare_sheet.py 신설. 두 판을 낸다 — _diag/glyph_compare/glyph_compare.png(자릿수마다 위=실사진 6~16개, 아래=합성, 같은 높이)와 glyph_overlay.png(자릿수마다 실사진 평균과 합성 평균을 빨강/초록으로 겹친 판). 명령: python make_glyph_compare_sheet.py --per-digit 16 --synth-count 900. 표본은 자릿수마다 실사진 16 · 합성 16.
- 2026-09-17T00:07-07:00 — 슬롯 자르기 규약을 실사진과 합성에 **똑같이** 적용한 것이 이 시트가 공정한 이유다: 밴드 상자를 자릿수만큼 균등 분할해 한 칸을 자른다. 3자리 값만 쓴다 — 2자리는 앞 빈자리가 밴드에 포함되는지가 기기마다 갈려(band_label_slot_audit.py, 2026-09-14) 슬롯 번호와 글자가 어긋난다. 실사진 밴드는 사람 라벨(band_boxes.jsonl)만 쓴다 — 검출기 출력을 쓰면 검출 오차가 글리프 차이로 보인다.
- 2026-09-17T00:07-07:00 — 만들면서 버그를 하나 냈다가 잡았다. 처음엔 합성 쪽에서 rects(워프 **전** 패널 좌표)로 panel(워프 **후** 그림)을 잘랐다. 기울어진 장에서 이웃 칸과 mg/dL 이 끌려 들어와 시트가 거짓말을 했다. quad_panel -> quad 호모그래피를 되찾아 슬롯 네 귀를 옮긴 뒤 자르는 것으로 고쳤다. [[unnamed-coordinate-frame]] 이 가리키던 자리를 그대로 밟았다 — 시트를 눈으로 안 봤으면 그대로 보고했을 것이다.
- 2026-09-17T00:07-07:00 — 겹친 판을 '모양'과 '자리' 두 줄로 나눴다. 중심을 안 맞추고 겹치면 슬롯 분할이 한 칸 어긋나기만 해도 판 전체가 옆으로 밀려 기울기 차이처럼 읽힌다. 위 줄은 잉크 무게중심을 맞춰 모양만 보고, 아래 줄은 자른 그대로 둬서 자리 차이를 본다. 아래 줄의 좌우 어긋남은 글리프 설계가 아니라 슬롯 규약일 수 있다 — 그 구분은 사람이 해야 한다.
- 2026-09-17T00:07-07:00 — 눈으로 보이는 것(재지 않았다, 판정도 하지 않는다): 합성 숫자가 실사진보다 더 기울어 보인다(1·4·7 에서 가장 뚜렷). 합성의 세그먼트 사이 틈이 더 넓고 고르게 보이고, 실사진 쪽은 획 끝이 더 뭉툭하다. 실사진 0·8 에는 꺼진 세그먼트 잔상이 눈에 띈다. 이건 관찰이지 측정이 아니다 — 값은 사람이 부른다.

## Handoff
- 2026-09-17T00:07-07:00 — QUESTION: AC#1 은 끝냈다(대조 시트 두 판, _diag/glyph_compare/). 나머지 셋은 내가 정하면 안 되는 자리라 세워 둔다. AC#2·#3 이 저장소 규칙과 정면으로 부딪힌다. 이 카드는 2026-09-11 에 썼는데 antipatterns/no-real-photo-ruler-for-synth 가 2026-09-15 에 섰다. 그 규칙은 '합성의 설계값을 실촬 사진을 재서 정하지 않는다 — 합성이 기준을 주고 사람이 보고 값을 부른다' 이고, 근거로 실물 잉크가 밴드의 0.99 를 채운다고 재서 되먹였다가 521장이 잘린 사고를 든다. 그런데 AC#2 는 '획 굵기·세그먼트 간격·기울기의 실사진 분포를 측정해' 이고 AC#3 은 '잔상 농도가 실사진 측정값에 맞춰진다' 다. 둘 다 규칙이 금지하는 되먹임이다. 선택지: (가) AC#2·#3 을 규칙에 맞게 다시 쓴다 — '측정한다' 를 '시트를 보고 사람이 값을 선언한다' 로 바꾸고, 정한 사람과 날짜를 코드에 남긴다(규칙이 요구하는 바다). (나) 글리프는 예외로 두고 측정을 허용한다 — 그렇다면 왜 여백·자리와 다른지를 규칙 페이지에 적어야 한다. (다) 카드를 접고 규칙에 맞는 새 카드로 다시 세운다. 나는 (가) 를 추천한다. 시트가 이미 있어서 값을 부르는 데 필요한 것은 다 갖췄고, AC#3 의 '15% 는 근거 없이 정한 값' 이라는 문제 제기도 규칙 안에서 풀린다 — 규칙은 판단으로 정하는 것을 허용하되 '누가 언제 정했는지를 코드에 남기라' 고 한다. 지금 15% 에 없는 것은 근거가 아니라 서명이다. AC#4(DSEG 변형 선택)도 같은 이유로 사람 몫이다 — 시트를 보고 고르면 된다. 참고로 눈에 보이는 것: 합성이 실사진보다 더 기울어 보이고, 세그먼트 틈이 더 넓고 고르며, 실사진 쪽 획 끝이 더 뭉툭하다. 관찰이지 측정이 아니다.

## Result
