---
title: 실사진 밴드 라벨로 검출기 파인튜닝 — 기기 단절 분할
status: todo
ordinal: 45000
created: 2026-09-12
---

## Goal
<!-- kanban:goal:begin -->
밴드 라벨 277장(join 264, 51종)을 찍어 놓고 학습에는 한 장도 쓰지 않고 있다. v0 카드가 '합성만으로, 사람 라벨은 게이트 전용' 으로 못박았고 97장(사실상 3기기)일 때는 그게 맞았다 — 학습하기엔 적고 평가셋이 오염된다. 51종이 된 지금 전제가 바뀌었다. 그리고 이것이 도메인 격차를 줄이는 가장 직접적인 길이다: 같은 모델이 학습 합성에서 IoU 0.901, 실사진에서 0.727 이다(격차 0.174). 합성을 고쳐 간접적으로 좁히는 것보다 실사진 파인튜닝이 빠르다. 기기 단절 분할로 홀드아웃 기기를 떼면 게이트는 여전히 정직하다 — 리더에서 이미 쓴 방식이다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 기기 단절 분할을 만든다 — 기기(brand+model+variant) 단위로 학습/홀드아웃을 가른다. 같은 기기의 사진이 양쪽에 들어가면 안 된다. build_device_split.py 의 규칙과 고정 시드를 재사용하고 재구현하지 마라. 분할 결과(기기 수·장수)를 적는다
- [x] #2 홀드아웃에 가로형과 세로형이 모두 들어가고, 지금 약한 기기(OneTouch UltraMini · On Call Extra · KOOKMIN Check · GluNEO plus)가 한쪽에 몰리지 않는지 확인한다. 몰리면 시드를 바꾸지 말고 그 사실을 보고한다
- [ ] #3 합성으로 사전학습한 뒤 실사진 학습셋으로 파인튜닝한다. 실사진만으로 처음부터 학습하지 마라 — 184장 규모로는 무리다. 사전학습 체크포인트와 파인튜닝 예산(에폭·lr)을 적는다
- [ ] #4 미학습 기기 홀드아웃에서 게이트를 잰다 — det IoU median·p10, 기기별 분해. 이것이 정본 수치다
- [ ] #5 세 값을 나란히 놓는다 — 합성 학습셋 IoU · 파인튜닝 전 실사진 IoU(v0 = 0.727, 잘린 장 제외 246장) · 파인튜닝 후 미학습 기기 IoU. 도메인 격차가 얼마나 줄었는지가 이 카드의 답이다
- [x] #6 잘린 장(사람 라벨이 GM 크롭 안에 0.95 미만)은 따로 집계한다 — 하드 실링이라 파인튜닝으로 안 고쳐진다. band_det_clip_split.py 로 가른다
- [x] #7 학습에 쓴 사진 id 목록을 산출물로 남긴다. 이후 어떤 평가도 그 사진을 쓰면 안 된다 — 목록이 없으면 다음 사람이 모르고 오염시킨다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-12T21:08-07:00 — 2026-09-12 이 카드가 왜 지금 생겼나 — 사람이 지적했다. 밴드 라벨 180장을 찍은 직후 내가 세운 계획(합성 조준 → 재학습)에는 그 라벨을 학습에 쓰는 카드가 없었다. v0 의 '사람 라벨은 게이트 전용' 규칙을 전제가 바뀐 뒤에도 그대로 들고 있었던 것이다. 97장·3기기일 때 맞던 규칙이 277장·51종에서도 맞는지 다시 묻지 않았다.
- 2026-09-12T21:08-07:00 — 2026-09-12 근거 수치 — 같은 v0 체크포인트로 잰 값이다. 학습에 쓴 합성 2,600장에서 det IoU median 0.901 p10 0.824(세로형 0.904 · 가로형 0.862), 실사진에서 0.727 p10 0.570(잘린 장 제외 246장). 두 몫이 있다: 합성에서조차 1.0 이 아닌 0.10(훈련·용량 여유)과 합성→실사진 0.174(도메인 격차). 도메인 격차가 더 크다. 에폭만 늘려서는 0.83 근처가 한계다. 재현: python band_det_synth_gap.py --images <synth>/images --manifest <synth>/manifest.jsonl / python eval_band_detector.py gate --ckpt band_det_v0.pt
- 2026-09-12T21:08-07:00 — 2026-09-12 분할 규모 — join 264장 51종, 기기당 중앙 4장(최대 30 OneTouch UltraMini, 최소 1). 기기 70/30 이면 대략 학습 35종 184장 / 홀드아웃 16종 80장이다. 184장은 작다 — 그래서 AC#3 이 합성 사전학습 위에 파인튜닝하라고 못박는다. 실사진만으로 처음부터 학습하면 그 규모에서 무너진다.
- 2026-09-17T00:14-07:00 — AC#1 분할 완료 — assets_dev/train/build_band_device_split.py 신설. 규칙을 재구현하지 않고 build_device_split 에서 device_key·SEED(20260911)·HOLD_TARGET/MAX·SHARE_CAP 을 import 한다. 바뀌는 것은 모집단뿐(리더는 캐시 풀, 여기는 band_boxes.jsonl). 결과: 밴드 라벨 277장 중 기기 라벨과 조인되는 것 259장(18장은 기기 라벨 없음), 기기 54종. train 140장/26종 · holdout 119장/28종(45.9%). 교차 기기 0종. 홀드아웃 단일 기기 최대 비중 CareSens/N 19장 16.0%(상한 25%).
- 2026-09-17T00:14-07:00 — AC#2 결과가 중요하다 — **홀드아웃에 가로형이 0장이다.** 시드를 바꾸지 않았다(카드 지시대로 사실을 적는다). 원인은 시드 운이 아니라 구조다: 밴드 라벨이 붙은 가로형 47장이 기기 두 종뿐이고(OneTouch/UltraMini 30장 · 이름모를 가로형모델 17장) 기기 단절 분할이라 그 둘이 통째로 한쪽에 간다. 홀드아웃에 가로형을 넣으려면 둘 중 하나를 홀드아웃으로 보내야 하는데 그건 시드가 아니라 규칙을 바꾸는 일이다. 결과적으로 이 분할의 게이트는 **가로형 성능을 전혀 재지 못한다.** 약한 기기 넷 중 셋이 train 이다(UltraMini·On Call Extra·GluNEO plus), KOOKMIN Check 만 holdout.
- 2026-09-17T00:14-07:00 — AC#6 잘린 장(사람 밴드 라벨이 GM 크롭 안에 0.95 미만): train 14/137장(10.2%) · holdout 3/119장(2.5%). 자는 band_det_clip_split.inside_frac() 을 그대로 부른다 — 새로 짜지 않았다. train 쪽 분모가 140 이 아니라 137 인 것은 3장이 gmscreen_quads_oriented 에 없어 포함률을 잴 수 없기 때문이다.
- 2026-09-17T00:14-07:00 — AC#7 산출: _diag/band_device_split/band_device_split.json — train_ids·holdout_ids·기기 목록·잘린 장 목록·시드·규칙 출처를 담았고 '이후 어떤 평가도 train_ids 를 쓰면 안 된다' 를 파일 안에 적었다.
- 2026-09-17T00:14-07:00 — 보고 실수 하나를 잡았다. 처음 실행에서 'OneTouch UltraMini 밴드 라벨 코퍼스에 없음' 으로 찍혔는데 거짓이었다 — display() 가 'OneTouch / UltraMini' 로 슬래시를 넣는데 내가 공백 이름으로 부분일치를 걸었다. 실제로는 30장으로 코퍼스 최대 기기다. 기기명 대조는 공백·구분자를 지우고 하도록 고쳤고 왜 그랬는지를 코드에 남겼다. 없다고 보고할 뻔한 것이 가장 큰 기기였다.
- 2026-09-17T00:15-07:00 — 근거 조회 2026-09-17 (review 세션, 조회 전용 — 학습·추론을 돌리지 않았다).

[검증한 것 — 산출물이 handoff 서술과 일치한다]
_diag/band_device_split/band_device_split.json: n_total 259 · n_train 140 · n_holdout 119 · train_devices 26 · holdout_devices 28 · seed 20260911 · warning 에 'train_ids 에 있는 사진은 이후 어떤 평가에도 쓰면 안 된다' 기록됨(AC#7 충족). 가로형 두 기기의 배치도 확인했다 — 'OneTouch / UltraMini' 와 '이름모를 가로형모델' 이 둘 다 train 이고, holdout 의 'OneTouch / Ultra' 는 이름만 비슷한 세로형이다. AC#2 의 보고가 맞다.

[결정 (2) 가로형 — 셋째 선택지('가로형 라벨을 더 붙인다')는 지금 규모를 못 낸다. 자가 깨져 있다]
어느 기기가 가로형인지 판정하는 자는 assets_dev/train/device_orient_stats.py 다('사람 지침 2026-09-13: 라벨링 비율 보면 알 수 있다'). 이 파일이 **실행되지 않는다** — IndentationError: unexpected indent (line 43). raise SystemExit(main()) 뒤에 같은 함수 본문 조각이 한 벌 더 붙어 있다. 커밋 dddb7e2(2026-09-13 'GM 쿼드를 사람 라벨 우선으로 — 지금까지 모델 예측을 실측이라 썼다')에서 깨졌다.
더 나쁜 것은 그 커밋의 의도가 이 파일에 반영되지 않았다는 점이다. 살아 있는 main() 은 여전히 gmscreen_quads_oriented.jsonl 을 직접 읽고, 사람 라벨 우선 경로인 gm_quads.quad_rows() 는 import 만 돼 있고 쓰이지 않는다. 붙어 있는 죽은 조각 쪽이 quad_rows() 를 쓴다 — 즉 고치려던 내용이 죽은 쪽에 남았다. **들여쓰기만 고치면 예측 기반 옛 경로가 되살아난다.** 고칠 때 반드시 quad_rows() 경로로 살릴 것.
그리고 이 자는 살아나도 범위가 좁다. targets 를 PROFILE_DEVICES 로 걸어 프로파일 있는 기기만 본다(현재 14키). 54종 중 어느 기기가 가로형인지 세려면 그 필터를 빼야 한다.
결론: '가로형 밴드 라벨을 더 붙인다' 를 고르려면 먼저 '코퍼스 54종 중 가로형이 몇 종·몇 장인가' 를 알아야 하는데, **이 세션은 그 수를 낼 수 없다.** 자가 깨져 있고 즉석 스크립트로 잰 값은 인용할 수 없다(CLAUDE.md). 이 카드와 별개로 자를 고치는 작업이 필요하다.

[결정 (2) 나머지 두 선택지의 성질]
(A) 그대로 간다 — 되돌릴 수 있다. 대신 게이트 보고서에 '이 게이트는 가로형을 재지 못한다' 를 반드시 적어야 한다. 안 적으면 다음 사람이 홀드아웃 IoU 를 전체 성능으로 읽는다.
(B) 가로형 기기 하나를 홀드아웃으로 보내는 예외를 둔다 — 되돌릴 수 있으나 규칙 변경이다. 비용이 구체적이다: 가로형 밴드 라벨 47장이 두 기기뿐이라(UltraMini 30 · 이름모를 가로형모델 17) 어느 쪽을 보내도 학습에서 가로형이 통째로 빠지거나(30장 이동 시 train 가로형 17장만 남음) 홀드아웃 가로형이 17장으로 얇아진다. 재는 쪽과 배우는 쪽 중 하나를 포기하는 선택이지 양쪽을 얻는 선택이 아니다.

[결정 (1) 어느 검출기 — (가)의 숨은 비용 하나를 적어 둔다]
handoff 의 (가) 추천과 'AC#5 를 같은 모델·같은 자로 다시 재도록 바꾼다' 는 제안은 이 저장소 규칙과 정합한다 — 모집단·자가 다른 두 수치를 한 표에 놓지 않는다(antipatterns/comparison-across-different-denominators · concepts/metric-provenance). v0 의 0.901/0.727 을 YOLOX 수치와 나란히 두면 정확히 그 안티패턴이다.
다만 '파인튜닝 전 YOLOX synthband 의 실사진 IoU' 를 재는 경로가 아직 없다. infer_synthband.py 는 --set 에 COCO 세트 디렉터리와 annotations/instances_*.json 을 요구한다(synth_coco/B 처럼). 실사진에는 그 주석이 없다 — band_boxes.jsonl 의 사람 라벨을 COCO 주석으로 내보내는 단계가 어느 AC 에도 없다. (가)를 고르면 그 단계가 AC#3 앞에 하나 더 생긴다. 분할이 이미 나와 있으니 train_ids/holdout_ids 를 그대로 쓰면 되고 큰 작업은 아니지만, 예산에 넣지 않으면 '바로 시작' 이 아니다.
(나)는 AC#5 가 문구 그대로 성립하는 대신 2026-09-16 에 폐기 방향이 정해진 경로에 학습을 태운다. 되돌림 비용은 학습 1회다.

[재지 않은 것] YOLOX synthband 의 실사진 IoU 를 재지 않았다(경로 없음). 코퍼스의 가로형 기기 수를 세지 않았다(자 깨짐). v0 의 0.901/0.727 을 재확인하지 않았다 — 2026-09-12 카드 Note 의 값을 그대로 인용했다.
- 2026-09-24T21:26-07:00 — RESUMED: 사람 결정 2026-09-24(권고 채택): (가) 살아 있는 YOLOX synthband 를 파인튜닝한다 — 사람 밴드 라벨이 전부 축정렬이라 형식이 정확히 맞고 결과가 바로 쓰인다. AC#5 는 '파인튜닝 전 YOLOX synthband 의 실사진 IoU'를 새로 재서 같은 모델·같은 자 위에 세 값을 올리는 방향으로 재정의(작업 시 반영). 남은 열린 문제 하나: 이 분할의 홀드아웃에 가로형 0장(구조적) — 그대로 갈지·가로형 기기 하나를 홀드아웃 예외로 보낼지·라벨을 더 붙일지는 착수 시 사람에게 다시 묻는다.

## Handoff
- 2026-09-17T00:14-07:00 — QUESTION: 데이터 쪽 절반(AC#1·#2·#6·#7)은 끝냈고 학습 쪽(AC#3·#4·#5)은 '어느 검출기를 파인튜닝할 것인가' 가 정해져야 시작할 수 있다. 이건 내가 정할 자리가 아니다. 상황: 이 카드는 2026-09-12 에 v0(4점 쿼드 회귀 CNN)을 전제로 썼는데, 2026-09-16 에 '검출기 출력을 축정렬 사각형으로 바꾼다' 가 결정되면서 v0 을 전제한 형제 카드가 폐기됐다(밴드 쿼드 검출기 재학습 2차). 지금 살아 있는 검출 경로는 마일스톤의 YOLOX synthband 다. 선택지: (가) YOLOX synthband 를 파인튜닝한다 — 사람 밴드 라벨이 전부 축정렬이라 형식이 정확히 맞고, 살아 있는 경로라 결과가 바로 쓰인다. 다만 AC#5 가 나란히 놓으라는 v0 수치(합성 0.901 · 실사진 0.727)는 다른 모델·다른 출력 형식이라 같은 표에 못 올린다. (나) v0 을 파인튜닝한다 — AC#5 의 세 값 비교가 카드 문구 그대로 성립하지만 폐기되는 경로에 학습을 태우는 것이다. 나는 (가)를 추천하고, AC#5 는 '파인튜닝 전 YOLOX synthband 의 실사진 IoU' 를 새로 재서 그것과 비교하도록 바꾸는 것을 함께 제안한다. 그러면 세 값이 같은 모델·같은 자 위에 놓인다. 또 하나 반드시 알고 결정해야 할 것: 이 분할의 홀드아웃에 가로형이 0장이다. 구조적이라 시드로 안 풀린다(가로형 밴드 라벨이 기기 두 종뿐이고 둘 다 train 으로 갔다). 즉 무엇을 파인튜닝하든 이 게이트는 가로형 성능을 재지 못한다. 그대로 갈지, 가로형 기기 하나를 홀드아웃으로 보내는 예외를 둘지(기기 단절 규칙은 유지하되 담기 순서에 제약을 추가), 아니면 가로형 밴드 라벨을 더 붙일지도 함께 정해 달라.

## Result
