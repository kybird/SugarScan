---
title: 학습 도구 미병합 브랜치를 리뷰해 살릴 것만 병합한다
status: todo
ordinal: 15000
created: 2026-09-10
depends_on: ["스택된 스캐너 브랜치를 main 에 병합한다"]
---

## Goal
<!-- kanban:goal:begin -->
glm/G28-tflite-parity · G29-letterbox-warp · glm/cv-band-viability 세 개를 리뷰하고, 지금도 유효한 assets_dev/train 변경만 main 에 병합한다. 셋 다 main 대비 30~41 behind 라 이후 작업(G30~G34)이 같은 파일을 이미 고쳤을 수 있다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 세 브랜치 각각 살림/폐기 판정과 근거 한 줄이 docs/reports/ 보고서 한 장에 남는다
- [ ] #2 webtool.py · webtool.html · build_cache_v2.py · eval_reader.py 는 병합 후에도 main 최신 동작이 살아 있다 — G34 가 추가한 /devices 라우트와 device_tags.jsonl 저장이 그대로 뜬다(서버 재시작해 왕복 저장 1건 확인)
- [ ] #3 assets_dev/train 스크립트가 실제로 도는 것을 확인한 뒤 병합한다 — 최소 한 개는 실행해 출력 확인(임포트만 되는 것은 근거가 아니다)
- [ ] #4 docs/DONE.md 에 이미 적힌 G28·G29 절과 새로 들어온 코드가 어긋나지 않는다(문서는 완료, 코드는 미병합이던 상태를 해소했다는 한 줄이 남는다)
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-10T13:26-07:00 — 이 셋은 문서보다 코드가 위험하다. main 은 이 브랜치들이 갈라진 뒤 G30·G31·G32·G34 로 assets_dev/train 을 크게 고쳤다 — webtool.py 는 G34 가 /devices 라우트를, cv-band-viability 는 같은 파일에 라벨 스킵 로직을 넣는다. 같은 함수를 양쪽이 고쳤을 수 있으니 diff 를 줄 단위로 볼 것. G28 은 결론이 '중단'이다(tflite 변환 불가, docs/DONE.md G28 절) — 스크립트를 넣을 이유가 '기록 보존' 뿐이라면 병합하지 말고 보고서만 남기는 판정도 정답이다. cv-band-viability 에는 라벨 되돌리기 커밋(0eb1790 과 그 Revert 5f0e85a)이 있다 — 라벨 데이터를 건드리는 커밋이라 병합 전 dry-run 으로 무엇이 바뀌는지 먼저 찍어 볼 것(patterns/dry-run-before-repair). 학습 캐시·라벨 파일을 덮어쓰는 변경이 섞여 있으면 그 브랜치는 즉시 handoff.

## Handoff

## Result
