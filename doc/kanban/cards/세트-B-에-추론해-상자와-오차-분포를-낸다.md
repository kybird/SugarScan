---
title: 세트 B 에 추론해 상자와 오차 분포를 낸다
status: review
ordinal: 54000
created: 2026-09-16
depends_on: ["YOLOX 를 혈당 숫자 상자로 파인튜닝한다"]
milestone: 합성만으로 사진에서 값까지 한 번 통과시킨다
---

## Goal
<!-- kanban:goal:begin -->
검출기가 처음 보는 세트 B 에 추론해 예측 상자를 jsonl 로 낸다. 같은 장의 정답 상자와 비교해 오차 분포(변별 치우침·크기비·포함률)를 낸다 — 이 분포가 리더 학습 재료이자, 나중에 검출기 사양을 정하는 근거다. 지어낸 흔들기 폭을 쓰지 않는 이유가 여기 있다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 B 1000장 전량에 상자가 나온다(건너뜀 0)
- [x] #2 정답 대비 포함률·넓이비·변별 치우침의 분위수가 출력된다
- [x] #3 포함률이 낮은 최악 10장을 시트로 그려 눈으로 확인한다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-16T23:12-07:00 — 추론(명령: python infer_synthband.py --set synth_coco/B --ckpt yolox_out/synthband_v0/best_ckpt.pth --out _diag/synthband_v0/B.jsonl). n=1000, 행은 1000개 전부 나오지만 conf 0.25 에서 상자가 안 나온 장이 1장 있다(panel_20260917_22.png, pred=null 로 기록). 그 장은 딱딱한 그림자 경계가 숫자 한가운데를 세로로 가른다 — conf 를 0.10 까지 내리면 상자가 하나 나오고 최고 점수는 0.1908 이다(임계만 바꿔 재봤다).
- 2026-09-16T23:12-07:00 — 분포 자는 synthband_box_error.py 신설. 검출 실패는 분포에서 빼고 따로 센다 — 0 으로 채우면 실패가 꼬리로 위장돼 분위수가 조용히 나빠진다. 부호 규약은 파일 머리말에 한 번만 적었다(양수 = 예측이 정답 밖으로 나감).
- 2026-09-16T23:12-07:00 — 축이 둘로 갈린다는 것을 눈으로 확인하다 알았다. 처음엔 밴드 포함률 최악 10장을 보고 '예측 상자가 마지막 숫자 획을 자른다'고 읽었는데, 정확히 재니 틀렸다. 정답 밴드는 가장 넓은 값을 담으려고 여백을 미리 떼므로(patterns/reserve-by-the-widest-value) 밴드 포함률 0.73 이어도 숫자는 멀쩡하다. 그래서 매니페스트의 quad_panel -> quad 호모그래피를 되찾아 숫자 필드(rects['band'])를 워프 후 좌표로 옮겨 따로 쟀다. 눈으로 본 인상을 자로 확인하기 전에는 보고하지 않는다.
- 2026-09-16T23:12-07:00 — 네 변 중 right 가 가장 타이트하다(평균 -0.0086, p1 -0.0788 — 네 변 중 최솟값). 필드 포함률 최악 꼬리의 시트를 보면 오른쪽 변이 마지막 자리 세로획을 스친다. 리더를 예측 상자로 학습하는 다음 카드에서 이 비대칭이 그대로 입력이 된다.
- 2026-09-16T23:12-07:00 — 결과 요약(모집단 _diag/synthband_v0/B.jsonl, n=1000 · 검출 성공 999). 명령: python synthband_box_error.py --pred _diag/synthband_v0/B.jsonl --set synth_coco/B --manifest synth_coco/B/manifest.jsonl --worst-sheet _diag/synthband_v0/B_worst.png --worst-n 10 | 밴드 포함률 p1 0.7907 p5 0.8514 p50 0.9573 p95 1.0000, 1.0 인 장 125/999 | 넓이비 p1 0.7907 p50 0.9810 p99 1.2529 | 치우침 평균 left -0.0045 right -0.0086 top -0.0032 bottom +0.0029 | 숫자 필드 포함률 p1 0.9720 p5 0.9945 p50 1.0000, 통째로 덮음 917/999 · <0.95 1장 · <0.90 0장 | IoU p50 0.9179. 시트 _diag/synthband_v0/B_worst.png (초록=예측 빨강=정답, 필드 포함률 최악 10장). 전부 합성이고 검출기 학습 세트(A)와 같은 생성기라 일반화 아님.
- 2026-09-16T23:12-07:00 — 근거 조회 2026-09-17 (review 세션, 조회 전용 — 추론을 다시 돌리지 않았다). 합격선은 사람이 정한다. 정하기 전에 볼 것 셋.

[1] 이 결정은 칼날 위가 아니다 — 임계에 넓은 빈 구간이 있다.
_diag/synthband_v0/B.jsonl(n=1000, 검출 성공 999) 의 score 분포: 최소 0.6200 · p1 0.7323 · p5 0.8303 · 중앙 0.9158. **0.30 미만 0장 · 0.40 미만 0장 · 0.50 미만 0장.** 즉 성공한 999장은 전부 0.62 이상이고, 실패한 1장은 0.1908 이다. 0.19 와 0.62 사이가 통째로 비어 있다.
따라서 임계를 0.25 로 두든 0.40 으로 두든 0.60 으로 두든 세트 B 결과는 999/1000 으로 같다. 0.20 까지 내리면 1000/1000 이 된다. 선택지 (가)와 (나)의 차이는 정확히 **사진 1장**이고, 그 사이에 다른 장이 딸려 들어올 여지가 없다. n_det>1 인 장은 28장이다.
재현: _diag/synthband_v0/B.jsonl 의 score 필드 집계(추론 불필요).

[2] (나)의 논거 '배포와 다른 임계' 는 지금 성립하지 않는다 — 배포 임계가 아직 없다.
conf 0.25 는 infer_synthband.py:49 의 argparse 기본값이고 파일 어디에도 근거 주석이 없다. 이 저장소는 운영점을 정할 때 그것을 명시하는 관습이 있다(diag_cv_band.py:59 'grow_x=0.30, grow_y=0.10 — 최종 운영점', eval_band_detector.py:10 'cv-band-viability 보고서의 운영점'). synthband 에는 그런 선언이 없다. 그리고 검출기는 앱에 들어가 있지 않다 — ocr_bootstrap 에 등록된 엔진은 SegmentRuleEngine 과 7-세그먼트 CNN 둘뿐이고 ONNX 경로는 아직 열리지 않았다(CLAUDE.md · DONE.md G28·G31).
즉 0.25 대 0.10 은 '배포값 대 비배포값' 이 아니라 '미선언값 대 미선언값' 이다. (나)를 기각하려면 다른 근거가 필요하고, (가)를 고르더라도 0.25 가 왜 그 값인지는 여전히 아무도 안 정했다.

[3] 질문을 한 단계 올리면 한 번에 끝난다.
다음 카드 「리더를 YOLOX 예측 상자로 학습한다」 Goal 원문: '배포 때 리더가 받는 것은 검출기 출력이고, 그 오차는 무작위가 아니라 구조적이라 정답 크롭으로 배우면 그 구조를 못 배운다.' 그 카드의 전제 자체가 '학습 크롭과 배포 크롭이 같은 임계에서 나온다' 이다. 그리고 마일스톤 마지막 「세트 C 로 끝단 평가한다」도 같은 추론을 한 번 더 돌린다.
따라서 사람이 답할 것을 '이번 999 를 합격으로 볼까' 가 아니라 '**synthband conf 운영점을 얼마로 선언할까**' 로 두면 세 카드가 한 번에 정리되고, AC#1 의 합부는 그 선언에서 기계적으로 따라 나온다. 선언한 값은 infer_synthband.py 기본값과 주석에 박아 다음 사람이 다시 묻지 않게 한다.

[선택지와 되돌림]
(가) 999/1000 을 합격. 되돌릴 수 있다(숫자를 다시 안 뽑아도 됨). 앱의 기존 규칙과 정합한다 — 검출 실패는 수동 입력 경로로 떨어지는 것이 CLAUDE.md 의 명시 규칙이고, 리더 학습 코퍼스가 배포 분포와 같아진다.
(나) 임계를 낮춰 1000장. 되돌릴 수 있으나 재추론 1회가 든다. **고르기 전에 반드시 재야 할 것이 있다 — 이 세션은 재지 않았다**: conf 0.10 에서 나온 그 상자(score 0.1908)의 IoU·포함률이 얼마인가. 지금 B.jsonl 에는 pred=null·iou=0.0 으로만 있어 상자 품질을 모른다. 나쁜 상자를 1장 넣는 것은 1장을 빼는 것보다 해로울 수 있다 — 그 상자가 리더 학습 재료가 되기 때문이다. 필요한 명령: python infer_synthband.py --set synth_coco/B --ckpt yolox_out/synthband_v0/best_ckpt.pth --conf 0.10 --out <임시> 뒤 id 22 의 iou 확인. GPU 가 한가할 때.
(다) 그 장을 빼고 다시 굽는다. 되돌리기 가장 비싸다(재생성 + 모든 분포 수치 무효화). 어려운 장을 지우는 것이라는 handoff 의 반대 의견에 더할 근거: 그 1장을 빼면 B 의 난이도 분포가 바뀌어 위 [1]의 빈 구간도 다시 재야 한다.

[재지 않은 것] 실패 장(panel_20260917_22.png)의 그림자 경계가 원인이라는 진단은 handoff 작성자의 관찰이고 이 세션이 확인하지 않았다. 세트 C 에서도 같은 비율의 실패가 날지는 재지 않았다.

## Handoff
- 2026-09-16T23:12-07:00 — QUESTION: AC#2·#3 은 객관 증거로 채웠고 산출물도 다 나왔다. 사람이 정할 것은 AC#1 의 합격선 하나다. 문구는 'B 1000장 전량에 상자가 나온다(건너뜀 0)' 인데 conf 0.25 에서 999/1000 이다. 못 나온 1장(panel_20260917_22.png)은 딱딱한 그림자 경계가 숫자를 세로로 가르는 장이고, conf 0.10 까지 내리면 상자가 나온다(최고 점수 0.1908). 선택지 셋: (가) 999/1000 을 합격으로 보고 그 1장은 리더 코퍼스에서 빠진다 — 앱의 기존 규칙과 맞는다(검출 실패 = 수동 입력 경로). (나) 리더 코퍼스용으로만 임계를 낮춰 1000장을 채운다 — 배포와 다른 임계로 만든 상자를 리더가 학습하게 된다. (다) 합성기 쪽 문제로 보고 그 장을 빼고 다시 굽는다 — 어려운 장을 지우는 것이라 반대한다. 나는 (가)를 추천한다. 배포에서 검출이 실패하면 리더도 돌지 않으므로, 리더 학습 코퍼스가 배포 분포와 같아지는 쪽이 (가)다. 다만 '무엇이 합격인가' 는 제품 판단이라 내가 정하지 않는다. 결정이 나면 resume 해서 AC#1 만 마감하면 된다.

## Result
