---
title: 합성 생성이 같은 시드에서 같은 코퍼스를 내게 한다
status: done
ordinal: 38000
created: 2026-09-12
depends_on: ["합성을 GM 크롭과 같은 물건으로 — 기기 몸체를 함께 렌더한다"]
---

## Goal
<!-- kanban:goal:begin -->
synth_panel.py gen --count 300 --seed 31000 을 두 번 돌리면 다른 코퍼스가 나온다. 같은 명령 두 실행에서 바깥 링 3.18/3.11%, 전체 2.71/2.63%, glyph_plane_check min 0.2357/0.8076, 유효 표본 299/297 이 나왔다. 그래서 AC 수치를 인용해도 그 명령이 같은 값을 다시 내지 못한다. 원인 둘. (1) generate 가 numpy 전역을 심지 않는데 광학 잡음이 np.random.normal 이다(synth_panel.py:816). 형제 스크립트 validate_synth_panel.py:46 은 np.random.seed 를 제대로 한다. (2) render_panel 의 재시도 루프가 밀도 값으로 rng 소비 횟수를 가른다 — 1e-5 의 부동소수 흔들림 하나가 재렌더 횟수를 바꾸고 그 뒤 패널이 전부 어긋난다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 #1 같은 시드 두 실행의 manifest.jsonl 이 바이트 동일하고 이미지도 md5 동일하다. seed 31000 n=300 으로 보인다
- [x] #2 #2 rng 소비가 데이터에 의존하면 재현이 깨진다는 것을 코드 주석으로 못박는다 — 다음 사람이 같은 자리에 같은 것을 다시 넣지 않게
- [x] #3 #3 밀도 목표 재시도의 동작(최대 3회, 미달 시 보정)은 유지한다. 이 카드는 재현성만 고친다 — 선택 정책 변경은 별도 카드다
- [x] #4 #4 고친 뒤 seed 31000 n=300 의 링·안쪽·전체 밀도와 세로형 밴드 기하를 다시 재서 적는다. 값이 움직였으면 얼마나 움직였는지 적는다
- [x] #5 #5 validate_synth_panel.py 하드 검사가 전부 0 을 유지한다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-12T19:13-07:00 — np.random.seed(seed0) 만으로는 부족하다 — 시험했다. seed 31000 n=300 에서 이미지 244/300 이 바이트 동일까지 회복하고 나머지는 어긋난다. cv2.setNumThreads(1) 로도 안 잡혔다. 뿌리는 재시도 루프의 데이터 의존 rng 소비다.
- 2026-09-12T19:13-07:00 — 고치는 방향 하나: 한 패널에 쓸 난수를 재렌더 횟수와 무관하게 미리 뽑아 두면 소비가 고정된다. 정답을 지정하는 것은 아니다 — 다른 방법이 낫다면 그 방법으로 하고 이유를 적어라.
- 2026-09-12T19:13-07:00 — 건드리지 말 것: BAND_GEOM · PITCH_RATIO · ASPECT_BINS · REAL_DENSITY 같은 실측 상수. 이 카드는 난수 흐름만 고친다. 상수를 함께 움직이면 AC#4 의 '값이 움직였는가' 판정이 불가능해진다.
- 2026-09-12T19:13-07:00 — 왜 중요한가: 이 저장소는 측정값 옆에 그 값을 내는 명령을 적게 돼 있다(CLAUDE.md). 그 명령이 같은 값을 다시 내지 못하면 규칙이 반만 지켜진 것이다. glyph_plane_check min 이 실행마다 0.2357 / 0.8076 / 0.713 으로 튀어 min 을 통계로 쓸 수 없는 것이 실제 피해다.
- 2026-09-12T19:13-07:00 — 근거: docs/reports/synth-gm-crop-review.md 지적 5. 기준 코드는 claude/synth-gm-crop-review(ce96d97)다 — main 병합 전에는 착수하지 마라. 수치가 그 트리 기준이다.
- 2026-09-13T19:23-07:00 — 2026-09-13 완료 확인. 원인 (1) np.random 전역은 rng 파생으로 바꿨다(np.random.default_rng(rng.getrandbits(63))). 카드가 모르던 둘째 구멍도 있었다 — _dot_time_text 의 am/pm 이 모듈 전역 random.choice 였다. 원인 (2) 재시도 루프의 데이터 의존 rng 소비는 구조를 남기고 주석으로 못박았다(AC#2) — 모든 난수원이 rng 파생이면 결정성은 깨지지 않는다. 검증: synth_panel.py gen --count 300 --seed 31000 두 실행에서 manifest.jsonl md5 동일(92a5367f84724f62c9b638ffe3dede8d), 이미지 300장 전량 md5 동일. AC#4 재측정(seed 31000 n=300): 바깥 링 1.74% (실측 1.90%) · 안쪽 1.10% (실측 1.11%) · 전체 median 1.49% p10 0.56% p90 3.11% (실측 1.77% [0.46, 3.84]) · 세로형 밴드/유리 w 0.894 h 0.420 cx 0.515 cy 0.415 (실측 0.888 / 0.447 / 0.512 / 0.407). AC#5 하드 검사 전부 0.

## Handoff

## Result
- 2026-09-13T19:23-07:00 — 같은 시드 두 실행이 바이트 동일해졌다(seed 31000 n=300, manifest md5 92a5367f84724f62c9b638ffe3dede8d, 이미지 300장 전량 일치). 원인은 전역 난수 두 곳이었다 — 광학 잡음의 np.random.normal 과 _dot_time_text 의 random.choice. 둘 다 넘겨받은 rng 에서 파생시켰다. 재시도 루프의 데이터 의존 rng 소비는 그대로 두고 주석으로 못박았다: 모든 난수원이 rng 파생인 한 결정성은 유지되고, 전역 난수를 하나라도 들이면 그 구조 때문에 코퍼스 전체가 갈라진다. 커밋 7a1a453.
