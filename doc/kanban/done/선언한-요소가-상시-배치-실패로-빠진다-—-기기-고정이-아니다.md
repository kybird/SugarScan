---
title: 선언한 요소가 상시 배치 실패로 빠진다 — 기기 고정이 아니다
status: done
ordinal: 50000
created: 2026-09-13
---

## Goal
<!-- kanban:goal:begin -->
validate n=120 seed 23000 에서 icon:triangle 3건, icon:bluetooth 3건, meal 3건이 배치 실패로 빠진다. 프로파일이 '이 기기는 이 요소를 가진다'고 선언했는데 특정 기기에서 계통적으로 못 들어가는 것이면, 그건 상태성이 아니라 자리 설계가 틀린 것이다. 기기 고정 요소는 항상 그린다는 규칙(STATEFUL_ELEMENTS 분리, 2026-09-13)이 배치 실패로 조용히 깨진다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 어느 기기의 어느 요소가 몇 % 빠지는지 기기별로 인쇄한다 — 전체 건수만으로는 계통 실패와 우연을 못 가른다
- [x] #2 계통적으로 빠지는 요소는 자리를 고치거나 프로파일에서 뺀다. 근거 사진에 없으면 빼는 쪽이다
- [x] #3 고친 뒤 n=120 에서 기기 고정 요소의 드롭이 0 이다(상태성 요소 제외)
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-17T00:28-07:00 — 증상부터 조회했다(코드보다 현장 먼저). 카드가 적은 바로 그 설정 n=120 seed 23000 에서 '뺀 요소(배치 실패): {}' 다. n=600·n=2000 seed 23000 에서도 0 이다. 2026-09-13 이후 어딘가에서 이미 고쳐졌다 — 이 카드가 고칠 것이 남아 있지 않다.
- 2026-09-17T00:28-07:00 — 카드 Goal 의 증거를 카드 자신의 AC#3 잣대로 다시 보면 3건 중 1건만 위반이었다. AC#3 은 '기기 고정 요소의 드롭이 0(상태성 요소 제외)' 인데, 당시 보고된 icon:bluetooth 와 meal 은 STATEFUL_ELEMENTS 에 들어 있다. 기기 고정인 것은 icon:triangle 하나뿐이다. 전체 건수를 그대로 세면 상태성과 고정이 섞여 문제 크기가 부풀어 보인다 — 이 카드가 AC#1 에서 요구한 분해가 바로 그것을 막는 장치다.
- 2026-09-17T00:28-07:00 — AC#1 의 분해를 validate_synth_panel 에 넣되 함수(report_drops)로 빼고 시험을 붙였다. 지금 드롭이 0 이라 그 인쇄 가지가 한 번도 실행되지 않기 때문이다 — 실행된 적 없는 보고 코드는 다음에 드롭이 생겼을 때 처음 돌고, 그때 틀려 있으면 진단이 아니라 방해가 된다([[metric-path-not-under-test]]). test_report_drops.py 가 가짜 매니페스트 네 경우(드롭 없음·상태성만·고정 계통·섞임)로 직접 돌린다. PASS.
- 2026-09-17T00:28-07:00 — AC#2 는 고칠 대상이 없어 비어 있다. 계통적으로 빠지는 요소가 0 이므로 자리를 고치거나 프로파일에서 뺄 것이 없다. 없는 문제를 고쳤다고 적지 않는다.

## Handoff

## Result
- 2026-09-17T00:28-07:00 — 증상이 더는 재현되지 않는다. 카드가 적은 바로 그 설정에서 확인했다 — python validate_synth_panel.py --count 120 --seed 23000 -> '뺀 요소(배치 실패): {}'. n=600·n=2000 seed 23000 에서도 0 이다(기기 12종 전부 포함). 2026-09-13 이후 어딘가에서 이미 고쳐졌고 이 카드가 고칠 것은 남아 있지 않다. AC#1 기기 x 요소 분해를 validate_synth_panel.report_drops() 로 넣었다 — 요소마다 '그 기기의 장 수' 를 분모로 찍고 기기 고정/상태성을 갈라 표시하며, 기기 고정 드롭 합계에 OK/FAIL 을 낸다. 드롭이 0 이면 그 인쇄 가지가 실행되지 않으므로 함수로 빼고 test_report_drops.py 를 붙였다(가짜 매니페스트 네 경우: 드롭 없음 0 · 상태성만 0 · 고정 계통 3 · 섞임 2, 전부 기대와 일치, PASS). 실행된 적 없는 보고 코드를 들고 다니지 않기 위해서다. AC#2 는 고칠 대상이 0 이라 비어 있다 — 없는 문제를 고쳤다고 적지 않는다. AC#3 기기 고정 요소 드롭 0(상태성 제외), n=2000 seed 23000. 곁들여 발견: 카드 Goal 의 증거 3건(icon:triangle 3 · icon:bluetooth 3 · meal 3)을 카드 자신의 AC#3 잣대로 보면 위반은 icon:triangle 하나뿐이다 — icon:bluetooth 와 meal 은 STATEFUL_ELEMENTS 다. 전체 건수를 그대로 세면 상태성과 고정이 섞여 문제가 부풀어 보이고, AC#1 이 요구한 분해가 정확히 그걸 막는다. 손대지 않은 것: 하드 검사 sizes(458 @n=2000)는 다른 카드 몫이다.
