---
title: 밴드 검출망을 직접 설계하고 코퍼스 크기 곡선을 잰다
status: done
ordinal: 59000
created: 2026-09-17
---

## Goal
<!-- kanban:goal:begin -->
docs/SPEC.md §5.3 에 따라 구조를 직접 설계한다(외부 데이터·가중치 없음, 스크래치). 그 다음 사람 지시대로 **코퍼스 크기**를 바꿔가며 일반화가 어디서 포화하는지 잰다 — 모델은 하나로 고정한다. 전부 합성이고, 평가는 §4 순서대로 합성 게이트(시드가 다른 홀드아웃)로만 한다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 구조가 스펙의 과제 성질을 반영한다 — 장당 물체 1개(NMS 없음)·클래스 1개(분류 헤드 없음)·항상 large(stride 8 없음)
- [x] #2 한 배치 과적합 시험으로 학습이 성립함을 먼저 보인다
- [x] #3 게이트가 IoU 가 아니라 숫자 필드 포함률이고, 프로파일별 최저값을 함께 낸다
- [x] #4 곡선의 점들이 중첩 부분집합이고 스텝·모델·시드가 전부 고정이다
- [x] #5 방향별 장수와 합격률의 관계를 내고, 포화 여부를 적는다
<!-- kanban:ac:end -->

## Plan

## Notes

## Handoff

## Result
- 2026-09-18T00:46-07:00 — AC 다섯 다 채웠다. 코드 5d85cd4, 결과 band_out/curve_eval.log.

AC#1 구조가 과제 성질을 반영한다 — BandNet 0.504M(width 1.0):
  장당 물체 1개  -> NMS 없음, argmax 하나
  클래스 1개    -> 분류 헤드 없음
  항상 large    -> stride 8 층 없음. /16 한 장만
  정답이 축정렬 -> 칸 중심에서 네 변까지의 **거리** 회귀(절대 좌표 아님)
  입력 1채널 416 — 합성기가 이미 1채널로 굽는다

AC#2 과적합 시험: 400장 400스텝 17초에 학습 IoU 0.848.

AC#3 게이트는 IoU 가 아니라 숫자 필드 포함률이다(eval_band.py). 생성기가
digit_box 를 주므로(SPEC 9.5) 직접 센다. 프로파일별 최저값을 함께 낸다.

AC#4 곡선의 점이 중첩 부분집합이고(build_band_curve.py) 모델·스텝(8,000)·시드가
전부 고정이다. 이미지는 복사하지 않고 주석만 여러 벌이다.

AC#5 결과 — 평가는 굽는 시드가 다른 세트 V 1,000장:
    방향별  학습장수   합격률   세로형   가로형  검출실패  IoU세로  IoU가로
      500     1000   91.90%  90.11%  93.88%     0   0.9127  0.9373
     1000     2000   97.50%  96.77%  98.31%     0   0.9310  0.9516
     2000     4000   99.60%  99.81%  99.37%     0   0.9476  0.9629
     4000     8000   99.60%  99.62%  99.58%     0   0.9566  0.9708
     7998    15996   99.80%  99.81%  99.79%     0   0.9648  0.9758

  **합성 게이트 99% 는 방향별 2,000 에서 넘고 거기서 포화한다.**
  검출 실패는 전 구간 0 이다.

  다만 포화한 것은 **게이트 지표**이지 상자 품질이 아니다 — 합격률이 평평해진
  뒤에도 IoU 는 계속 오른다(세로 0.9476->0.9648). 통과율로만 보면 안 보인다.

  가로형이 세로형과 대등하거나 낫다. 50:50 균등 생성(SPEC 9.6)의 결과다.

남은 것(다음 카드감): **최악 프로파일이 97.62% 에 붙어 장수로 안 풀린다.**
세 점 모두에서 불합격인 장은 둘이고 정체가 같다 — 둘 다 가로형이고 둘 다
좌우가 잘린다(좌 31.9px · 우 40.9px). 점수가 0.999/1.000 이라 못 찾는 것이
아니라 **변을 짧게 잡는다.** 폐기된 계열에서 실촬을 쟀을 때 나온 치우침
패턴과 같은 방향이다(좌 43%·우 38% 파고듦, 상하는 11~13%) — 모델도 코퍼스도
바뀌었는데 같은 방향이 남았다. 다음에 볼 곳은 장수가 아니라 가로 변 회귀다.
기록은 doc/raw/2026-09-18.md Case 1.
