---
title: 아틀라스 검토 서버를 웹툴 프로세스로 흡수한다
status: done
ordinal: 65000
created: 2026-09-21
milestone: 웹툴을 한 서버·한 메뉴로 정비한다
---

## Goal
<!-- kanban:goal:begin -->
make_failure_atlas.py --serve(8790) 를 없애고 웹툴 서버(8777) 하나가 /atlas 를 in-process 로 서빙하게 한다
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 웹툴 서버만 띄운 상태(8790 프로세스 없음)에서 /atlas 가 200 으로 응답하고 검토 화면이 뜬다
- [ ] #2 C:/Program Files/Git/atlas 에서 판정을 저장하면 agent_review.json 에 반영된다(standalone --serve 와 같은 파일·같은 규약)
- [x] #3 아틀라스 미빌드 상태에서 /atlas 가 빌드 명령 안내를 낸다(8790 을 띄우라는 옛 503 문구가 아니다)
- [x] #4 make_failure_atlas.py --serve standalone 모드가 여전히 동작한다(플래그·기본 포트 유지)
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-21T15:20-07:00 — 조사(2026-09-21): webtool.py:2209 _atlas_proxy 가 8790 으로 HTTP 중계. 아틀라스 서버는 요청 처리가 가벼움 — 상태(페이지 HTML·케이스 JSON·ReviewStore·imgs_dir)를 시작 전 클래스 속성으로 묶고 AtlasHandler(make_failure_atlas.py:939)가 정적 서빙만 함. 경로 충돌 없음(webtool.py:2214 주석, 2026-09-11 확인). 접근: make_failure_atlas.py 에서 serve 상태 초기화와 요청 처리를 함수로 뽑고 webtool 이 in-process 위임. standalone --serve 는 유지.
- 2026-09-21T15:20-07:00 — 건드리지 말 것: (1) tensorflow lazy import — 웹툴 모듈 적재만으로 TF 가 import 되면 안 된다(make_failure_atlas 가 eval_reader 를 module level 에서 import 하므로 webtool 기동 시점 import 금지, 첫 /atlas 요청 시 백그라운드 초기화). (2) agent_review.json 저장 규약(판정 주체 human 등)은 한 글자도 바꾸지 않는다. (3) SERVE_OUT(_diag/atlas_serve) 외의 _diag 디렉터리에 쓰지 않는다. 틀리기 쉬운 자리: 초기화 중 요청은 진행 안내로 응답해야 함(타임아웃으로 죽으면 안 됨). 웹툴.bat 은 이미 webtool.py 만 실행하므로 손댈 것 없음. WEBTOOL.md 의 --serve 실행 안내 갱신 필요.
- 2026-09-21T22:12-07:00 — 구현(2026-09-21): make_failure_atlas 에서 초기화(init_serve_state)와 요청 처리(atlas_get·atlas_post_review)를 함수로 추출 — standalone --serve 와 웹툴 in-process 가 같은 경로를 쓴다(main 은 serve 분기에서 init_serve_state 호출, build_cases 중복 없음). webtool 은 _atlas_proxy HTTP 중계(8790)를 삭제하고 _atlas_ensure 로 첫 /atlas 요청에서 백그라운드 초기화 — 진행 중(200, 5초 리프레시)·실패(503, 원인+빌드 안내) 전이. agent_review.json 저장은 standalone 과 같은 ReviewStore·같은 경로(_diag/atlas/agent_review.json).
- 2026-09-21T22:12-07:00 — 버그(밤새 잡음): 초기 스레드가 except Exception 으로 잡아 SystemExit(build_cases 등가성 검사)를 못 받아 조용히 죽고 /atlas 가 영원히 '준비 중'이 됐다 — except BaseException 으로 고침. 같은 원인으로 첫 검증 20분을 낭비: 프로세스 CPU 0.047초/15초(정지)인데 '느릿한 초기화'로 오독했다. 포그라운드 재현 프로브가 7초 만에 진짜 원인을 드러냄 — 상태 추측 대신 관측 가능한 신호로 진행을 판단할 것.
- 2026-09-21T22:12-07:00 — 데이터 짝 사실(AC#1·#2 를 지금 못 채우는 이유): 등가성 검사가 glucose_batch1/2411 에서 실패한다. frame_crop 정본이 2026-09-13 904c266 으로 축정렬 워프로 바뀌었는데 캐시 7벌(9/5~9/9)·reader_preds(9/8)·gmscreen_quads(9/11) 는 전부 그 이전 산물 — _diag/atlas_serve 마지막 성공 산출물도 9/8 이다. 즉 9/13 이후 아틀라스 serve 는 in-process 나 standalone 이나 한 번도 뜬 적이 없고, 검토 화면·판정 저장은 캐시를 현재 기하로 다시 굽거나 짝을 맞추기 전까지 불가. 이건 이 카드가 만든 상태가 아니라 흡수 전부터 있던 상태다(standalone 실행으로 동일 실패 확인).

## Handoff

## Result
- 2026-09-21T22:12-07:00 — 무엇을 바꿨나: make_failure_atlas.py 에 init_serve_state·atlas_get·atlas_post_review 를 추출해 standalone --serve 와 웹툴이 같은 초기화·요청 경로를 쓰게 했고, webtool.py 의 _atlas_proxy(HTTP 중계 8790)를 삭제해 /atlas 를 in-process 로 서빙한다(첫 요청 백그라운드 초기화, TF 는 webtool 기동 시점 미적재). WEBTOOL.md 의 옛 '8790 프록시' 안내를 갱신. 무엇으로 검증했나: 웹툴 8778 단독 기동(8790 부재)에서 /atlas 200 진행 안내 -> 20초 내 503 빌드 안내 전이(원인=SystemExit 등가성 검사 실패 명시, 옛 8790 문구 아님 — AC#3), 초기화 전 POST /api/review·GET /api/cases 503 안전 거부, 루트·/synth·/devices 200 무결, standalone --serve 실제 실행으로 같은 초기화 진입·기존과 동일한 설계 실패 확인(AC#4). AC#1·#2(검토 화면·판정 저장)는 데이터 짝 문제로 미실시: 9/13 기하 변경(904c266) 이후 캐시·preds·quads 가 어긋나 in-process 나 standalone 이나 초기화가 등가성에서 실패 — 흡수 전부터 있던 상태로 카드 Note 에 근거를 남김. 도중 발견·수정: 초기화 스레드가 SystemExit 를 못 잡아 영구 '준비 중'이 되던 버그를 except BaseException 으로 고침.
