---
title: 글리프 평면 자가검사가 저대비 패널에서 분해능을 잃는다
status: done
ordinal: 49000
created: 2026-09-13
---

## Goal
<!-- kanban:goal:begin -->
glyph_plane_check 는 워프된 글리프 마스크 자리가 실제 잉크인지 보는 자가검사다. 문턱을 대비 비례(0.25·대비, 바닥 12)로 바꾼 뒤에도 저대비 패널에서 무너진다 — validate n=120 seed 23000 에서 0.9 미만 5.0%, 최저 0.59. 산출물은 멀쩡하고(오버레이로 확인) 가드만 못 읽는다. 바닥값 12 가 저대비 구간을 지배하기 때문이다. 통과만으로는 가드가 살아 있는지 알 수 없다 — 이 상태로는 '평면이 어긋났다'와 '패널이 흐리다'를 여전히 구별 못 한다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 대비 구간별로 gpc 분포를 인쇄한다 — 어느 구간에서 분해능이 0 인지 수치로 보인다
- [x] #2 일부러 글리프 평면을 8px 어긋뜨렸을 때 저대비 구간에서도 점수가 떨어지는지 증명한다. 안 떨어지면 그 구간에서 가드는 죽은 것이다
- [x] #3 바닥값의 근거를 적거나, 바닥 없이 성립하는 문턱으로 바꾼다 — 광학 노이즈 σ 와의 관계를 수치로 적는다
- [x] #4 validate 출력에 '0.9 미만' 비율뿐 아니라 저대비 구간 한정 비율을 함께 찍는다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-17T00:23-07:00 — 자를 새로 짜지 않고 감쌌다 — diag_gpc_resolution.py 가 synth_panel.glyph_plane_score 를 wrap 해 같은 인자로 두 번 부른다(정상 마스크 / 8px 어긋뜨린 마스크). 점수 정의가 바뀌어도 진단이 따라간다. 분해능 = 정상 gpc - 어긋뜨린 gpc.
- 2026-09-17T00:23-07:00 — 카드가 적은 메커니즘이 측정과 맞지 않는다. 카드는 '바닥값 12 가 저대비 구간을 지배하기 때문이다' 라고 했는데, 바닥이 실제로 지배하는 구간(대비 30~48, 문턱 경계는 12/0.25=48)에서 분해능 중앙값이 0.273 으로 멀쩡하다. 가드가 죽는 곳은 대비 30 미만이고 거기는 n=3/2500(0.12%)뿐이다. 즉 '저대비에서 분해능을 잃는다' 는 절반만 맞고, 원인이 바닥값이라는 설명은 이 측정이 지지하지 않는다.
- 2026-09-17T00:23-07:00 — 대신 드러난 것: 분해능 0.05 미만인 장이 모든 대비 구간에 7.6~12.3% 로 고르게 깔려 있다. 대비와 무관한 꼬리라 '저대비 문제' 로 부르면 잘못 조준하게 된다. 구간 중앙값만 보면 이 꼬리가 안 보이고, 꼬리만 보면 구간을 싸잡는다 — 그래서 진단이 둘 다 찍는다.
- 2026-09-17T00:23-07:00 — AC#3 은 바닥값을 바꾸지 않고 근거를 적는 쪽을 택했다. 측정이 바닥값을 범인으로 지목하지 않기 때문이다. 바닥 12 와 광학 노이즈 σ~uniform(2,9) 의 관계: σ=2 에서 6.00σ(거짓 잉크 0.00%) · σ=4 에서 3.00σ(0.27%) · σ=6 에서 2.00σ(4.55%) · σ=9 에서 1.33σ(18.24%). σ 가 큰 쪽에서 배경이 잉크로 잘못 세어지는 비율이 확 는다 — 대비와 무관한 꼬리의 원인 후보는 바닥값이 아니라 이 σ 상단이다. 수치를 맞추려 상수를 움직이지 않았다.

## Handoff

## Result
- 2026-09-17T00:23-07:00 — assets_dev/train/diag_gpc_resolution.py 신설 · validate_synth_panel.py 에 저대비 한정 비율 추가. 명령: python diag_gpc_resolution.py --count 2500 --seed 23000 --shift 8 (모집단: synth_panel.render_panel n=2500). AC#1 대비 구간별 gpc 분포를 인쇄한다 — 0~30 n=3 / 30~48 n=94 / 48~70 n=445 / 70~100 n=871 / 100~140 n=837 / 140+ n=250, 구간마다 gpc p50·p10·0.9미만 비율. AC#2 가드가 살아 있는지를 통과율이 아니라 어긋뜨림으로 증명했다 — 글리프 마스크를 8px 평행이동했을 때 분해능(정상 gpc - 어긋뜨린 gpc) 중앙값이 30~48 에서 0.273 · 48~70 에서 0.252 · 70~100 에서 0.293 · 100~140 에서 0.327 · 140+ 에서 0.319 다. 즉 저대비 구간에서도 가드는 어긋남을 본다. 죽은 구간은 대비 30 미만 하나뿐이고(분해능 중앙값 -0.023, 장 단위 100% 가 0.05 미만) 표본이 3/2500 = 0.12% 다. AC#3 바닥값을 바꾸지 않고 근거를 적었다 — 측정이 바닥값을 범인으로 지목하지 않기 때문이다. 바닥 12 와 σ~uniform(2,9): σ=2 6.00σ(거짓 잉크 0.00%) · σ=4 3.00σ(0.27%) · σ=6 2.00σ(4.55%) · σ=9 1.33σ(18.24%). AC#4 validate_synth_panel 이 저대비(밴드 대비<48) 한정 0.9미만 비율을 함께 찍는다 — n=400 seed 777005 에서 저대비 n=15 26.7% 대 그 외 n=385 10.6%. 카드 전제의 정정이 이 작업의 결론이다: 카드는 '바닥값 12 가 저대비 구간을 지배하기 때문' 이라고 했는데, 바닥이 실제로 지배하는 구간(대비<48)에서 분해능이 멀쩡하다. 대신 분해능 0.05 미만인 장이 모든 대비 구간에 7.6~12.3% 로 고르게 깔려 있다 — 대비와 무관한 꼬리라 '저대비 문제' 로 부르면 잘못 조준한다. 원인 후보는 바닥값이 아니라 σ 상단(σ=9 에서 배경의 18.24% 가 잉크로 세어진다)이고, 그건 이 카드의 범위 밖이라 수치만 남긴다.
