---
title: GM 검출 추론 전처리를 학습과 맞춘다 — 스트레치에서 레터박스로
status: done
ordinal: 26000
created: 2026-09-11
---

## Goal
<!-- kanban:goal:begin -->
YOLOX 검출기의 추론 전처리가 학습과 다르다. 학습은 레터박스(114 회색 패딩, 비율 유지)인데 추론은 스트레치다. 일치시키고 가로형 검출이 달라지는지 잰다. 재학습 없음.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 detect_datumo_gm.py 가 YOLOX preproc(레터박스)로 추론하는 경로를 갖고, 박스를 비율 r 로 역변환한다 — 기존 스트레치 경로는 플래그로 남겨 나란히 비교 가능해야 한다
- [x] #2 두 전처리로 2,494장을 각각 추론해 검출률과 박스 기하를 비교한 표가 나온다
- [x] #3 가로형 51장을 따로 분해한 표가 나온다 — 이 작업의 동기가 거기다
- [x] #4 G32 가 진단한 가로형 실패 13장이 각각 어떻게 달라졌는지 장 단위로 적힌다
- [x] #5 gmscreen_quads.jsonl 을 덮어쓰지 않는다 — 새 출력은 별도 파일로 낸다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T21:12-07:00 — 근거(2026-09-11 코드 확인): YOLOX 표준 preproc 가 레터박스다 — D:/tmp/YOLOX/yolox/data/data_augment.py:142-154, padded_img = np.ones(input_size)*114 이고 r = min(비율)로 비율을 유지한다. ValTransform 과 TrainTransform 둘 다 이 preproc 를 부른다. reading_exp.py 는 BaseExp 기본 로더를 그대로 쓰므로 학습은 레터박스다. 그런데 detect_datumo_gm.py:82 는 cv2.resize(img0,(imgsz,imgsz)) 스트레치이고 :91 의 역변환도 sx=sw/imgsz, sy=sh/imgsz 로 스트레치 기준이다. 파일 주석의 'YOLOX evaluator와 동일 전처리: 스트레치' 는 사실과 다르다.
- 2026-09-11T21:12-07:00 — 왜 가로형에서만 크게 아픈가: 3024x4032 세로 사진은 스트레치하면 가로가 33퍼센트 늘고, 4032x3024 가로 사진은 반대로 세로가 33퍼센트 늘어난다. 왜곡 방향이 사진 방향에 따라 뒤집힌다. 코퍼스의 98퍼센트가 세로라 모델이 그 방향 왜곡에는 견디지만 가로는 반대라 분포 밖이다. G32 의 '실패 16장 중 14장(88퍼센트)이 학습 데이터' 가 이것으로 설명된다 — 못 배운 게 아니라 학습 때와 다른 그림을 보여 주고 있는 것이다.
- 2026-09-11T21:12-07:00 — 이 가설은 exp 파일로 추정한 것이다. 학습이 실제로 표준 로더로 돌았는지는 확정이 아니다. 확정은 이 비교 실험이 한다. 수치가 안 움직이면 가설이 틀린 것이고 그대로 보고하라 — 결과를 좋게 보이게 만들지 마라.
- 2026-09-11T21:12-07:00 — 리더에는 레터박스를 넣지 마라. G29 에서 이미 기각됐다(97.06 -> 94.74퍼센트, 위험군 0.98 -> 3.48퍼센트, McNemar p=0.0001). 세로형 밴드를 320x160 에 레터박스로 넣으면 내용 폭이 320 -> 125px 로 줄어 CTC 유효 타임스텝이 40 -> 15.6 이 된다. 검출기와 리더는 지배 항이 다르다 — 검출기는 기하 충실도, 리더는 가로 표본 수.
- 2026-09-11T21:12-07:00 — 건드리지 말 것: gmscreen_quads.jsonl(2,494장, 파이프라인 전체가 의존한다). 새 출력은 별도 파일로 내고 비교만 한다. 채택 여부는 사람이 정한다. 체크포인트 yolox_out/gmscreen/latest_ckpt.pth 도 그대로 둔다 — 재학습 없는 작업이다.
- 2026-09-12T16:22-07:00 — 2026-09-12 표적 명시 — 사람이 밴드 라벨을 붙였는데 gmscreen_quads 가 없는 13장이 있다: glucose_batch1 의 1044 1046 134 1826 2198 2319 607 700 703 716 726 808 96. GM 검출기가 박스를 아예 못 낸 사진들이라 GM 크롭 기반 지표에서 통째로 빠진다(band_queue.py progress 가 집계 밖으로 보고). 이 카드의 전처리 수정이 이 13장을 회복시키는지 before/after 로 재라 — 사람 밴드 라벨이 이미 있어 IoU 를 바로 잴 수 있는 표본이다.
- 2026-09-12T16:24-07:00 — 2026-09-12 표적 정정 — 앞 Note 에 적은 13장 중 10장은 코퍼스 밖 사진이라 표적이 아니다. 코퍼스 안이면서 사람 밴드 라벨이 있는데 GM quad 가 없는 것은 3장이다: glucose_batch1/1046, glucose_batch1/1826, glucose_batch1/2198. 이 3장만 before/after IoU 로 재면 된다.
- 2026-09-12T20:57-07:00 — 2026-09-12 수치 출처 표시 — 이 카드 Notes 의 G29 기각 근거(97.06→94.74%, 위험군 0.98→3.48%)는 2026-09-11 scratch 재구축으로 폐기된 옛 리더의 성적이다. '리더에는 레터박스를 넣지 마라' 는 결론 자체는 그 실험이 보여준 방향이라 유지하되, 새 리더가 생기면 다시 재야 하는 잠정 판정으로 읽어라. 이 카드가 다루는 것은 리더가 아니라 GM 검출기의 추론 전처리라 그 기각과 축이 다르다 — 혼동하지 마라.
- 2026-09-12T21:02-07:00 — 2026-09-12 표적 확대 — v0 재평가(docs/reports/band-det-v0-regate-54devices.md)에서 더 큰 집합이 나왔다. 앞 Note 의 3장(gmscreen_quads 가 아예 없는 장)과는 다른 부류다. GM 이 박스는 냈는데 숫자를 잘라낸 장이 264장 중 18장(6.8%)이다 — 사람 밴드 라벨이 GM 크롭 안에 든 면적이 0.95 미만. 이 18장의 검출 IoU median 은 0.339(p10 0.000)이고, 온전히 든 246장은 0.727 이다. 숫자가 크롭 밖이면 어떤 밴드 검출기도 못 이긴다 — 하드 실링이다. 가장 심한 여섯: 2431(0.030) · 1610(0.098) · 1593(0.291) · 606(0.581) · 1614(0.603) · 1094(0.686). 이 여섯이 눈검증 시트의 최악 여섯과 정확히 같다. 재현: python band_det_clip_split.py. 이 카드가 전처리를 고친 뒤 같은 명령으로 before/after 를 재라.
- 2026-09-16T23:50-07:00 — 어긋남이 카드가 지목한 기하 하나가 아니라 둘이었다. 채널도 갈려 있다 — 학습 로더는 cv2.imread(BGR)이고 YOLOX preproc 는 채널을 안 바꾸는데, detect_datumo_gm.py 는 img[..., ::-1] 로 RGB 를 넣고 있었다. 파일 머리말의 'YOLOX evaluator와 동일 전처리: 스트레치+BGR' 은 두 군데 다 사실이 아니었다. 한 번에 둘을 바꾸면 어느 쪽이 움직였는지 알 수 없어 --preproc 로 세 팔(stretch/letterbox/yolox)로 갈랐다.
- 2026-09-16T23:50-07:00 — 카드가 적어 둔 기준 체크포인트(gmscreen/latest_ckpt.pth)는 낡았다 — 2026-09-12 에 ft3 가 정본으로 승격됐다. 운영 gmscreen_quads.jsonl 을 좌표까지 재현하는 것은 yolox_out/gmscreen_ft3/best_ckpt.pth 다(앞 40장 전수 대조, 좌표 완전동일 40/40, 최대차 0.0). 이 대조가 동시에 리팩터링 회귀 검사이기도 하다 — stretch 팔이 운영과 같은 줄이다.
- 2026-09-16T23:50-07:00 — 막힘: 이 카드의 체크포인트도 numpy 2.x 로 절여져 있어 'ModuleNotFoundError: No module named numpy._core' 로 안 읽혔다. 오늘 Case 7 에서 만든 migrate_ckpt_numpy1.py 로 열었다. Case 7 이 예측한 '2026-09-10 이전 체크포인트를 부르는 모든 스크립트가 같은 자리에서 죽는다' 가 두 번째 스크립트에서 그대로 재현됐다.
- 2026-09-16T23:50-07:00 — 개수 지표가 이 개선을 거의 못 본다. diag_wide_gm 의 GM 실패는 18/49 -> 17/49 로 거의 안 움직이는데, 같은 장의 w/h 중앙값은 1.98 -> 2.30 으로 뚜렷이 오른다. 접힘으로 남은 장들이 전부 문턱 쪽으로 올라왔다(0.72->1.17, 0.71->1.06, 0.80->1.10, 0.68->0.91). 1.2 문턱이 연속량의 이동을 통째로 삼킨 것이다. 개수와 분포를 나란히 놓지 않았으면 '효과 없음'으로 닫을 뻔했다. 그래서 --wide-detail 이 둘을 같이 찍는다.
- 2026-09-16T23:50-07:00 — 판정 규칙을 두 군데 쓰지 않으려고 diag_wide_gm.py 에서 classify()/horiz_ids() 를 함수로 빼고 gm_preproc_ab.py 가 그걸 부른다. 리팩터링 뒤 같은 입력에 같은 수(OK 31 · 미검출 7 · 접힘 11)가 나오는 것을 확인했다.
- 2026-09-17T11:24-07:00 — 2026-09-17 정정 — 이 카드 Result 의 마지막 문장이 틀렸다. '채널 축(yolox)은 … 가로에서 못해 판정이 깨끗하지 않다(1826 이 IoU 0.526 -> 0.039 로 무너진다)' 에서 1826 을 채널 축 탓으로 돌린 것이 오귀속이다. 짝비교로 다시 보면(명령: python gm_preproc_ab.py --arm letterbox=… --arm yolox=… --paired letterbox yolox) 가로 n=42 에서 yolox 승 5 · 패 14 · 무 23 이고 짝차이 중앙 -0.0044 인데 평균은 -0.0195 다 — 꼬리 한 장이 평균을 끈다. 전체는 n=405 승 161 패 95 무 149, 중앙 +0.0032 로 오히려 양수다. 1826 자체를 열어 보면(명령: python diag_gm_candidate_tie.py --ids glucose_batch1/1826,glucose_batch1/1598) 두 전처리의 입력이 픽셀 평균 3.37/255 밖에 안 다르고(그 사진은 |R-B| 평균 6.7 로 거의 무채색이라 R<->B 를 바꿔도 화소가 안 변한다), 진짜 원인은 후보 두 개가 0.3% 차이로 붙어 있는데 넓이가 33.5배 다른 것이다(기기 전체 2417x2920 vs 작은 조각 463x455). 채널이 아니라 근접 동점이고, 우리가 top-1 을 제정신 검사 없이 받는 것이 문제다. 보고서에 「2026-09-17 정정」 절로 전문을 적었고 --paired 를 자에 넣었다.

## Handoff

## Result
- 2026-09-16T23:51-07:00 — 보고서 docs/reports/gm-preproc-letterbox-ab-2026-09-16.md. 재학습 없음 — 체크포인트를 ft3/best_ckpt 하나로 고정하고 추론 전처리만 바꿔 쟀다. 코드: detect_datumo_gm.py 에 --preproc {stretch,letterbox,yolox} · --limit · --selftest 추가, gm_preproc_ab.py 신설, diag_wide_gm.py 의 판정을 classify()/horiz_ids() 로 빼 한 곳에서만 정의. AC#1 레터박스 경로와 비율 r 역변환 — python detect_datumo_gm.py --selftest 이 yolox 모드를 YOLOX 정본 preproc 와 픽셀 대조한다(4032x3024·3024x4032·1000x1000 세 모양 모두 픽셀 동일 True, 역배율 1/sx == r True, SELFTEST PASS). 기존 스트레치 경로는 기본값으로 남아 있고 운영 출력을 좌표까지 재현한다(앞 40장 전수 대조 40/40 동일, 최대 좌표차 0.0). AC#2 세 팔 전량 추론 후 비교표(모집단: 사람 화면 라벨 screen_boxes.jsonl source=human, n=410; 명령은 보고서 재현 절) — IoU 중앙 운영 0.828 -> letterbox 0.876 -> yolox 0.885, >=0.75 76.3% -> 89.1% -> 88.7%, 전량 검출률 2497/2512(99.4%) -> 2507(99.8%) -> 2508(99.8%). AC#3 가로 층(n=45) 분해 — IoU 중앙 0.614 -> 0.799 -> 0.768, >=0.75 15.4% -> 52.4% -> 50.0%. 세로 층(n=365)도 0.835 -> 0.880 -> 0.891 로 같이 오른다. AC#4 가로 49장 중 판정 부류가 갈린 9장을 장 단위로 적었다(회복 6: 1046·134·607·808·1826·2198, 퇴보 3: 1091·606·1094). AC#5 gmscreen_quads.jsonl 은 그대로다(mtime 2026-09-11 21:52) — 새 출력은 전부 _diag/preproc_ab/ 이고, 스크립트에 운영 파일 + 비스트레치 조합을 막는 가드를 넣었다. 가설이 맞았다. 다만 개수 지표는 이 개선을 거의 못 본다 — diag_wide_gm 의 GM 실패는 18/49 -> 17/49 인데 같은 장의 w/h 중앙값은 1.98 -> 2.30 이다. 접힘으로 남은 장들이 전부 1.2 문턱 쪽으로 올라왔고 문턱을 못 넘었을 뿐이다. 개수와 분포를 같이 찍게 만든 이유가 이것이다. 곁가지: '' ft3 가 놓친 5장 회귀 진단'' 카드의 5장(1046·1826·2198·498·702)이 전처리만 고쳐도 전부 살아난다 — 그 카드가 찾던 것은 ft3 의 회귀가 아니라 추론 전처리였다. 채택은 하지 않았다 — 카드 Note 대로 사람이 정한다. 채널 축(yolox)은 세로에서 낫고 가로에서 못해 판정이 깨끗하지 않다(1826 이 IoU 0.526 -> 0.039 로 무너진다).
