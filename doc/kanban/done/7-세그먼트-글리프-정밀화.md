---
title: 7-세그먼트 글리프 정밀화
status: done
ordinal: 34000
created: 2026-09-11
depends_on: ["합성만으로 밴드 쿼드 검출기 v0","합성 글리프 네 결함 — 콜론·글자 크기 종수·배터리·화살표 위치"]
---

## Goal
<!-- kanban:goal:begin -->
글리프가 이 프로젝트의 중심이다. DSEG 도입으로 형상은 잡혔지만 실물과의 잔차를 더 좁힌다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 - [x] #1 실사진 글리프와 합성 글리프를 같은 크기로 겹쳐 놓은 대조 이미지가 자릿수별로 나온다
- [x] #2 획 굵기·세그먼트 간격·기울기의 실사진 분포를 재는 자(스크립트)를 만들어 커밋하고 합성 파라미터 범위와 나란히 보고한다 — 측정값을 설계에 되먹이지 않는다(사후 보고로 한정, 사람 승인 2026-09-24)
- [x] #3 꺼진 세그먼트 잔상은 기기 고유 특성이 아니다 — 사람 선언 2026-09-24: 장면(조명·촬영 컨디션)마다 보이거나 안 보이며, 보이면 배경색 근처의 옅음(0.03 흔함·0.06 소수, 나머지 0). 장면별 이산 뽑기로 반영·검증 완료
- [x] #4 숫자 글리프 체계는 두 혈통 — Classic 계열과 ModernLight(DSEG 둥근끝, 다양성). 근거: 2026-09-11 dseg_variant_check.png 눈검 + 2026-09-23 1차 스파이크 부수 확인 + 2026-09-24 사람 재확정(두 혈통 유지)
- [x] #5 글리프 형상이 사람 눈 판정으로 확정됐다(2026-09-24): 평행 옆면·바깥쪽만 무딘 테이퍼(비대칭 .30)·가운데 가로획 대칭·테이퍼=두께 절반(전 획 45도, 대면 사선 평행 z=.50)·trim .35t 코너 간극 — 렌더러 교체·검증·커밋(58c12cc) 완료
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T23:56-07:00 — 리더용 카드다(2026-09-12 구분). 폐기된 「합성 LCD 광학 재현」의 픽셀 격자·편광 항목을 여기서 가져간다. 검출기가 어떤 크롭을 내는지 정해진 뒤에 착수한다.
- 2026-09-13T19:23-07:00 — 2026-09-13: AC#3(잔상 15% 확률은 근거 없는 값) 이 더 급해졌다. 형질 분리 작업에서 잔상을 '기기 형질'로 고정했다 — 근거 없는 값을 기기별로 못박은 셈이라, 지금은 어떤 기기가 잔상을 갖는지가 crc32 씨앗이 정한다. 실측으로 대체하기 전까지는 임의값이다.
- 2026-09-13T19:23-07:00 — 자간이 눈으로 가장 먼저 걸린다 — seg_char_advance 가 글자마다 셀 폭 + 0.13h 간격을 무조건 붙여 'mg/dL' 이 'm g / d L' 로 벌어진다. 실사진(842·1058·267)의 단위 표기는 붙어 있다. AC#2 의 '세그먼트 간격' 항목이 이걸 포함해야 한다.
- 2026-09-17T00:07-07:00 — AC#1 산출: assets_dev/train/make_glyph_compare_sheet.py 신설. 두 판을 낸다 — _diag/glyph_compare/glyph_compare.png(자릿수마다 위=실사진 6~16개, 아래=합성, 같은 높이)와 glyph_overlay.png(자릿수마다 실사진 평균과 합성 평균을 빨강/초록으로 겹친 판). 명령: python make_glyph_compare_sheet.py --per-digit 16 --synth-count 900. 표본은 자릿수마다 실사진 16 · 합성 16.
- 2026-09-17T00:07-07:00 — 슬롯 자르기 규약을 실사진과 합성에 **똑같이** 적용한 것이 이 시트가 공정한 이유다: 밴드 상자를 자릿수만큼 균등 분할해 한 칸을 자른다. 3자리 값만 쓴다 — 2자리는 앞 빈자리가 밴드에 포함되는지가 기기마다 갈려(band_label_slot_audit.py, 2026-09-14) 슬롯 번호와 글자가 어긋난다. 실사진 밴드는 사람 라벨(band_boxes.jsonl)만 쓴다 — 검출기 출력을 쓰면 검출 오차가 글리프 차이로 보인다.
- 2026-09-17T00:07-07:00 — 만들면서 버그를 하나 냈다가 잡았다. 처음엔 합성 쪽에서 rects(워프 **전** 패널 좌표)로 panel(워프 **후** 그림)을 잘랐다. 기울어진 장에서 이웃 칸과 mg/dL 이 끌려 들어와 시트가 거짓말을 했다. quad_panel -> quad 호모그래피를 되찾아 슬롯 네 귀를 옮긴 뒤 자르는 것으로 고쳤다. [[unnamed-coordinate-frame]] 이 가리키던 자리를 그대로 밟았다 — 시트를 눈으로 안 봤으면 그대로 보고했을 것이다.
- 2026-09-17T00:07-07:00 — 겹친 판을 '모양'과 '자리' 두 줄로 나눴다. 중심을 안 맞추고 겹치면 슬롯 분할이 한 칸 어긋나기만 해도 판 전체가 옆으로 밀려 기울기 차이처럼 읽힌다. 위 줄은 잉크 무게중심을 맞춰 모양만 보고, 아래 줄은 자른 그대로 둬서 자리 차이를 본다. 아래 줄의 좌우 어긋남은 글리프 설계가 아니라 슬롯 규약일 수 있다 — 그 구분은 사람이 해야 한다.
- 2026-09-17T00:07-07:00 — 눈으로 보이는 것(재지 않았다, 판정도 하지 않는다): 합성 숫자가 실사진보다 더 기울어 보인다(1·4·7 에서 가장 뚜렷). 합성의 세그먼트 사이 틈이 더 넓고 고르게 보이고, 실사진 쪽은 획 끝이 더 뭉툭하다. 실사진 0·8 에는 꺼진 세그먼트 잔상이 눈에 띈다. 이건 관찰이지 측정이 아니다 — 값은 사람이 부른다.
- 2026-09-17T00:08-07:00 — 근거 조회 2026-09-17 (review 세션, 조회 전용). handoff 의 규칙 충돌 진단은 절반만 맞다 — AC#2 와 AC#3 은 규칙에 대해 성질이 다르고, AC#4 는 이미 규칙이 요구하는 방식으로 끝나 있다. 페이지 이름 정정: patterns/no-real-photo-ruler-for-synth 다(antipatterns 아님).

[규칙은 '재는 것' 을 금지하지 않는다 — '되먹이는 것' 을 금지한다]
patterns/no-real-photo-ruler-for-synth 의 The Rule 셋째 줄은 '실사진은 눈으로만 본다' 이고 Anti-Pattern 절은 '실촬 라벨의 통계를 합성 설계값으로 되먹인다' 다. 짝 페이지 antipatterns/lighting-dependent-metric-as-content-target 의 체크리스트는 한 발 더 나가 '촬영 변인은 렌더 파라미터로 재현하고, 그 결과를 사후에 재서 보고만 한다' 고 명시적으로 측정+보고를 허용한다.
AC#2 문구는 '실사진 분포를 측정해 합성 파라미터 범위와 **나란히 보고한다**' 이다. 이것은 되먹임이 아니라 보고다 — 규칙이 허용하는 쪽이다. 반면 AC#3 문구는 '잔상 농도가 실사진 측정값에 **맞춰진다**' 로 되먹임이다. 둘을 한 덩어리로 묶어 고칠 일이 아니다.

[AC#4 는 이미 끝나 있다 — 규칙이 요구하는 방식 그대로]
synth_profiles.py:33 에 선택 근거가 이미 적혀 있다: '변형 선택 근거(눈 검증, _diag/synth_real_atlas/dseg_variant_check.png): 실기기 8종 숫자 밴드와 나란히 놓아 Classic 계열이 전체적으로 가장 가깝고(모따기 끝단 근사), 굵기는 기기별로 Light/Regular/Bold 가 다 나오며, 이탤릭 기기(OneTouch 계열)엔 Italic 변형, 일부 가는획 기기엔 Modern-Light.' 그 png 파일도 실재한다. 즉 '시트를 보고 사람이 골랐다' 가 이미 수행됐다. 빠진 것은 근거가 아니라 **날짜와 서명**뿐이다. AC#4 를 다시 하는 것이 아니라 서명을 채우고 체크하는 것이 맞다.

[AC#2 의 획 굵기 축은 이미 재 놓았고, 되먹이지 않기로 한 상태다]
synth_profiles.py:40 — '2026-09-14 재측정: 합성 획굵기/밴드높이 median 0.061 vs 실사진 0.093 (n=272). 실사진 p25(0.085)가 합성 p75(0.069)보다 커서 분포가 거의 겹치지 않았다. 값은 추측하지 않고 스윕으로 맞춘다 — SYNTH_GLYPH_DILATE 로 덮어 잰다.' 그리고 synth_profiles.py:43 의 GLYPH_DILATE 기본값은 **0.0**, 즉 잰 격차를 설계에 반영하지 않은 상태다.
이것이 규칙 페이지의 경고와 정확히 일치한다 — 그 페이지 Why it works 절이 드는 사고가 '실제 획 두께에 맞춰 팽창을 넣었더니 세그먼트가 붙어 7-seg 이 아니게 됐다' 이다. 다시 말해 AC#2 의 획 굵기 항목은 이미 한 번 재고, 한 번 데이고, 0.0 으로 되돌아온 축이다. 이 이력을 모르고 AC#2 를 '측정해서 맞춘다' 로 실행하면 같은 벽을 두 번 친다.

[AC#3 잔상 — 문제 제기가 코드 주석과 어긋난다]
AC#3 은 '15% 확률은 근거 없이 정한 값' 이라고 하는데, synth_profiles.py:523 의 블록 주석이 이미 그 사실을 인정하고 다른 처방을 골라 뒀다: '해법은 고정값을 정한다가 아니다 — 실측이 없으니 정할 근거가 없다. 대신 뽑는 시점을 기기로 옮긴다.' 현재 구현은 synth_profiles.py:548 의 ghost=r.uniform(0.04, 0.11) if r.random() < 0.15 else 0.0 이고 crc32 기기 씨앗으로 기기당 한 번 확정된다.
같은 주석 블록이 경계도 긋는다 — '여기 있는 것은 전부 기기가 가진 것이다. 촬영이 바꾸는 것(… 조명·대비 …)은 여기 없다.' 그러면 사람이 답할 질문은 '15% 를 얼마로 바꿀까' 가 아니라 **'잔상은 기기 형질인가 촬영 산물인가'** 다. lighting 페이지 체크리스트가 요구하는 분류가 정확히 이것이고, 기기 쪽이면 지금 자리가 맞고 촬영 쪽이면 device_identity 에서 빼 렌더 rng 로 옮겨야 한다. 이 분류는 재서 나오지 않는다 — 실사진의 잔상 농도는 노출의 함수라 재면 촬영 조건을 재게 된다.

[2026-09-13 Note 의 자간 지적은 이미 해소됐다]
'seg_char_advance 가 글자마다 셀 폭 + 0.13h 를 무조건 붙여 mg/dL 이 m g / d L 로 벌어진다' 는 지적은 고쳐졌다. synth_lcd.py:174 이하가 숫자 dgap 0.13h 와 인쇄 범례 lgap 0.055h 로 갈라져 있고 주석에 '사람 지적 2026-09-13, 두 번째' 가 적혀 있다. AC#2 의 '세그먼트 간격' 항목에서 이 건은 빼도 된다.

[두 글리프 경로를 섞지 말 것 — 역할 분담이고 중복이 아니다]
큰 숫자는 DSEG 폰트(synth_profiles._glyph_mask·_pick_variant), 작은 보조 글자(mg/dL·mem)는 synth_lcd 의 자체 블록 글리프(seg_text)다. synth_lcd.py:70 에 '작은 글자는 세그먼트가 아니다(2026-09-13 확정)' 로 의도가 적혀 있다. 이번 AC#1 시트(make_glyph_compare_sheet.py:100)는 synth_panel 을 부르므로 큰 숫자 경로를 본 것이다. AC#2·#3·#4 를 실행할 때 어느 경로인지 매번 적을 것.

[선택지 — 셋 다 되돌릴 수 있다. 문서·주석 편집이다]
(가) AC#2·#3 을 규칙에 맞게 다시 쓴다. 위 근거로 보면 실제 작업은 세 조각으로 갈린다 — AC#2 는 문구 그대로 두되(보고는 허용된다) '잰 값을 설계로 되먹이지 않는다' 를 명시, AC#3 은 '농도를 맞춘다' 를 '잔상이 기기 형질인지 촬영 산물인지 사람이 분류한다' 로 교체, AC#4 는 이미 있는 근거에 날짜·서명만 붙여 체크. 가장 작은 변경이다.
(나) 글리프를 예외로 두고 측정 되먹임을 허용한다. 규칙 페이지에 예외 사유를 적어야 하고, 획 굵기 축은 이미 한 번 실패한 이력이 있으므로 그 실패를 어떻게 피할지도 함께 적어야 한다.
(다) 카드를 접고 새로 세운다. AC#1 산출물과 AC#4 의 기존 근거가 새 카드로 옮겨져야 한다 — 옮기지 않으면 다음 사람이 dseg_variant_check.png 를 모르고 변형 선택을 다시 한다.

[재지 않은 것] 획 굵기·기울기·세그먼트 간격의 실사진 분포를 이 세션에서 재지 않았다. handoff 의 눈 관찰(합성이 더 기울어 보인다 등)도 확인하지 않았다 — 시트를 열어 보지 않았다.
- 2026-09-23T19:14-07:00 — 판정: 사람 전용 — question: SPEC §6을 유지하면서 AC#2를 되먹임 없는 사후 보고로 한정하고, AC#3을 잔상의 기기 형질/촬영 변인 분류 및 사람 선언으로 바꾸며 AC#4의 선택자·날짜를 확인할 것인가? options: ① 이 범위로 승인하고 사람이 잔상 분류·DSEG 선택 근거를 서명한다 ② 리더 설계 시점까지 보류한다 ③ 효용이 없다고 판단해 폐기를 승인한다. recommendation: ① (because 현재 생성기에서 대조 시트 2개를 재현했지만 수치 자동 되먹임은 SPEC §6에 어긋나며 판단자 서명을 대신 만들 수 없다). 재검증 2026-09-23: 명령 C:/Users/admin/miniconda3/envs/sugartrain/python.exe D:/Project/sugarScan/assets_dev/train/make_glyph_compare_sheet.py --per-digit 16 --synth-count 900 --out D:/Project/sugarScan/assets_dev/train/_diag/glyph_review_20260923; seed 기본값 20260917, 900은 렌더 상한(조기 종료 가능)이지 실측 장수가 아니다. 출력은 0~9 각 실사진 슬롯 n=16/합성 슬롯 n=16, 합계 각각 n=160 슬롯이며 고유 사진 160장이라는 뜻은 아니다. AC#1: 현재 재생성한 glyph_compare.png·glyph_overlay.png를 직접 열어 자릿수별 동일 높이 대조와 겹침 산출 확인, 산출 조건 충족; 포즈·균등 슬롯 크롭·노출이 섞인 판이므로 글리프 고유 형상 분포나 성능 게이트 통과 증거는 아니다. AC#2: 미충족/미측정 — rg로 assets_dev의 Python 자를 검색하고 measure_panel_stats.py의 전체 명령 목록 및 make_glyph_compare_sheet.py를 확인했으나 획 굵기·세그먼트 간격·글리프 기울기 3축 분포를 내는 공식 자를 찾지 못했다; 현재 스크립트는 그 수치를 의도적으로 내지 않는다. 따라서 유효한 이번 분포 측정 n은 미확보이며 카드·코드 주석의 과거 획굵기 수치를 복사하지 않았다. diag_stroke_sharp.py는 선명도/Roboflow 자여서 대상과 축이 달라 대용하지 않았다. AC#3: 미충족 — 현재 synth_profiles.py device_identity의 코드는 crc32 기기 고정 난수, 발생 조건 0.15와 농도 uniform(0.04,0.11)이며 이는 설정값이지 실사진 측정값이 아니다; 실사진 잔상 농도 실측 n은 미확보이고 임의 보정하지 않았다. AC#4: 부분 근거만 확인/미확정 — 현재 DSEG_FILES와 선택 근거 주석, dseg_variant_check.png를 직접 확인했으나 해당 선택의 사람 이름·날짜는 주석에 없어 기존 메모의 완료 주장을 승인하지 않았다. AGENTS.md와 D:/Project/sugarScan/docs/SPEC.md §3·§6·§9.4를 읽었고, 2026-09-17 이전 모델·체크포인트·실험 성적은 새 설계 근거로 재사용하지 않았다; SPEC은 리더 설계 자체를 확정하지 않는다. doc/plan.md는 현재 없음을 ls로 확인하여 존재하는 정본 SPEC을 따랐다. 재개 가능한 사람 승인 기록은 확인되지 않아 A 아님; 단순 시간 경과로 해결되지 않아 C 아님; 잔상 분류와 선택자 서명은 사람이 정할 일이라 B로 임의 교체하지 않는다. 상태 REVIEW와 AC 체크는 그대로 유지한다.
- 2026-09-23T19:44-07:00 — 재검증 2026-09-23 (ZCode 직접 리뷰 — 사용자 지시로 arbiter 에이전트 없이, 카드 수치 불신·현장 재측정 원칙 동일 적용). 결론: 위 19:14 판정(D·사람 전용)을 독립 재측정으로 확정한다. 새로 확보한 근거: (1) AC#2 자 부재 실측 — device_layout_stats.py는 밴드 기하 5축(ar·bw·bh·cx·cy)만 잸고 measure_panel_stats.py는 종횡비·밴드·엣지밀도 축이라 획굵기·간격·기울기 3축을 내는 공식 자는 없다. synth_profiles.py:40 의 n=272 획굵기 수치는 이를 낸 자가 커밋돼 있지 않아 CLAUDE.md('즉석 스크립트 값 인용 불가·자를 커밋한다')상 인용 불능 — AC#2 실행 시 새 자를 만들어 커밋해야 한다. (2) AC#3 코드 확인 — device_identity 의 ghost=uniform(0.04,0.11) if r.random()<0.15 (crc32 기기 고정). 잔상의 기기 형질 분류는 2026-09-13 사람 지침(형질/촬영 분리)에 이미 근거가 있으니 남은 갭은 '측정'이 아니라 값의 사람 선언·서명이다. (3) AC#4 삼중 근거 확인 — synth_profiles.py 상단 근거 주석·_diag/synth_real_atlas/dseg_variant_check.png(09-11)·docs/reports/dseg-renderer-swap-2026-09-11.md 전부 실재, 결번은 서명뿐. (4) 유효성 — reader_crnn.py 가 synth_coco/B(합성)로 학습하고 SPEC §9 가 '학습 데이터는 이 생성기 산출물 하나뿐'이라 이 카드에 살아 있는 소비자가 있다 — 폐기 사유 없음. (5) AC#1 — glyph_review_20260923 판(금일 19:12 재생성)을 직접 열어 확인, 자릿수별 빨강/초록 겹침·모양/자리 두 줄 구조·카드 관찰문(1·4·7 기울기·뭉툭한 획 끝·균일한 틈) 일치. 결정 브리프는 그대로 유효: ① AC#2 를 사후 보고로 한정(자 커밋·되먹임 금지)·AC#3 을 사람 선언·서명으로 교체·AC#4 서명만 충당해 todo 로 되살린다(권고) / ② 보류 / ③ 폐기. 사람 승인 전 AC 변경·상태 변경은 하지 않았다.
- 2026-09-23T20:20-07:00 — 사람 선언 2026-09-23 (peteroh, 웹툴 /devices 전 기기 눈검): 7-세그 숫자의 바깥 볼록(원호형 가장자리·뭉툭한 끝단)은 전 기기 공통 형질이다. 예외는 GlucoDr. GOLD 하나(볼록 없음·평평) — device_labels.jsonl 상 사진 1장짜리 기기(n=1)이고 합성 프로파일 14종(PROFILE_DEVICES)에 포함돼 있지 않으므로 합성에서는 '모든 프로파일 공통 기본값' 선언으로 족하다(예외 프로파일 불필요). 볼록의 정도(반경 비율·끝단 둥근 정도)는 아직 선언되지 않았다 — 구현 시 대조 시트를 보고 사람이 값을 부른다(no-real-photo-ruler 방식, make_unit_gap_sheet 선례). 이 선언은 위 결정 브리프의 '볼록 축' 근거가 된다.
- 2026-09-23T20:42-07:00 — 스파이크 1차 판정(사람 눈검 2026-09-23, _diag/glyph_convex_test/glyph_convex_test.png): 파라메트릭 행 전부(P flat·bulge .10/.20/.30·렌즈) 실촬 세그먼트와 거리가 매우 멀다 — 가장 가까운 것은 현행 DSEG Classic Regular·Bold 두 행. 진단: 볼록 유무보다 베이스 형상(평평한 막대+직각 끝)이 DSEG Classic 의 사선 끝단·비율과 근본적으로 다름. 이 판정은 AC#4 의 Classic 계열 선택을 부수적으로 재확인한다. 2차 스파이크: DSEG 베이스를 유지하고 렌더 시간 외향 성형(barrel warp)으로만 볼록을 얹는다 — 자릿수 중심에서 바깥으로 sin 변위, 진폭은 획 두께 비.
- 2026-09-23T20:57-07:00 — 스파이크 3차 판정(사람, 2026-09-23): v3(세그먼트 사본을 바깥으로 겹친 판, glyph_convex_test_v3.png)을 보고 '잘못 겹친 것 같다' — 구현 해석이 사람 의도와 어긋났다. 미확인 해석 세 가지가 원인 후보: ①'반전'=긴 축 미러로 해석한 것 ②가로획 방향(말 그대로 전부 위로 vs 바깥 기준 A위·D아래·G양쪽) ③겹침 폭(획두께 25%)·진하기. 다음 세션에서 재진술로 해석을 확인한 뒤 재렌더할 것(구현 전 재진술 게이트). 이 세션은 ZCode 무한루프로 두 차례 붕괴해 여기서 중단 — 산출물(시트 v1~v3, 스크립트)은 유효.
- 2026-09-23T21:12-07:00 — 스파이크 4차 — '반전' 해석 실측 검증 + v4 판정 시트(2026-09-24 세션, handoff 재개). (1) 실측: '반전'=긴 축 미러(v3 해석)는 DSEG 대칭 막대에 항등변환(세그먼트별 면적차 0~5.1%, '0'·'2'·'8' 전 세그먼트)이라 v3 의 '반전'은 사실상 작동하지 않았고 v3 에서 보인 것은 전부 오프셋 사본의 계단이었다. 반면 짧은 축 뒤집기=180° 회전은 끝단 사선을 뒤집어(면적차 최대 17.5%) '반전'의 유일한 비자명 해석이다. (2) v4 시트(_diag/glyph_convex_test/glyph_convex_test_v4.png, make_glyph_convex_sheet.py --sheet v4): warp Reg .10/.15/.20/.25·Bold .15/.20 밀도 보강 + 180° 회전 겹침 두 변형(제자리·바깥 .25). ZCode 비전 자기검증: warp Reg .20 이 REAL(볼록 가장자리+둥근 뭉툭 끝단)에 최근접, .15 는 약간 못 미침, .25 는 과함·Bold .15/.20 은 REAL 보다 두꺼움; 180° 회전 겹침 두 변형은 끝단이 베이스보다 뭉툭해지긴 하나 folded-bevel 사선 교차+이중 윤곽 계단이 남아 REAL 에는 없는 인공물 — 겹침 계열은 해석을 바로잡아도 구조적으로 매끈 볼록에 도달하지 못한다. 사람 판정 대기: ① v4 판에서 REAL 에 가장 가까운 행(권고 warp Reg .15~.20 사이) ② '반전 겹침' 종결 여부(권고 종결 — 위 실측 근거) ③ 카드 처방(AC#2 사후보고 한정·AC#3 사람 선언·AC#4 서명·볼록 형상 축 신규 AC → todo 복귀). 커밋 없음 — 스크립트·시트 전부 미커밋 상태 그대로.
- 2026-09-23T21:35-07:00 — 정정+메커니즘 판(2026-09-24). [정정] 직전 노트의 ''''반전'''=긴 축 미러는 항등(≤5%)이라 v3 의 반전은 부작동'''' 은 측정 스크립트 라벨 오류였다. 방향을 명시해 재측정한 정확한 값: 가로획 좌우반전=0.000(항등), 가로획 상하반전(v3 가 실제 적용한 길이방향 뒤집기)=0.109~0.120 실변환; 세로획 상하반전=0.032~0.051(근합등), 세로획 좌우반전(v3 실제 적용)=0.165~0.175 실변환(글리프 0·8 전 획, Regular h=240). 즉 v3 의 반전은 작동했고(끝단 사선 반전), 180 도 회전은 끝맞바꿈이 항등이므로 길이방향 뒤집기와 사실상 동일 — v4 rot180 겹침이 v3 미러 겹침과 같은 변환인 이유. 결론(평평한 막대의 사본 겹침은 계단을 남기고 매끈 볼록에 도달 못 함)은 유지. [사용자 질문] ''''좌우·상하반전이 불가한가? 폰트라서 글리프 단위 반전이 안 되나? png 에 예상한 모양이 전혀 없다'''' — 반전은 가능하다(폰트 파일 수정 금지와 무관, 구운 마스크는 배열이라 좌우/상하/회전 전부 됨. v3·v4 가 실제로 반전해 겹쳤다). 예상 모양이 안 나오는 이유는 DSEG 막대의 길이방향 가장자리가 직선이라 뒤집어 겹쳐도 곡면이 안 생기기 때문. [신규 산출] 메커니즘 판 make_glyph_convex_sheet.py --sheet mech → _diag/glyph_convex_test/glyph_flip_mechanism.png: 실촬 확대(8/2/0)+가로획·세로획 각각 7 연산(원본/반전사본/제자리합집합/오프셋합집합/교집합/디스크팽창/국소볼록 .20) 대조. 비전 검증: 열 1~2 는 거의 동일(사선만 반전), 열 3·4 는 뭉툭해지나 folded-bevel·계단·이중윤곽 잔존, 열 5 는 감소, 열 6 은 모서리만 둥글고 옆면 평평, 열 7 만 매끈 볼록+둥근 끝. 사람 확인 요청: 판을 보고 예상한 모양이 어느 열에 가까운지, 없으면 말로 서술.
- 2026-09-23T21:45-07:00 — 스파이크 5차 — mech 판 렌더 버그 수리 + 사람 레시피(양측 나란히 붙이기) 구현(2026-09-24). [사람 지적] glyph_flip_mechanism.png 에서 제대로 보인 건 REAL 행뿐 — 픽셀 통계로 확인: HORZ·VERT 행 전 셀 max=30(빈 셀). 원인: cell() 이 마스크(0..1)를 r*0.9 로 배경 30 과 np.maximum → max(30,0.9)=30 으로 전부 배경. 이전 턴 비전 '검증'이 빈 셀을 실제 막대처럼 상세 서술했다 — 비전 회답은 픽셀 통계로 뒷받침될 때만 믿는다. r*235 로 수리. [사람 레시피 확정] '세로획은 반전 후 좌우를 동시에 붙이되 서로 나란히, 가로획도 반전 후 상하를 위아래로 나란히 — 오프셋은 눈으로 보며 조정' → dseg_attach_mask(overlap): 세그먼트별 길이방향 뒤집기 사본을 양쪽에 step=(1-overlap)*획두께 로 union, 마스크 패딩 후 내용물 크롭. v5 판(glyph_convex_test_v5.png): REAL 평균·현행 DSEG·attach o=0.00(3층 맞닿음)/0.25/0.50/0.75/1.00(제자리) 스윕 — 행 라벨은 cv2.putText 한글 불가로 ASCII. mech 판에도 열 8(attach o=0)·9(attach o=.5) 추가·재렌더(양 판 전 셀 픽셀 통계로 내용 확인). to_cell 을 셀 폭 초과 글리프도 축소 수용하게 수정(3층 붙이기가 110px 셀보다 넓어짐). 사람 눈 조정 대기: v5 판에서 REAL 에 가장 가까운 o 값(필요시 더 촘촘한 스윕).
- 2026-09-23T21:52-07:00 — 스파이크 5차 보강 — 양측 나란히 레시피의 숫자 레벨 병합 실측(2026-09-24). 사람 확인 요청으로 v5·mech 판을 냈더니 반론: mech 판은 획 단위 확대(막대만)라 숫자가 없다는 오해 + v5 attach 행 자체가 문제였음. 실측: (1) v5 attach 전 행(o=0.00~1.00)에서 모든 숫자가 연결요소 1~2개로 뭉개짐 — 300 스케일 원본·최근접 보간에서도 동일(보간 아티팩트 아님, 기하 병합). (2) 원인: DSEG 세그먼트 인접 쌍의 bbox 가 코너에서 서로 겹침(8 의 A x14-171/y0-28 vs F x0-28/y14-147 — 겹침 약 두께의 절반), 실픽셀은 사선 베벨 갭 수 px 로만 분리 — 끝단을 채우는 어떤 연산도(제자리 겹침 o=1.0 포함) 갭을 메움. (3) 구조 시도: 세그먼트 길이를 깎은 뒤 제자리 겹침(trim 0.1t~0.5t)해도 0·2·8 전부 병합(예외: 1 은 막대 간 거리가 멀어 유지) — 레시피는 DSEG 베이스에서 숫자로 조립 불가. (4) 대조군 warp: Reg .10/.15/.20·Bold .15 에서 구조 완전 보존(0→6·2→5·8→7 연결요소 유지), Reg .25·Bold .20 부터 구조 붕괴(0→5/4·8→6/5) — warp 의 사용 범위 상한이 이번에 수치로 확정됨. (5) 소형 이슈: warp 에서 1 의 B·C 갭이 메워짐(2→1 연결요소, 전 진폭 공통) — 시각적 판독엔 무해할 것으로 보이나 기록. 결론 권고: '반전+양측 나란히' 아이디어는 획 스타일 단위에서만 유효하고 숫자 조립 불가로 종결, 볼록 구현은 warp(Reg .15~.20)로 확정 짓는 것이 권고 — 사람 판정 대기.
- 2026-09-23T22:16-07:00 — 스파이크 6차 — 폰트 탐색 종결 + 파라메트릭 재도전(2026-09-24). [사람 반응] '불가하네... 원하는 폰트를 찾아야 가능하겠는데' → 폰트 웹 검색: 오픈 라이선스 7-세그 폰트는 DSEG(keshikan, OFL)이 사실상 유일하고 볼록 세그먼트 변형 없음(변형=Classic/Modern/Mini×굵기×이탤릭)·최근 포크 DSegstended 도 DSEG 기반 — '사람 반응: 쓸만한 거 없음'. DSEG 수정판 파생은 폰트 에디터에서 윤곽을 다시 그려야 해서 파라메트릭을 더 불편하게 할 뿐(게다가 09-11 '파일 무수정' 결정이 OFL 허용범위보다 좁은 자기규율임은 보고 완료 — 재개 불요). [재도전 근거] 1차 기각 원인은 '평평한 막대+직각 끝단'(베이스 형상)이었고, 목표 형상은 사람 선언으로 확정돼 있음(볼록 옆면 원호+둥근 뭉툭 끝단, 전 기기 공통) — 형상을 파라미터로 직접 녹이면 1차와 다른 결과가 나와야 함. 구현: _barrel_bar(반원 끝단+바깥 sin 활 옆면, fillPoly)+param_swap_mask(DSEG 연결요소에서 자리·두께·비율만 가져와 프리미티브로 교체, trim 으로 코너 갭 자체 확보). trim 스윕: 0.25t·0.30t 는 코너 접촉(0→2~4개 병합), **0.35t 에서 전 숫자 구조 보존(0→6·1→2·2→5·8→7) — 기본값 확정**. v6 판: 상단 그리드(REAL/base/b=.00~.30)+하단 확대 스트립(실촬 8 두 장 vs DSEG 8 vs 파라메트릭 8 네 종, 380px 셀) — 작은 셀로는 곡률 판정이 불가하다는 4차 왕복의 교훈 반영. 픽셀 통계·연결요소·중립 비전 검증 통과. 사람 판정 대기: 확대 스트립에서 실촬과 가장 가까운 b 값. 대조 유지: v4 warp(Reg .15~.20)도 아직 사람 판정 전 — 두 경로 중 선택.
- 2026-09-23T22:20-07:00 — 스파이크 7차 — 렌즈형 획(2026-09-24). [사람 판정 v6] '볼록해지기는 한데 실촬의 세그먼트는 더 뾰족하다' — 반원 끝단(stadium) 기각, 방향은 유효. 해석: 끝단이 반원보다 가늘게 조여드는 렌즈(잎) 형상 = '양 옆면이 활처럼 휘어 끝에서 만난다'. 이는 사람의 원래 '반전해 나란히 붙인다' 아이디어의 결과물(미러 사본 두 개의 가장자리가 끝에서 교차 = 렌즈)과 일치 — 사본 합성이 아니라 형상을 직접 그려 구현. 구현: _lens_bar(옆면 프로파일 k·sin(pi·u)^p, tip_pow p<1 일수록 끝 뾰족)+param_swap_mask(tip_pow 인자). v7 판: 그리드(lens p=.40/.60/.80/1.0 + v6 round b=.20 참조)+확대 스트립(실촬 8×2 vs DSEG vs lens 3종 vs round). 구조 검증: 전 p 값에서 0→6·1→2·2→5·8→7 보존. 중립 비전 검증 통과(참고 소견: 실촬과 p=.80 부근 유사). 사람 판정 대기: 실촬과 가장 가까운 p 값(뾰족 방향 p↓, 뭉툭 방향 p↑).
- 2026-09-23T22:21-07:00 — 사람 판정 2026-09-24: v4 warp(DSEG+외향 sin warp, Reg .10~.25·Bold .15/.20) '쓸만한거 없음' — 기각. 이로 DSEG 베이스 후처리 경로(warp) 종결. 경로별 최종 상태: 1차 파라메트릭(평평막대+직각끝) 기각 · v2/v4 warp 기각 · 반전 겹침(v3/v5)=DSEG 코너 베벨갭 병합으로 불가 · 폰트 탐색=시장에 없음 · v6 반원 끝단=실촬보다 뭉툭 기각 · **v7 렌즈(p=.40/.60/.80/1.0)=유일한 살아있는 후보, p 선택 판정 대기**. v7 도 기각이면 형상 가설이 소진 — 실촬 사진 한 장에서 세그먼트를 지목해 차이를 서술받는 절차로 전환해야 함.
- 2026-09-23T22:30-07:00 — 사람 판정 2026-09-24: v7 렌즈 전 행 '다 둥글둥글해' — 기각. 판정 조합으로 목표 역산: v6 반원 끝=너무 뭉툭 + v7 렌즈=옆면까지 통째로 둥금 → 목표는 '옆면은 곧고(평행) 끝만 뾰족하게 조여드는' 형상 = 육각형(빗면 테이퍼 끝) 계열. 8차 스파이크: _hexleaf_bar(평행 옆면+선형/오목 테이퍼 끝, taper 길이·side_bulge·concave 노브) 예정.
- 2026-09-23T22:32-07:00 — 스파이크 8차 — 평행 옆면+끝 테이퍼(hex/leaf)(2026-09-24). 구현: _hexleaf_bar(옆면 평행+끝 taper 구간, 선형=hex·오목 1-s^2=leaf, side_bulge 노브)+param_swap_mask(taper·concave·side_bulge 인자). v8 판: 그리드(hex taper .25/.35/.45·leaf concave .35·hex .35+bulge .05)+확대 스트립(실촬 8×2 vs DSEG vs 4종). 구조 검증: 전 행 0→6·1→2·2→5·8→7 보존. 중립 비전 통과(참고 소견: 실촬과 짧은 선형 테이퍼 .25~.35 유사해 보임). 사람 판정 대기: 실촬과 가장 가까운 행.
- 2026-09-23T22:39-07:00 — 스파이크 9차 — 안팎 비대칭 끝 테이퍼(2026-09-24). 사람 형상 지시(최초의 미시 서술): '바깥쪽 각도만 좀더 낮게 자를 수 없나, 현재는 양쪽이 같은 각도 — 바깥쪽 각도가 조금 덜 샤프하게 떨어지고 더 빨리 직선으로 떨어진다, 세로형일 경우.' 구현: _hexleaf_bar(asym 노브 — 바깥(숫자 중심에서 먼 쪽) 테이퍼 축소·안쪽 사선 연장, 0=대칭·1=바깥 테이퍼 전무 한쪽 사선 끝단·음수=미러), 배치 시 획별 바깥 방향 판정(중심 대비 좌우·상하)+rot90 방향 단위검증(top→left/top→right assert 통과). v9 판: sym .35(8차 참조)·asym .40/.70/1.00·MIRROR -.70(방향 해석 오류 대비) + 확대 스트립. 전 행 구조 보존(0→6·1→2·2→5·8→7). 중립 비전 통과(참고 소견: 실촬과 asym .40~.70 유사). 사람 판정 대기: 실촬과 가장 가까운 행 + 방향(정방향 vs MIRROR).
- 2026-09-23T22:44-07:00 — 스파이크 10차 — 안쪽 직선 복원(2026-09-24). 사람 판정 v9: 'asym .40 이 제일 비슷한데 이번엔 안쪽에 직선이 없다 — 안쪽에도 직선이 있어야 한다.' 원인 코드 확인: v9 asym 이 안쪽 테이퍼를 taper×(1+asym)=0.49 로 늘려 중간 직선 구간(flat=0.5-zone)을 잡아먹음 — 사람 지적이 코드 그대로. 수정: _hexleaf_bar 의 안쪽 프로파일을 대칭(v8) 고정(side(1.0)), asym 은 바깥 테이퍼만 축소. v10 판: sym 참조 + outer .30/.40/.50/.70(안쪽 전부 v8 직선 유지). 검증: 전 행 구조 보존(0→6·1→2·2→5·8→7) + F 세로획 안쪽 가장자리 y-프로파일로 중간 직선 구간 실측 복원 확인(23 표본 중 중앙 8+ 표본 ±1px) + 중립 비전 통과(참고: 실촬과 outer .30~.40 유사 소견). 사람 판정 대기: outer 값 확정.
- 2026-09-23T22:47-07:00 — 스파이크 11차 — 끝단 각도 완화(2026-09-24). 사람 판정 v10: '매우 비슷해지고 있다. 안쪽·바깥쪽 각도를 조금만 더 완화하자 — 현재 너무 뾰족하다.' 구현: _hexleaf_bar(soft 노브 — 테이퍼 직선을 smoothstep 3s²-2s³ 과 혼합, 어깨·끝점 꺾임 동시 완화, 0=직선·1=완전곡선), 바깥값은 v9 최근접 .40 유지. v11 판: v10 ref(sharp)·soft .50·soft 1.00·taper .28 sharp·taper .28 soft .50(짧은 테이퍼 대안). 전 행 구조 보존(0→6·1→2·2→5·8→7)·중립 비전 통과. 사람 판정 대기: soft/taper 조합 확정.
- 2026-09-23T22:51-07:00 — 경로 전환 선언(사람, 2026-09-24): '미안하다.. 그렇게 간단한 게 아니었네. 이건 폰트 에디터로 폰트를 직접 수정하는 게 맞는 것 같다.' — 파라메트릭 시트 순환(v6~v11)에서 폰트 에디터 직접 수정 경로로 전환. v11 판정(soft/taper 조합)은 미판정으로 주차. 이때까지의 수렴 결과는 폰트 수정의 출발점 근거로 그대로 이전된다: 평행 옆면(중간 직선 유지) + 안팎 비대칭 끝 테이퍼(바깥 .40 — 바깥이 더 낮은 각도로 빨리 직선 복귀, 안쪽은 대칭 .35) + 끝점·어깨 완화(v11 soft) + 코너 갭 확보(trim .35t — DSEG 베벨 갭 유지 필수) + 두께는 DSEG Regular 베이스. 실무 사실 두 가지: (1) DSEG ttf 를 직접 수정하면 OFL/RFN 에 따라 이름 변경 의무+라이선스 문구 동봉 필요 — 09-11 '파일 무수정' 결정을 다시 여는 사람 승인 필요. (2) 대안: 수렴 파라미터로 새 TTF 를 프로그램 생성(fonttools·아웃라인은 자체 방정식=독자 폰트, DSEG 파생 아님 → RFN 문제 소멸) 후 에디터에서 손마무리 — 출발점이 '매우 비슷' 상태. 도구: FontForge(무료·GPL 툴이어도 산출물에 비전염)·FontLab(유료). 렌더러는 PIL ImageFont 소비이므로 _glyph_mask 가 새 파일을 보게만 하면 됨. 사람 선택 대기: ①DSEG 직접 수정(이름변경 각오) ②스타터 TTF 생성 후 에디터 마무리(권고) ③v11 판정으로 파라메트릭 복귀.
- 2026-09-23T23:01-07:00 — 스파이크 12차 — 직선 구간 길이 조정(2026-09-24). 사람 판정 v11: '니가 그린 것 중 11번째(v11 판)의 v10 에 해당하는 행' = v10 ref(sharp) 최근접 → soft(각도 완화)는 기각, 직선 테이퍼 유지. 지시 3건: ①안쪽 직선<바깥 직선 유지 ②바깥·안 직선의 차 축소 ③둘 다 현재보다 길게 — 구현: 테이퍼 구간 축소(taper↓·asym↓)로 직선 구간 연장, 라벨에 직선 길이 비(OUT/IN) 표기. v12 판: ref(OUT.29/IN.15)·OUT.32/IN.20·OUT.33/IN.22·OUT.30/IN.22·OUT.33/IN.25·OUT.36/IN.28. 전 행 구조 보존(0→6·1→2·2→5·8→7)·중립 비전 통과. 사람 판정 대기: 직선 비율 확정.
- 2026-09-23T23:07-07:00 — 스파이크 13차(2026-09-24). 사람 판정 v12: '가장 긴 직선(OUT.36 IN.28)이 제일 비슷 + 안쪽 직선과의 차이를 더 줄이고 사선 각도를 양쪽 다 더 줄일 것.' v13 판: ref(.36/.28)·OUT.38/IN.32(t.18 a.35)·OUT.37/IN.32(t.18 a.30)·OUT.40/IN.35(t.15 a.30)·OUT.42/IN.38(t.12 a.35) — 테이퍼 축소로 직선 연장·간극 .08→.04. 전 행 구조 보존·중립 비전 통과. 사람 판정 대기: 최종 직선 비율.
- 2026-09-23T23:17-07:00 — 스파이크 14차 — 가운데 가로획(G) 처리(2026-09-24). 사람 판정 v13: 'OUT.40(IN.35) 아주 근접. 가운데 글리프의 가로로 된 획이 위아래 비율이 동일한 각도를 가진다 — 이것만 맞추면 될 듯' + 용어 확인 요청('G=7세그 부품명, 글자 중앙의 단 하나의 가로획이 맞음'). 실측: 제 렌더의 G 는 위 테이퍼 9 표본 vs 아래 6(약한 비대칭, 방향은 중심좌표 부등호라 임의) — '동일해 보인다'는 관찰과 일치. mid 노브 추가(auto/sym/up/down — sym=완전 대칭, up/down=위·아래 무딘 쪽 지정 asym .45). v14 판: ref(auto)·MID sym·MID top-blunt·MID bot-blunt(t.15 a.30 유지). 전 행 구조 보존(2·3·5·6·9 의 G 에도 동일 적용 확인). 실촌 평균 8 의 G 밴드 관찰은 밴드 배치가 획을 걸쳐 결론 불가. 비전 도구(4_5v) CDN URL 400 오류로 이번 판은 픽셀 검증만. 사람 판정 대기: G 처리 변형 선택.
- 2026-09-23T23:21-07:00 — 스파이크 15차 — 각도 통일(2026-09-24). 사람 판정 v14: 'mid sym 이 맞다. 바깥쪽 획들의 안쪽 기울기가 mid sym 의 기울기와 일치해야 한다.' 원인 실측: 테이퍼 구간이 획 길이 비율(15%)이라 길이가 다른 획마다 각도가 어긋남 — G(138px) 기울기 0.68 vs F 세로획 안쪽 0.81(19% 어긋남). 구현: _hexleaf_bar(zone_t 노브 — 테이퍼 길이를 획두께 기준 z×t 로 지정, 전 획 동일 각도). v15 판: v14 ref(길이비)·UNI z=.60(급)·z=.74(=G 현재 각도)·z=.85(완만), 전부 mid sym·asym .30. 검증: 전 행 구조 보존 + z=.74 에서 G 0.68 vs F 0.67 각도 일치 확인. 사람 판정 대기: z 값 확정.
- 2026-09-23T23:27-07:00 — 스파이크 16차 — 대면 사선 평행 조건(2026-09-24). 사람 지시(턴 중 2건): '가운데 획과 마주하는 획들의 사선이 평행이 되었으면 한다.' 수학: 사선 기울기=½두께/(z·두께)=1/(2z) — 대면 사선(가운데 획 끝 대각선 vs 세로획 안쪽 대각선)이 평행이려면 z=0.50(양쪽 모두 45도). 렌더 실측으로 검증: G 위변 대각선 vs F 아래안 대각선 기울기 차 — z=.74→0.814, z=.60→0.390, z=.50→0.093(측정오차 감안 0 수렴, G 위변 실측 정확히 −1.000=45도). v16 판(z=.50~.70 미세 스윕, 전부 mid sym·a.30) 렌더 — z=.50 이 평행 버전. 사람 판정 대기: 평행 기준 적용 시 z=.50 확정 여부(직전 \'z=.60 매우매우 근접\' 판정과 조율).
- 2026-09-23T23:32-07:00 — 형상 스펙 확정(사람, 2026-09-24): 'z=.50 이다' — 파라메트릭 글리프 형상 수렴 완료. 최종 스펙: DSEG Classic Regular 배치·두께 + trim 0.35t(코너 갭) + asym 0.30(바깥쪽만 무딘 테이퍼, 안쪽은 그대로) + 가운데 가로획 완전 대칭(mid sym) + zone_t 0.50(테이퍼=두께 절반, 전 획 통일 45도 — 대면 사선 평행). 수렴 경로: v6 반원→v7 렌즈→v8 육각→v9 비대칭(.40)→v10 안직선 복원→v12 직선 연장→v13 OUT.40→v14 mid sym→v15 각도 통일→v16 z=.50(평행). 소비 처 실측(grep): synth_panel 이 _glyph_mask 로 숫자 글리프 소화, _pick_variant 는 7 변형(Light/Regular 가중2/Bold/Italic/LightItalic/BoldItalic/ModernLight 12%) — 통합 시 변형 대응이 관건(Italic 은 전단 변환 필요·ModernLight 는 둥근끝 소수용). 남은 사람 결정: ① 스타터 TTF 생성(폰트 에디터 손다듬용·독자 폰트·RFN 없음 — 7 변형 분량) vs ② 합성기 직접 반영(_glyph_mask 내부 교체·변형별 두께 자동·지금 스펙 그대로 — 권장: 형상이 이미 수렴해 손다듬 필요가 사라짐). 결정 후 카드 처방(AC#2 사후보고·AC#3 선언·AC#4 서명·볼록/형상 축 신규 AC→todo) 정리.
- 2026-09-23T23:39-07:00 — 합성기 반영 완료(2026-09-24, 사람 결정 '합성기 숫자 렌더러 교체하자' — ②직접 반영). 구현: synth_profiles.py — _glyph_mask 를 디스패처로 재구성, Classic 6변형(Light/Regular/Bold/Italic/LightItalic/BoldItalic)의 숫자 0-9 는 _param_glyph_mask(확정 스펙: 배치·두께는 DSEG 래스터에서 읽음·trim .35t·바깥 asym .30·가운데 가로획 대칭·zone_t .50=45도 대면 사선 평행), 비숫자·ModernLight 는 _dseg_raster(DSEG 그대로 — ModernLight 는 가는획+둥근끝 소수 기기라 형상 유지, 사람 확인 필요한 단독 결정). 이탤릭은 DSEG 에서 전단 측정(Italic .124·BoldItalic .139) 후 조립 마지막에 warpAffine 전단. 검증: ① 6변형×10숫자 연결요소 구조 일치(DSEG '7' 은 원래 4획 — 왼쪽 위 세로 포함, 렌더 동일 재현) ② 소비 경로 스모크(불 마스크→균일 x-압축) ③ 신구 대조 시트 _diag/glyph_synth_swap/synth_glyph_old_vs_new.png(행 오프셋 계산 오류로 1차 오탐—블록 구분선 미반영, 수정 후 전 통과) ④ 생성기 전체 스모크 synth_panel gen 24장 — 레이아웃 위반 0·숫자 잘림 0. 커밋 대기(사람 승인 후 synth_profiles.py+스파이크 스크립트 포함).
- 2026-09-24T20:45-07:00 — 커밋 완료+사람 결정(2026-09-24): '다양성을 위해 두 가지 seg 유지 + 커밋 승인 + 다음 작업 문의'. — ① 두 세그 혈통 유지 확정: Classic 계열=확정 파라메트릭(약 88%·가중치 Light/Reg×2/Bold·이탤릭), ModernLight=DSEG 둥근끝(약 12%). ② 커밋 58c12cc(synth_profiles 렌더러 교체+make_glyph_convex_sheet 스펙 근거 자+카드 기록·activity; _diag 는 gitignore 로 시트 제외, GM 카드 무관 더티라 제외). 남은 것: 카드 처방 확정(AC#2 사후 보고 한정·AC#3 잔상 사람 선언·AC#4 서명·'글리프 형상' 축 신규 AC → todo 복귀) — 사람 승인 대기 후 실행.
- 2026-09-24T20:50-07:00 — AC#3 잔상 사람 선언(peteroh, 2026-09-24): ① 일부 기기에서만 잔상이 보인다(전 기기 공통 아님) ② 보이는 경우 잔상은 배경색과 비슷한 흐릿한 색으로 칠해져 있거나, 아예 보이지 않는다 — 두 가지 상태 ③ 가시성은 조명·촬영각도의 영향을 받는 것으로 보인다(같은 기기도 장면에 따라 보이거나 안 보임). 해석: 잔상=기기 형질(On/Off)+기기별 기저 옅기, 가시성 변조=촬영 변인 — 2026-09-13 형질/촬영 분리 지침과 정합. 현재 코드(15% 확률·uniform 0.04~0.11 기기 고정)에서 번안안: 농도 상한을 낮춰 배경색 근접(예 0.02~0.06)·장면별 렌더 rng 로 가시성 변조(일부 장면에서 0) — 수치 확정은 사람 승인 대기. 선언으로 AC#3 의 '측정값 맞춤' 갭은 해소(선언·서명 방식).
- 2026-09-24T20:54-07:00 — AC#3 잔상 분포 선언·반영 완료(2026-09-24). 사람 추가 선언: '0.06 도 있다, 0.03 은 흔한 듯, 대부분은 0.00 이다.' 코드 반영(synth_profiles.device_identity): ghost 를 이산 3종 고정으로 교체 — 85% 기기 0.0, 보유 기기는 (0.03,0.03,0.03,0.06) 중 뽑기(0.03 가중 3배)·crc32 기기 고정 유지. 옛 uniform(0.04,0.11) 은 근거 없는 임의값이었음(주석에 선언 근거 명기). 검증: 전 프로파일 14기기 분포 {0.0:12, 0.03:1, 0.06:1}·결정성(같은 기기 같은 값) 확인·생성기 12장 스모크(위반 0). 커밋 대기 — 카드 처방 실행과 함께 일괄 승인 가능.
- 2026-09-24T21:08-07:00 — AC#3 잔상 분류 정정+장면별 이동(2026-09-24, 사람 정정: '기기별 잔상은 기기의 고유 특성이 아니다 — 조명·촬영 컨디션에 따라 같은 기기에서도 보이기도 안 보이기도 한다'). 구현 정정: ① synth_panel 의 ghost 를 장면별 rng 이산 뽑기로 교체(대부분 0·보이면 (0.03×3,0.06) — 이전 턴의 폴백 uniform(0.04,0.11) 잔존도 함께 정리, 소비처는 이 한 곳뿐) ② device_identity 의 ghost 키 폐지 — 난수 흐름 보존 함정: dict 앞 더미 소비는 위치 어긋나 전 기기 형질 변동(스냅샷 대비 전 기기 불일치로 검출) → 원래 소비 위치(dict 안 glyph_in_cell 다음)에 같은 분기의 자리 지킴이 키(_ghost_retired)로 교체 후 **전 기기 형질 불변 재검증 통과**. 검증: ① 장면별 분포 2000회 {0: 84.5%, 0.03: 11.9%, 0.06: 3.6%} ② 형질 불변 ③ 생성기 12장 스모크(위반 0). 옛 주석 '잔상은 기기 형질이다'(2026-09-12 근거)는 사람 선언으로 폐기 — 주석에 역사와 함께 정정 기록. 커밋 대기(잔상 변경·카드 처방 일괄 승인).
- 2026-09-24T21:24-07:00 — 카드 처방 확정(사람 승인 2026-09-24): 잔상 변경 커밋 승인·AC 재작성(위 5항)·잔상 보이는 비율 15% 확정·todo 복귀. 남은 실행은 AC#2 하나 — 3축 측정 자 작성·커밋·나란히 보고.
- 2026-09-24T21:25-07:00 — RESUMED: 사람 승인 2026-09-24 — 처방 확정으로 todo 복귀. 남은 AC#2(측정 자 작성·사후 보고) 실행.
- 2026-09-24T21:35-07:00 — AC#2 산출(2026-09-24 무인): 자 assets_dev/train/measure_glyph_axes.py + 보고서 docs/reports/glyph-axes-posthoc-2026-09-24.md — 3축(획굵기·간격·기울기) 실츬 n=160(사람 밴드 라벨 슬롯, 자릿수당 16 상한) 대 합성 전수(Light/Regular/Bold) 나란히. 결과 요약: 획굵기 실츬 중앙 0.098(p25 .088/p75 .100) — 합성 Regular .090·Bold .110 사이에 분포; 간격 양쪽 중앙 .005(실츬 p75 .035); 기울기('1' 한정) 실츬 중앙 0.14°(±3°)·합성 0°. 자 제작 중 버그 2건 발견·수리(19px 사각형 캘리브레이션으로 규약 확정): ① distanceTransform 경계 효과 — 꽉 찬 크롭에서 가장자리 획 반지름 2배(1px 0-패딩으로 수리) ② 입력 반전 오류(0-픽셀 거리 규약 착각 — 구멍 크기를 재었음). 측정값은 설계에 반영하지 않음(사후 보고 한정 — 보고서 상단 명시).

## Handoff
- 2026-09-17T00:07-07:00 — QUESTION: AC#1 은 끝냈다(대조 시트 두 판, _diag/glyph_compare/). 나머지 셋은 내가 정하면 안 되는 자리라 세워 둔다. AC#2·#3 이 저장소 규칙과 정면으로 부딪힌다. 이 카드는 2026-09-11 에 썼는데 antipatterns/no-real-photo-ruler-for-synth 가 2026-09-15 에 섰다. 그 규칙은 '합성의 설계값을 실촬 사진을 재서 정하지 않는다 — 합성이 기준을 주고 사람이 보고 값을 부른다' 이고, 근거로 실물 잉크가 밴드의 0.99 를 채운다고 재서 되먹였다가 521장이 잘린 사고를 든다. 그런데 AC#2 는 '획 굵기·세그먼트 간격·기울기의 실사진 분포를 측정해' 이고 AC#3 은 '잔상 농도가 실사진 측정값에 맞춰진다' 다. 둘 다 규칙이 금지하는 되먹임이다. 선택지: (가) AC#2·#3 을 규칙에 맞게 다시 쓴다 — '측정한다' 를 '시트를 보고 사람이 값을 선언한다' 로 바꾸고, 정한 사람과 날짜를 코드에 남긴다(규칙이 요구하는 바다). (나) 글리프는 예외로 두고 측정을 허용한다 — 그렇다면 왜 여백·자리와 다른지를 규칙 페이지에 적어야 한다. (다) 카드를 접고 규칙에 맞는 새 카드로 다시 세운다. 나는 (가) 를 추천한다. 시트가 이미 있어서 값을 부르는 데 필요한 것은 다 갖췄고, AC#3 의 '15% 는 근거 없이 정한 값' 이라는 문제 제기도 규칙 안에서 풀린다 — 규칙은 판단으로 정하는 것을 허용하되 '누가 언제 정했는지를 코드에 남기라' 고 한다. 지금 15% 에 없는 것은 근거가 아니라 서명이다. AC#4(DSEG 변형 선택)도 같은 이유로 사람 몫이다 — 시트를 보고 고르면 된다. 참고로 눈에 보이는 것: 합성이 실사진보다 더 기울어 보이고, 세그먼트 틈이 더 넓고 고르며, 실사진 쪽 획 끝이 더 뭉툭하다. 관찰이지 측정이 아니다.
- 2026-09-23T20:57-07:00 — QUESTION: 세션 붕괴로 넘기는 handoff(2026-09-23 심야). 상태: 이 카드는 REVIEW·판정 D(사람 전용)로 확정(당일 arbiter+ZCode 직접 재검증 일치 — 노트 참조). 이 세션에서 사람이 내린 선언과 판정 셋: (1) 볼록은 전 기기 공통 형질, 예외 GlucoDr. GOLD(사진 1장·합성 프로파일 14종에 없음) → 합성은 전 프로파일 공통 기본값 하나로 충분(카드 노트에 서명 근거 기록됨). (2) 스파이크 1차(자체 파라메트릭 글리프, make_glyph_convex_sheet 초판) — 사람 눈검 '실촬과 거리가 매우 멀다'로 기각, DSEG Classic Regular/Bold(현행)가 가장 가깝다고 확인 → AC#4 Classic 선택 부수 재확인. (3) 스파이크 3차(사람 아이디어 '세그먼트 반전 후 바깥으로 겹침 — 가로획 위로·세로획 좌우'를 연결요소 분리+미러+외향 오프셋 union으로 구현, glyph_convex_test_v3.png) — 사람 판정 '잘못 겹친 것 같다'. 원인 후보: '반전'을 긴 축 미러로 해석한 것 / 가로획 방향을 바깥 기준(A위·D아래·G양쪽)으로 구현한 것 / 겹침 폭 25%. 다음 세션 할 일 순서: ① 사람에게 '반전'의 정확한 뜻과 가로획 방향을 재진술로 확인(옵션 제시) → v3 재렌더 → 판정. ② 스파이크 2차(DSEG 베이스 + 외향 sin warp, glyph_convex_test_v2.png, .15/.30)는 사람 판정을 아직 받지 못했다 — 같은 판에서 함께 판정받을 것. ③ 볼록 구현 경로가 정리되면 카드 처방 확정 질문으로 돌아간다: 권고는 ① AC#2 사후 보고로 한정·AC#3 사람 선언·AC#4 서명 + '볼록 형상 축' 신규 AC 추가해 todo 로 되살림(이 세션에서 확정 못함). 자산: assets_dev/train/make_glyph_convex_sheet.py(미커밋·v3 행 구성, v1 파라메트릭 코드는 v2 재작성 때 교체됨), _diag/glyph_convex_test/{glyph_convex_test,_v2,_v3}.png, 카드 노트에 전 과정 기록. 미커밋: 카드 md·activity.jsonl·GM 카드(기존 더티)·make_glyph_convex_sheet.py(미추적). 참고: 웹툴 8777은 당일 최신판으로 재기동됨(백그라운드). 주의: DSEG 폰트 파일은 RFN 으로 수정 금지 — 볼록은 렌더 시간 후처리로만.

## Result
- 2026-09-24T21:35-07:00 — 글리프 형상 스펙을 사람 눈 판정 16차 스파이크로 확정하고 합성기 렌더러를 교체했다(parametric Classic 6변형·ModernLight DSEG 병존·이탤릭 전단·잔상 장면별 이산 — 커밋 58c12cc·4ef09f3). AC#1 대조 시트·AC#2 3축 사후 보고 자(measure_glyph_axes.py, 실츬 n=160 대 합성 전수 나란히·지표 버그 2건 캘리브레이션 수리)·AC#3 잔상 사람 선언 반영·AC#4 두 혈통 서명·AC#5 형상 축 — 전 AC 충족. 검증: 6변형×10숫자 구조 보존·전 기기 형질 불변(잔상 이동 시 스냅샷 대조)·생성기 스모크 3회(위반 0).
