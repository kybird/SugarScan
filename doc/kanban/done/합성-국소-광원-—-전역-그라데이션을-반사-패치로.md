---
title: 합성 국소 광원 — 전역 그라데이션을 반사 패치로
status: done
ordinal: 32000
created: 2026-09-11
depends_on: ["합성만으로 밴드 쿼드 검출기 v0","합성을 GM 크롭과 같은 물건으로 — 기기 몸체를 함께 렌더한다"]
---

## Goal
<!-- kanban:goal:begin -->
지금 글레어가 패널 전체를 덮는 큰 그라데이션이라 숫자가 통째로 잠긴다. 실물은 국소 반사다. 광원을 국소 패치로 바꾼다.
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [x] #1 반사가 패널 일부만 덮는다 — 숫자 영역 전체를 가리는 표본이 없다
- [x] #2 열화 상한 카드의 대비 기준을 이 변경 뒤에도 만족한다
- [x] #3 실사진에서 관찰되는 반사 유형(선형 줄무늬·타원 스페큘러·창문 반사)이 구분되어 들어간다 — 근거 id 를 적는다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-11T23:56-07:00 — 리더용 카드다(2026-09-12 구분). 검출기가 어떤 크롭을 내는지 정해진 뒤에 착수한다.
- 2026-09-12T01:02-07:00 — AC#2 정정(2026-09-12): '열화 상한 카드의 대비 기준'을 참조하는데 그 카드가 이 카드와 같은 단계(리더)에 있어 순서가 정해져 있지 않다. 어느 쪽을 먼저 하든 대비 기준(실사진 40 미만 0.0%)을 만족하면 된다.
- 2026-09-12T14:10-07:00 — 2026-09-12 선금 — 7bb19a9 에서 반사 패치 개수를 0~5 에서 0~2 로, 크기 상한(W·H 비율)을 낮췄다. 12장 montage 에서 글레어가 화면을 덮는 표본은 사라졌다. AC#1 의 절반은 이미 갚혀 있으니 처음부터 다시 만들지 마라 — 현재 코드를 읽고 AC#3(반사 유형 구분 + 근거 id)부터 시작한다. 반사 개수를 다시 올리려면 밀도 카드가 먼저다(밀도를 반사로 채우면 안 된다).
- 2026-09-12T14:10-07:00 — 2026-09-12 AC#2 기준선 — '열화 상한 카드의 대비 기준' 은 그 카드가 2026-09-12 에 정정됐다. 실사진 밴드 대비(measure_polarity.py, n=84)는 median 104 · p10 60 · min 21 · 40미만 1.2% 다. 옛 값(중앙 117.5 · 40미만 0.0%)을 쓰지 마라.
- 2026-09-17T00:04-07:00 — AC#3 근거 id — 실사진 화면 크롭을 눈으로 보고 골랐다. 고르는 자는 survey_real_glare.py 가 순위만 매기고 유형 판정은 안 한다. streak(가늘고 긴 띠): glucose_batch2/2515(왼쪽 가장자리 세로 띠) · glucose_batch1/2299(좌상단 대각 띠). specular(뭉툭한 타원): glucose_batch1/694(상단 중앙 얼룩) · glucose_batch1/822(상단에서 번진 얼룩). window(윤곽 있는 사각 반사): glucose_batch2/2623(좌하단 구조 있는 반사) · glucose_batch1/2338(어두운 화면에 비친 실루엣). 세 유형의 비율(0.45/0.30/0.25)과 크기 범위는 사람이 정한 값이다 — 실사진에 자를 대지 않는다.
- 2026-09-17T00:04-07:00 — 고르는 자를 한 번 갈아엎었다. 1판은 '화면 안 포화 화소 비율' 이었는데 상위 30장이 거의 전부 역광 LCD 였다 — 숫자 획 자체가 포화에 가까우니 비율이 올라간다. 반사는 안 걸리고 밝은 화면만 걸렸다. 획과 반사를 가르는 것은 밝기가 아니라 두께라서, 밝은 영역의 최대 내접 반지름(거리변환 최댓값)을 화면 높이로 나눈 값으로 바꿨다. 그러자 상위권이 실제 반사로 채워졌다.
- 2026-09-17T00:04-07:00 — AC#1 은 눈이 아니라 숫자로 판정했다. 매니페스트에 glare_cover(반사가 알파 0.15 이상으로 덮은 숫자 필드 비율)를 남기고 validate_synth_panel 이 전량에서 센다. 12장 몽타주를 보고 '안 덮는다' 고 말하지 않으려고 만든 자다.
- 2026-09-17T00:04-07:00 — 곁가지 주의: gpc(글리프 평면) 0.9 미만 비율이 변경 뒤 세 시드에서 7.7 / 10.0 / 10.0%(각 n=300)이고 변경 전에는 한 시드에서 6.0%(n=300, seed 777001)였다. 증가를 단정하지 않는다 — 변경 전 표본이 하나뿐이고, 더 중요하게는 반사 코드가 rng 소비 개수를 바꿔서 같은 시드라도 같은 장이 나오지 않는다. 즉 '같은 시드' 가 짝지은 비교를 보장하지 않는다. 방향(반사를 넣으면 글리프 평면 대비가 내려간다)은 기대되는 바이고, 크기를 재려면 짝지은 설계가 따로 필요하다.
- 2026-09-17T00:04-07:00 — 건드리지 않은 것: validate_synth_panel 의 하드 검사 sizes 가 63(n=300) · 135(n=600) 로 남아 있다. 이건 변경 전에도 같은 값이었고(63, n=300 seed 777001) 다른 카드(글자 크기 종수 AC)의 몫이다. 그래서 검증기는 여전히 '하드 실패 1건 이상' 을 찍는다 — 이 카드가 만든 것이 아니고 이 카드가 없앨 것도 아니다.
- 2026-09-17T00:04-07:00 — 합성기가 바뀌었으므로 마일스톤 세트 A/B/C(synth_coco, 시드 20260916~18)를 지금 다시 구우면 다른 그림이 나온다. 이미 구운 것은 스냅숏으로 그대로 두고, 다시 구울 일이 생기면 그때 세트 전체를 같이 굽는다 — 반만 다시 구우면 검출기와 리더가 다른 세상을 본다.

## Handoff

## Result
- 2026-09-17T00:04-07:00 — synth_panel.py 의 반사를 한 종류(가장자리 타원)에서 세 종류로 갈랐다 — _reflection_mask(streak/specular/window), REFLECT_KINDS·REFLECT_WEIGHTS(0.45/0.30/0.25)·GLARE_COVER_ALPHA 가 상수로 한 곳에 있다. survey_real_glare.py 신설(실사진 반사 후보 고르기), validate_synth_panel.py 에 반사 유형·가림 집계 추가. AC#1 숫자 필드를 통째로 가리는 표본 0 — 명령: python validate_synth_panel.py --count 600 --seed 777002 -> '숫자 필드 가림 median=0.0000 p90=0.0421 p99=0.1737 max=0.2318 · 가림 있는 장 122 · 50% 초과 0 · 90% 초과 0'(모집단 합성 n=600). 최대가 23.2% 라 어떤 장도 숫자 영역의 절반조차 못 가린다. AC#2 대비 기준 유지 — 같은 명령의 '밴드 대비 p95-p5 median=91 p10=59 | <40 비율 합성 1.5% vs 실측 1.1%', n=300 seed 777001 에서는 1.3%. 변경 전 같은 설정이 1.3% 였으므로 이 변경이 대비 기준을 움직이지 않았다(실측 기준선은 real_baseline.json, 카드 Note 의 n=84 값 40미만 1.2% 와 같은 자리). AC#3 세 유형이 구분되어 들어갔고 근거 id 를 적었다 — streak: glucose_batch2/2515 · glucose_batch1/2299 / specular: glucose_batch1/694 · glucose_batch1/822 / window: glucose_batch2/2623 · glucose_batch1/2338. 마스크 모양은 _diag/real_glare/masks.png 에서 눈으로 확인했다(띠·얼룩·창살 있는 사각이 서로 다르게 나온다). 유형 분포 실측: 패치 513개 / 600장(specular 246 · streak 139 · window 128). 자를 한 번 갈아엎었다 — 후보 고르기 1판이 '포화 화소 비율' 이라 역광 LCD 만 걸렸고, '밝은 영역 최대 내접 반지름' 으로 바꾸니 실제 반사가 올라왔다. 건드리지 않은 것: 하드 검사 sizes 63(n=300)·135(n=600)은 변경 전과 같은 값이고 다른 카드(글자 크기 종수) 몫이라 검증기는 여전히 '하드 실패 1건 이상' 을 찍는다 — 이 카드가 만든 것도 없앨 것도 아니다. 단정하지 않은 것: gpc 0.9 미만이 변경 뒤 7.7/10.0/10.0%(세 시드, 각 n=300) vs 변경 전 6.0%(한 시드) 인데, 반사 코드가 rng 소비 개수를 바꿔 같은 시드라도 같은 장이 안 나오므로 짝지은 비교가 아니다. 방향은 기대되는 바이나 크기는 재지 않았다.
