# 2026-09-16

## Case 1: 한 상수가 그림과 라벨을 동시에 정해, 라벨을 넓히면 숫자가 작아졌다

### Grounding
- Git Context: `hash:998e474` (작업 트리)
- Evidence: `assets_dev/train/lcd_layout.py` BAND_MARGIN 주석 · `test_lcd_layout.py`
- Confidence: 5/5

### Error
사람 지시는 "정답 사각형을 키워라"였다. `BAND_MARGIN` 을 0.10 → 0.20 으로
올렸더니 게이트가 4.4% → 34.7% 로 올랐다. 원인을 재 보니 **밴드는 한 픽셀도
안 커졌고 숫자가 작아져 있었다.** 프로세스를 분리해 측정(프로파일 6종 × 6장,
유리 대비 중앙):

```
   m      밴드/유리   숫자/유리   밴드/숫자
 0.10      0.515      0.429      1.200
 0.20      0.514      0.367      1.400
 0.30      0.514      0.321      1.600
```

밴드는 0.514 로 붙박이다. 오른 점수는 "상자를 키워서"가 아니라 "숫자를 14%
줄여 상자 안 여백을 늘려서" 산 것이고, 리더가 받을 해상도를 그만큼 깎았다.

### Environment Context
- Packages: sugartrain(py3.10, opencv, torch)
- Migration Issue: No

### Fix Code
```python
BAND_MARGIN = 0.10    # 라벨 — band_quad 가 정답 쿼드를 그릴 때만
LAYOUT_MARGIN = 0.10  # 그림 — slot_field 가 숫자를 물릴 때. 고정.

# slot_field
m = LAYOUT_MARGIN if margin is None else margin
# band_top/band_bot(배치 기준선)도 그림 쪽이다
pad_x = max(2, int(round(LAYOUT_MARGIN * dh)))
```

분리 뒤 같은 측정:

```
BAND 0.10 → 0.30 :  밴드/유리 0.515 → 0.666,  숫자/유리 0.429 → 0.416
```

### Analysis
- Root cause: `band_quad` 는 "밴드 = 필드 + m·dh" 로 쓰고, `slot_field` 는
  "밴드가 영역을 꽉 채우도록" 숫자를 역산한다. 같은 m 이 두 식에 들어가
  정확히 상쇄한다.
- Why it failed: 한 상수가 **의도**(정답을 얼마나 넉넉히 적을까)와
  **사실**(기기가 어떻게 생겼나)을 겸했다. 둘은 독립적으로 정해져야 한다.
- How it was fixed: 역할별로 상수를 갈랐다. 계약 테스트도 바꿨다 — 정답 상자가
  mid 영역을 넘는 것은 이제 허용이고(밴드는 배치 요소가 아니라 크롭 영역),
  유리를 넘지 않는 것만 지킨다.

### Related Knowledge
- Concepts: [[canonical-value]]
- **Anti-Patterns**: [[coupled-budget-loop-defeats-per-element-tuning]]

## Case 2: 캔버스를 넘으면 포즈를 줄이던 루프가, 라벨 크기에 촬영 각도를 종속시켰다

### Grounding
- Git Context: `hash:998e474` (작업 트리)
- Evidence: `assets_dev/train/synth_panel.py` 배율 루프 주석
- Confidence: 5/5

### Error
Case 1 을 고친 뒤에도 같은 시드 200장 중 **68장의 그림이 달랐다.** 요소는
하나도 안 움직였고(rects 동일) 난수 소비도 같았는데(78/81/75 로 양쪽 동일)
픽셀의 91%가 달랐다. 두 장을 나란히 보니 내용은 같고 **카메라 자세만** 달랐다.

구판:

```python
for shrink in (1.0, 0.5, 0.25, 0.0):
    Mk, Mr = _mats(shrink)
    quad = _warp_pts(quad0, Mk, Mr)
    if _in(quad):          # 라벨 쿼드가 캔버스 안인가
        img = _warp_img(pre, cv2.INTER_LINEAR, Mk, Mr)
        break
```

라벨을 키우면 쿼드가 더 쉽게 캔버스를 넘어 포즈가 깎인다. 실측(3k, 같은 시드):

```
BAND_MARGIN   |기울기| 중앙   p90     기울기 0 인 장
   0.10          3.84°      9.34°     39/300 (13.0%)
   0.22          2.15°      5.85°     51/300 (17.0%)
   0.30          1.72°      4.38°     68/300 (22.7%)
```

0.10 에서도 13%가 이미 반듯해져 있었다 — 전날 `cam_roll` 을 ±1.5 → ±15 로
넓힌 효과가 그만큼 무효화되고 있었다.

### Environment Context
- Packages: sugartrain
- Migration Issue: No

### Fix Code
```python
# 배율은 **유리**로 정한다 — 그림 쪽 값이다.
_glass0 = np.float32([[px0, py0], [px1, py0], [px1, py1], [px0, py1]])
for zoom in (1.0, 0.92, 0.84, 0.76, 0.68, 0.60):
    Mk, Mr = _mats(1.0, zoom)
    if _in(_warp_pts(_glass0, Mk, Mr)):
        img = _warp_img(pre, cv2.INTER_LINEAR, Mk, Mr)
        break
quad = _warp_pts(quad0, Mk, Mr)
# 라벨이 프레임을 넘으면 여백만 잘린다(유리가 안이고 숫자는 유리 안이다)
quad = np.stack([np.clip(quad[:, 0], 0, W - 1),
                 np.clip(quad[:, 1], 0, H - 1)], 1).astype(np.float32)
```

고친 뒤: 기울기 분포가 세 마진에서 동일(중앙 3.84 · p90 9.99 · 최대 15.33),
같은 시드 그림 **200/200 동일**, 쿼드 높이비 1.2000(이론값).

### Analysis
- Root cause: "프레임을 넘는다"의 판정을 **라벨**로 했고, 못 맞췄을 때의 처리로
  **촬영 변인**을 건드렸다. 판정 대상과 조정 대상이 다른 세계에 있었다.
- Why it failed: 포즈는 "어떻게 찍혔나"이고 라벨은 "무엇을 정답으로 적을까"다.
  후자가 전자를 바꾸면 A/B 에서 통제 안 된 변인이 같이 움직인다. 실촬에는
  뒤로 물러나 찍는 구도는 있어도 **각도를 펴서 찍는 구도는 없다.**
- How it was fixed: 판정을 유리(그림)로 옮기고, 조정을 배율(그림)로 바꿨다.
  라벨이 넘으면 여백만 자른다 — band_quad 의 clip 과 같은 규약이다.

### Related Knowledge
- Concepts: [[coordinate-frame]] · [[late-learned-low-amplitude-signal]]
- Patterns: [[split-rng-per-independent-axis]]
- **Anti-Patterns**: [[uncontrolled-budget-in-ab-comparison]]

## Case 3: 기본 인자가 모듈 적재 시 굳어, 없는 현상을 측정해 보고했다

### Grounding
- Git Context: `hash:998e474` (작업 트리)
- Evidence: `lcd_layout.band_quad` 시그니처
- Confidence: 5/5

### Error
"마진을 올리면 밴드가 12% 작아진다"고 보고했다. 그런 현상은 없었다.

```python
def band_quad(field, digit_h, clip=None, margin=BAND_MARGIN):   # 구판
```

기본값은 **모듈을 읽을 때 한 번** 평가된다. 실험 스크립트가
`lcd_layout.BAND_MARGIN` 만 바꾸고 모듈을 다시 읽지 않아, `slot_field` 는 새 값
(숫자가 줄어듦)을, `band_quad` 는 옛 값(밴드 그대로)을 쓰는 **어느 코드 경로에도
없는 상태**를 잰 것이다. 실제 굽기는 파일을 고쳐 새로 import 하므로 무사했다.

### Environment Context
- Packages: sugartrain
- Migration Issue: No

### Fix Code
```python
def band_quad(field, digit_h, clip=None, margin=None):
    m = (BAND_MARGIN if margin is None else margin) * digit_h
```

### Analysis
- Root cause: 파이썬 기본 인자의 조기 평가. 자가 잰 것은 제품이 아니라 자 자신의
  결함이었다.
- Why it failed: **자를 새로 만들고 검증하지 않았다.** 알려진 값이 나오는지
  기준선을 넣어 봤어야 했다 — 뒤에 부풀림 곡선을 낼 때는 0.20 줄이 실제 게이트
  94장(34.7%)과 일치하는지 먼저 확인했고, 그래서 그 표는 믿을 수 있었다.
- How it was fixed: 기본값을 호출 시점에 푼다. 측정은 마진별로 **프로세스를
  분리해** 다시 했다.

### Related Knowledge
- Patterns: [[measure-the-premise-not-just-the-claim]] · [[verify-premises-before-executing]]
- **Anti-Patterns**: [[metric-path-not-under-test]]

## Case 4: 게이트를 IoU 에서 포함률로 바꿨다

### Grounding
- Git Context: `hash:998e474` (작업 트리)
- Evidence: `assets_dev/train/eval_band_detector.py` CONTAIN_PASS 머리말
- Confidence: 5/5

### Design Decision
밴드 검출기 게이트의 1순위를 **포함률**(사람 밴드 라벨 중 예측 안에 든 넓이
비율)로 바꾸고, 2순위를 넓이비(예측/라벨), 진단을 변별 삐져나옴으로 두었다.
IoU 는 옛 판과 잇대어 보는 참고값으로 남기되 **합격을 정하지 않는다.**

사람 규칙: "게이트는 숫자를 전부 포함 여부이다. 넉넉하게 포함하든 타이트하게
포함하든 잘리지는 말아야지."

### Alternatives Considered
- Option A: IoU 유지 — 기각. 사람 라벨은 축정렬 직사각형이고 예측은 기울어진
  쿼드라 완벽히 맞춰도 1 이 안 된다. 천장이
  `r/((cos t + r sin t)(r cos t + sin t))` 이고 4° 0.864 · 8° 0.762 ·
  10° 0.715 다. 합성 기울기를 넓히면 검출이 좋아져도 내려간다 — 목표와 반대로
  움직이는 대리 지표.
- Option B: 포함률 단독 — 기각. 화면 전체를 찍으면 만점이다. 실제로 GM 박스
  폴백이 포함률 중앙 1.0000 · 넓이비 중앙 2.5배였다.
- Selected: 포함률(합격) + 넓이비(줄 세우기) + 변별 삐져나옴(진단).

### Decision Code
```python
CONTAIN_PASS = 0.999   # 부동소수 오차만 봐준다. 한 획이라도 밖이면 불합격.

def contain_ratio(pred, label):   # 교집합 넓이 / 라벨 넓이
def area_ratio(pred, label):      # 예측 넓이 / 라벨 넓이
def edge_shortfall(pred, label):  # 라벨 네 변이 예측 밖으로 나간 최대 깊이
```

### Analysis
- Context: IoU 0.838 로 "좋다"고 읽히던 모델(`band_det_r2_40k`)을 포함으로 재니
  **17/271 (6.3%)** 이었다. 네 변이 고르게 잘리고 있었다 — 사람이 처음 말한
  증상("좌우 위아래 상관없이 자른다")이 그대로 숫자로 나왔다.
- **Trade-offs**: 이 자는 엄격한 쪽으로 틀렸다. 사람 라벨은 숫자보다 크므로
  라벨을 담으면 숫자는 반드시 담긴다. 실촬에 숫자 다각형 정답이 없으니 라벨이
  가진 전부다. 느슨하게 하려면 숫자 범위를 지어내야 하고 그건 자가 아니다.
- Reversibility: High. 장별 값(contain/area/edges/iou)을 전부 jsonl 에 남긴다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[proxy-metric-moves-against-the-goal]]

## Case 5: 모집단이 다른 두 수치를 나란히 놓고 순위를 매겼다

### Grounding
- Git Context: `hash:998e474`
- Evidence: `assets_dev/train/device_labels.jsonl` · `_diag/*/gate_results.jsonl`
- Confidence: 5/5

### Error
"SD CodeFree 실촬 60장 — 지금까지 본 기기 중 가장 많습니다(CareSens N 19장,
UltraMini 28장)". 틀렸다. CareSens N 은 **240장**이다.

60 은 `device_labels.jsonl` 전체 2,494장에서 센 값이고, 19 는 **게이트 조인
표본 271장** 안에서 센 값이다. 열 배 차이 나는 두 모집단을 나란히 놓았다.
전체에서 다시 세면:

```
CareSens N 240 · SD CHECK GOLD2 189 · ACCU-CHEK Performa 180 ·
GluNEO plus 173 · 오토-첵 156 · … · SD CodeFree 60      (n=2494, 50종)
```

"프로파일 없는 기기 중 표본이 가장 크다"도 틀렸다 — SD CHECK GOLD2 189,
오토-첵 156, HANDOK BAROZEN II 123 이 전부 프로파일 없이 더 크다.

### Environment Context
- Packages: sugartrain
- Migration Issue: No

### Fix Code
없다. 보고 습관의 문제다.

### Analysis
- Root cause: 수치를 낼 때 **어느 파일·어느 모집단·n** 을 적지 않았다.
- Why it failed: 같은 하루에 같은 모양이 넷이었다 — (a) 이 건, (b) 측정하지
  않은 인과를 원인으로 단정(빈 모서리 때문 → 재니 상관 −0.006, 진짜는 넓이비
  +0.885), (c) 고장난 자로 잰 값을 발견으로 보고(Case 3), (d) 13분 구간의 굽기
  속도를 전체로 외삽해 완료 시각을 40분 틀리게 말함. 전부 "수치가 나왔다 →
  사실이다"로 건너뛴 자리다.
- 사람 지적: "난 그저 숫자주의에 잡음을 무시하는 걸 가리키고 싶을 뿐이야."
- How it was fixed: 수치에 모집단과 n 을 함께 적는다. 두 수치를 비교하기 전에
  같은 모집단인지 먼저 확인한다. 상관을 인과로 말하기 전에 재고, 못 쟀으면
  "재지 않았다"고 말한다. n 이 작으면 순위가 아니라 "구별 안 된다"가 정답일 수
  있다.

### Related Knowledge
- Concepts: [[metric-provenance]] · [[experiment-budget-parity]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[stale-baseline-quoted-as-current]]

## Case 6: [폐기] 밴드 쿼드 검출기 재학습 2차 — 재조준 합성으로 짝비교

### Grounding
- Evidence: doc\kanban\abandoned\밴드-쿼드-검출기-재학습-2차-—-재조준-합성으로-짝비교.md
- Confidence: 4/5

### Discovery
이 길은 아니었다 — 카드가 폐기됐다. 폐기 사유(안티패턴 원재료):

방향이 폐기됐다(사람 결정 2026-09-16). 이 카드는 기울어진 쿼드를 내는 밴드 검출기를 전제하는데, 검출기 출력을 축정렬 사각형으로 바꾸기로 했다. 근거 셋: (1) 실촬 라벨이 전부 축정렬이라 기울기 정답이 없다 — 사람 밴드 라벨 277장·화면 쿼드 410장 100% 직사각형이라 기울기를 채점할 방법이 없다. (2) IoU 는 기울어진 예측을 축정렬 라벨과 견주는 자라 천장이 있고, 합성 기울기를 넓히면 검출이 좋아져도 내려간다(8도 0.762). (3) 8좌표 자유 회귀는 카메라가 낼 수 없는 사각형을 내고, 사람이 화면에서 그 실패를 봤다 — 모서리 한 점만 끌려간 사다리꼴. 대신 마일스톤 '합성만으로 사진에서 값까지 한 번 통과시킨다' 가 축정렬 상자로 같은 질문에 답한다. 합성의 기울기 정답은 버리지 않는다 — 매니페스트에 남아 있으므로 필요하면 다시 꺼낸다.

### Analysis
- Why non-obvious: 다음 세션이 같은 벽에 다시 부딪히지 않게 하는 기록이다.
- Action taken: 주기 compile에서 안티패턴 승격 후보로 검토할 것.

### Related Knowledge
- **Anti-Patterns**: [[밴드-쿼드-검출기-재학습-2차-—-재조준-합성으로-짝비교]]

## Case 7: 2026-09-10 의 numpy 되돌리기가 그 이전 체크포인트를 전부 잠갔다

### Grounding
- Evidence: `assets_dev/train/migrate_ckpt_numpy1.py` · `yolox_out/gmscreen/best_ckpt.pth`(2026-08-28 07:48)
- Confidence: 5/5

### Error
gmscreen 체크포인트에서 파인튜닝을 시작하려고 YOLOX 를 불렀더니 학습이
시작되기도 전에 죽었다:

```text
ModuleNotFoundError: No module named 'numpy._core'
```

`yolox/core/trainer.py:340` 의 `torch.load(ckpt_file, ...)["model"]` 자리다.

### Environment Context
- Packages: sugartrain (numpy 1.23.5 · torch 2.5.1+cu121)
- Migration Issue: Yes

### Fix Code
읽기 전에 옛 모듈 경로를 새 경로로 잇고, 한 번 읽어 지금 numpy 로 다시 절인다.
원본은 건드리지 않는다.

```python
sys.modules["numpy._core"] = sys.modules["numpy.core"]
ckpt = torch.load(src, map_location="cpu")
torch.save(ckpt, dst)          # 지금 numpy 로 다시 절인다
```

`best_ap`·`curr_ap` 가 numpy 스칼라라 `.item()` 으로 파이썬 값까지 내렸다.

### Analysis
- Root cause: 모델이 아니라 **환경**이다. numpy 2.x 는 내부 모듈을
  `numpy.core` 에서 `numpy._core` 로 옮겼고 pickle 은 그 모듈 경로를
  **문자열로** 저장한다. 2026-09-10 에 TF 2.10 을 살리려고 numpy 를
  2.2.6 → 1.23.5 로 되돌리면서, 그 이전에 구운 체크포인트가 전부 안 읽히게 됐다.
- Why non-obvious: 복구 작업은 그날 **임포트와 미니 학습 루프로 검증**했고
  둘 다 통과했다. 검증이 닿지 않은 것은 **그 환경이 과거에 만들어 둔 산출물**
  이다. 되돌리기의 피해는 앞으로 돌릴 코드가 아니라 뒤에 쌓인 파일에 났다.
- Impact: 이 환경에서 2026-09-10 이전 체크포인트를 부르는 모든 스크립트가
  같은 자리에서 죽는다. 가중치는 멀쩡하다 — 경로 이름만 옛것이다.
- How it was fixed: `migrate_ckpt_numpy1.py`. 별칭을 건 채 저장하지 않고,
  저장 뒤 **별칭을 지우고 다시 읽어** 성공을 확인한다 — 별칭이 살아 있는
  프로세스에서만 읽히는 파일을 "고쳤다"고 말하지 않으려고.

### Related Knowledge
- **Anti-Patterns**: [[unpinned-pip-in-frozen-training-env]]

## Case 8: 추론 전처리가 학습과 갈려 있었다 — 그것도 두 축에서

### Grounding
- Evidence: `assets_dev/train/detect_datumo_gm.py` 머리말(수정 전) ·
  `D:/tmp/YOLOX/yolox/data/data_augment.py:142-155` ·
  `D:/tmp/YOLOX/yolox/data/datasets/coco.py:148` ·
  `docs/reports/gm-preproc-letterbox-ab-2026-09-16.md`
- Confidence: 5/5

### Discovery
카드는 기하 하나(스트레치 vs 레터박스)를 지목했는데, 코드를 읽으니 **채널
순서도** 갈려 있었다. 학습 로더는 `cv2.imread` 라 BGR 이고 YOLOX `preproc` 는
채널을 바꾸지 않는데, 추론은 `img[..., ::-1]` 로 RGB 를 넣고 있었다.

파일 머리말에 적혀 있던 "YOLOX evaluator와 동일 전처리: 스트레치+BGR" 은 두
군데 다 사실이 아니었다 — 기하는 레터박스여야 했고, 채널은 실제로 RGB 였다.
**주석이 코드보다 오래 살아남아 틀린 채로 근거 노릇을 하고 있었다.**

### Environment Context
- Packages: sugartrain · YOLOX(D:/tmp/YOLOX)
- Migration Issue: No

### Fix Code
한 번에 둘을 바꾸면 어느 쪽이 움직였는지 알 수 없어 축을 갈라 세 팔로 쟀다.

```python
#   stretch    기존 경로 그대로 — 스트레치 + RGB
#   letterbox  기하만 맞춘다 — 레터박스 + RGB
#   yolox      둘 다 맞춘다 — 레터박스 + BGR
```

전처리와 **역변환 배율을 같은 함수가 함께 내도록** 묶었다. 따로 두면 한쪽만
고쳐도 검출은 성공한 것처럼 보이고 박스만 조용히 어긋난다.

### Analysis
- Root cause: 추론 스크립트가 학습 파이프라인의 전처리를 **호출하지 않고 다시
  구현**했다. 그 순간부터 두 정의가 따로 늙는다.
- How it was fixed: `--selftest` 이 `yolox` 모드를 YOLOX 정본 `preproc` 와
  픽셀까지 대조한다. "학습과 맞췄다"를 읽어서가 아니라 대조해서 확인한다.
- Impact: 사람 화면 라벨 410장 대조에서 IoU 중앙 0.828 → 0.876, ≥0.75 76.3%
  → 89.1%, 전량 검출 2497 → 2507. 동기였던 가로 층은 ≥0.75 15.4% → 52.4%.
  세로 층도 82.9% → 93.4% 로 같이 올랐다 — 스트레치 왜곡은 가로에만 있는 게
  아니라 방향만 뒤집혀 양쪽에 다 있었다.
- 곁가지: 「ft3 가 놓친 5장 회귀 진단」 카드의 5장이 전처리만 고쳐도 전부
  살아난다. 그 카드가 찾던 것은 모델의 회귀가 아니라 추론 전처리였다.

### Related Knowledge
- **Anti-Patterns**: [[duplicated-geometry-implementation]] ·
  [[stale-baseline-quoted-as-current]]

## Case 9: 문턱 세기가 연속량의 이동을 통째로 삼켰다

### Grounding
- Evidence: `assets_dev/train/diag_wide_gm.py`(w/h < 1.2 를 "접힘"으로 센다) ·
  `docs/reports/gm-preproc-letterbox-ab-2026-09-16.md`
- Confidence: 5/5

### Discovery
전처리를 고친 뒤 기존 자(`diag_wide_gm.py`)로 재니 **GM 실패 18/49 → 17/49**
였다. 한 장. 개수만 보면 "효과 없음"이고 그대로 닫을 뻔했다.

같은 장의 w/h 자체를 재니 중앙값이 **1.98 → 2.30** 으로 움직였다. 접힘으로
남은 장들이 전부 문턱 쪽으로 올라와 있었다:

```text
0.72 -> 1.17   0.71 -> 1.06   0.80 -> 1.10
0.68 -> 0.91   0.92 -> 1.15   0.71 -> 1.06
```

**1.2 를 못 넘었을 뿐 다 좋아졌다.** 문턱이 1.2 라서 "거의 다 왔다"와
"전혀 아니다"가 같은 한 칸으로 셌다.

### Analysis
- Root cause: 자가 **이진 판정**이라 연속량의 이동을 볼 수 없다. 문턱 근처에
  분포가 몰려 있으면 개수는 개선에 거의 반응하지 않는다.
- Why non-obvious: 그 자는 틀린 자가 아니다. "쓸 만한 가로 박스인가"를 세는
  데는 맞고, 실제로 그 질문의 답은 별로 안 바뀌었다. 틀린 것은 **그 자 하나로
  실험의 성패를 판정하려 한 것**이다.
- How it was fixed: `gm_preproc_ab.py --wide-detail` 이 개수와 분위수를 **같이**
  찍는다. 둘 중 하나만 찍으면 다음 사람이 같은 자리에서 같은 오판을 한다.
- 일반화: 게이트 지표가 문턱 세기면 그 옆에 연속량의 분위수를 반드시 같이
  둔다. 문턱은 판정용이고 분포는 방향 판단용이라 역할이 다르다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[proxy-metric-moves-against-the-goal]] ·
  [[aggregate-hides-stratified-failure]]
