# 2026-09-13

## Case 1: 실사진 기준선이 기기 편향 표본에서 나왔다

### Grounding
- Git Context: `hash:f23f153`
- Evidence: `docs/GLM_TASKS.md` §3.8 기준선 표, `assets_dev/train/real_baseline.json`
- Confidence: 5/5

### Discovery
합성기 조준의 기준이던 실사진 기준선이 밴드 라벨 84장에서 나왔는데, 그 84장이
기기 편향 표본이었다. 밴드 라벨 76장 중 63장이 세 기기(OneTouch UltraMini 30 ·
CareSens N 19 · 이름모를 가로형 14)였고 54종 중 39종이 0장이었다.

사람이 기기 균등 씨앗 큐로 라벨링해 54종 전량이 채워진 뒤(277행, n=264) 다시
재니 전 축이 움직였다.

```
            옛 n=84    새 n=264
  링         2.84%      1.90%
  안쪽       1.38%      1.11%
  전체 밀도  2.52%      1.77%
  극성 반전  27.4%      33.7%
  대비       104        99
  세로 band_w 0.827     0.888
  세로 cy    0.391      0.407
```

### Analysis
- Why non-obvious: 편향은 수치 자체에 나타나지 않는다. 84장이라는 n 은 적혀
  있었지만 "그 84장이 세 기기" 라는 사실은 아무 데도 없었다.
- 밴드 폭 교란(새 라벨이 더 넓다, 세로형 0.827 에서 0.901)이 원인일 가능성을
  고정 제외 박스로 배제했다. 그래도 격차가 남아 기기 구성 탓임이 확인됐다
  (세로형 링 옛 2.63% 새 1.49%).
- Impact: 이 기준선에 맞춰진 1차 작업이 통째로 과녁을 벗어났다. 근거 주석이
  달린 상수(`PITCH_RATIO`)까지 그 목표에 맞추느라 오염됐다.
- Action taken: 기준선을 `real_baseline.json` 정본 하나로 모으고 세 소비처가
  읽게 했다. 옛 n=84 값 전부를 폐기 목록에 올렸다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[stale-baseline-quoted-as-current]] ·
  [[aggregate-hides-stratified-failure]]

## Case 2: 같은 모델의 성적이 평가셋을 바꾸자 올라갔다

### Grounding
- Git Context: `hash:c28b8c1`
- Artifact: `docs/reports/band-det-v0-regate-54devices.md`
- Confidence: 5/5

### Hypothesis
검출기 v0 의 게이트 IoU 0.669 가 낮은 것이 모델 탓인가.

### Results
같은 체크포인트를 학습 없이 새 평가셋에 돌렸다.

```
python eval_band_detector.py gate --ckpt band_det_v0.pt
  옛: 84장(사실상 3기기)  det IoU median 0.669
  새: 264장(54종)         det IoU median 0.717 p10 0.547
```

### Analysis
- Confirmed/Refuted: 모델 탓이 아니다. 모델은 같다. 옛 84장이 최하위 두
  기기(UltraMini 0.548 n=30, 이름모를 가로형 0.690 n=14)에 몰려 성적을
  끌어내리고 있었다.
- Implications: 편향 표본은 수치를 올리기만 하는 것이 아니라 내리기도 한다.
  어느 방향인지는 표본 구성을 봐야만 안다.
- 두 값을 뺄셈해 "+4.8pp 올랐다" 로 쓰면 안 된다. 다른 시험지다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[stale-baseline-quoted-as-current]]

## Case 3: 검출 실패가 두 갈래였다 — 크롭 잘림과 기기 약세

### Grounding
- Git Context: `hash:c28b8c1`
- Evidence: `assets_dev/train/band_det_clip_split.py`
- Confidence: 5/5

### Discovery
눈검증 시트의 최악 6장이 전부 같은 모양이었다. 사람 라벨이 GM 크롭 왼쪽
가장자리 띠로만 남고 검출기는 화면 가운데 정보칼럼을 숫자줄로 집는다.

사람 밴드 라벨이 GM 크롭 안에 든 면적 비율로 가르니:

```
python band_det_clip_split.py
  밴드가 크롭 안(>=0.95)  n=246  median 0.727  p10 0.570
  잘린 장(<0.95)          n= 18  median 0.339  p10 0.000
```

가장 많이 잘린 여섯(2431 0.030 · 1610 0.098 · 1593 0.291 · 606 0.581 ·
1614 0.603 · 1094 0.686)이 최악 여섯과 정확히 같다.

### Analysis
- Why non-obvious: 하나의 median(0.717)이 성격이 다른 두 모집단을 덮고
  있었다. 기본 눈검증 시트가 최저 n장을 뽑으므로 가장 심한 부류만 보이고
  나머지 실패 유형이 가려진다.
- 잘림은 하드 실링이다. 숫자가 크롭 밖이면 어떤 밴드 검출기도 못 이긴다.
  이건 GM 검출기 문제지 밴드 검출기 문제가 아니다.
- 하위 기기는 잘림 탓이 아니었다. 깨끗한 크롭만 봐도 약하다(UltraMini 24장
  0.574 · On Call Extra 0.573 · KOOKMIN Check 0.605, 전체 0.727).
- Action taken: 잘린 18장은 레터박스 카드로, 기기 약세는 합성 카드로 배정.

### Related Knowledge
- **Anti-Patterns**: [[aggregate-hides-stratified-failure]]
- Patterns: [[scene-component-split]]

## Case 4: 학습 손실이 낮다고 훈련이 끝난 것이 아니다

### Grounding
- Git Context: `hash:c28b8c1`
- Evidence: `assets_dev/train/band_det_synth_gap.py`
- Confidence: 5/5

### Hypothesis
실사진 IoU 0.727 이 낮은 것이 훈련 부족인가 도메인 격차인가.

학습 로그만 보면 훈련은 끝난 것처럼 보였다.
`epoch 40 train 0.00166 sanity-val 0.00031` — val 손실이 train 보다도 낮다.

### Results
같은 모델을 학습에 쓴 합성 2,600장에 돌려 같은 자(poly_iou)로 쟀다.

```
python band_det_synth_gap.py --images <synth>/images --manifest <synth>/manifest.jsonl
  합성   median 0.901 p10 0.824  (세로형 0.904 · 가로형 0.862)
  실사진 median 0.727 p10 0.570  (잘린 장 제외 246장)
```

### Analysis
- Confirmed/Refuted: 둘 다 있다. 학습 데이터에서조차 1.0 이 아닌 0.10 이
  훈련·용량 여유이고, 0.901 에서 0.727 로의 0.174 가 도메인 격차다.
  후자가 크다.
- Key insight: 손실은 IoU 가 아니다. MSE 0.00031 은 정규 좌표 RMSE 약 1.8%
  인데 그 정도 좌표 오차가 IoU 로는 0.1 을 깎는다. 낮은 손실을 보고
  "수렴했으니 남은 건 데이터 문제" 로 넘어가면 용량 여유를 놓친다.
- Implications: 에폭만 늘려서는 0.83 근처가 한계다. 그 이상은 합성을 고치거나
  실사진을 학습에 넣어야 한다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[proxy-metric-moves-against-the-goal]]

## Case 5: 고정 문턱 자가검사가 입력 분포를 넓히자 무력화됐다

### Grounding
- Git Context: `hash:e3c62cc`
- Artifact: `docs/reports/synth-overhaul-review.md`
- Confidence: 5/5

### Error
`glyph_plane_check` 가 크게 나빠졌다.

```
              병합 전    이 브랜치
  p10        0.9908     0.9384
  min        0.7043     0.0000
  0.9 미만   0.8%       8.7%   (n=300)
```

### Environment Context
- Packages: conda sugartrain (opencv-python, numpy, torch)
- Migration Issue: No

### Fix Code
아직 고치지 않았다. 카드로 세웠다 — 잉크 문턱을 패널 대비에 비례시킨다.
자가검사 정의 변경은 판단이 필요한 자리라 리뷰에서 고치지 않았다.

### Analysis
- Root cause: 검사가 `ink = abs(img - bg) > 30` 이라는 고정 문턱을 쓴다.
  열화 상한 카드가 대비 분포를 의도적으로 아래로 넓히자(실사진 p10 60 ·
  40미만 1.1% 를 맞추려고) 저대비 패널에서 잉크가 문턱을 못 넘는다.
- 수치로 확정했다. 대비와 gpc 가 단조 관계다.

```
  대비 0~40    gpc median 0.764   0.9미만 66.7%
  대비 40~60   gpc median 0.953   0.9미만 33.3%
  대비 120+    gpc median 1.000   0.9미만  2.4%
```

- Why it failed: 검사가 "글리프 평면이 어긋났다" 와 "패널이 흐리다" 를
  구별하지 못한다. 산출물은 정상이다. 최저 6장을 오버레이로 확인했고 쿼드는
  전부 숫자에 정확히 붙어 있다. 망가진 것은 검사의 분해능이다.
- 전날 이 검사가 살아 있는지 확인했던 방법(글리프 평면을 8px 어긋뜨리면
  median 0.9997 에서 0.8337)이 이제 저대비 패널에서 무의미하다. 어긋나지
  않아도 그 아래로 떨어진다.
- 일반화: 가드의 문턱이 고정이면 입력 분포를 넓히는 변경이 그 가드를
  조용히 무력화한다. 가드를 만들 때 문턱이 무엇에 상대적인지 적어야 한다.

### Related Knowledge
- **Anti-Patterns**: [[metric-path-not-under-test]] ·
  [[contract-guard-too-narrow]]

## Case 6: 지표가 전부 통과인데 사람이 합성을 8/8 로 구분했다

### Grounding
- Git Context: `hash:cb8fbc0`
- Evidence: `assets_dev/train/synth_vs_real_sheet.py`
- Confidence: 5/5

### Discovery
합성 8장과 실사진 8장을 섞어 쿼드 없이 보였더니 사람이 8장 전부를 맞혔다.
그 시점 합성은 잰 축이 전부 통과였다. 링 2.22(목표 1.90 플러스마이너스 0.5) ·
안쪽 1.49(1.11) · 전체 2.05(1.77) · 대비 median 103(99) · 밴드 기하 4축 ·
종횡비 · 하드 검사 0.

사람이 짚은 단서:
- LCD 안 글자가 세 방식으로 섞여 있다. DSEG 세그먼트(큰 숫자), Hershey
  벡터 폰트(mem, mg/dL — 세그먼트 LCD 가 낼 수 없는 곡선), 도트 양자화
  (시간줄). 실사진 722 는 값과 날짜와 시간이 전부 세그먼트다.
- 콜론이 통짜 사각형 하나다. 실물은 사각 점 두 개(722 의 1 콜론 27).
- 글자 크기가 한 장에 4~5종이다. 실물은 큰 숫자 하나와 작은 주석 하나뿐.
- 배터리 내부가 채워져 진행 막대처럼 보이고 돌기가 없다.
- 화살표가 떠다닌다. 실물에서 화살표는 몸체에 인쇄된 미터기를 가리키는
  지시자다(ACCU-CHEK Instant 1195 · 2039 · 1193 · 1215 — 검은 창 바깥
  오른쪽 회색 띠에 점 약 10개가 인쇄돼 있고 창 안 삼각형 세그먼트가
  그것을 가리킨다).

### Analysis
- Why non-obvious: 잰 축(밀도 · 대비 · 기하 · 극성)은 분포 통계다. 사람이
  본 것은 렌더링 방식과 모양이다. 통계가 맞아도 방식이 틀리면 즉시 구분된다.
- 이 결함들은 이전 세션에서 사람이 구두로 지적했는데 카드와 raw 와 위키
  어디에도 기록되지 않았다. 그래서 실행자가 본 적이 없고 같은 결함이 렌더러
  재작성을 두 번 통과했다. 기록하지 않은 것이 원인이다.
- Action taken: 카드로 세우고 AC 에 "고친 뒤 blind 시트를 다시 내고, 사람이
  여전히 전부 맞히면 남은 단서를 카드에 추가한다" 를 게이트로 넣었다.
  `synth_vs_real_sheet.py`(pair · blind · contrast)를 커밋해 이 검토를 명령
  하나로 만들었다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[sampled-uniformity-as-proof]]

## Case 7: 내가 낸 수치에 출처가 없었고, 해명까지 지어냈다

### Grounding
- Git Context: `hash:fec6e17`
- Evidence: `docs/reports/session-handoff-2026-09-12.md` 정정 블록
- Confidence: 5/5

### Error
인수인계 문서에 엣지 밀도 실사진 7.51%(p10 5.44%, 각 300장)를 적고 같은 축의
다른 측정 2.25% 와 나란히 놓았다. 정정하면서 자(측정 코드)가 달라 재현되지
않았다고 적었다.

사용자 지적: 넌 코드를 볼 수 있는데 무슨 개소리야.

### Fix Code
자는 `measure_panel_stats.py` 로 저장소에 있었고 `--frame-exc` 손잡이까지
코드에 적혀 있었다. 읽고 돌렸다(n=84).

```
--frame-exc 0        median 2.52%  p10 0.64%
--frame-exc 0.04     median 2.22%  p10 0.48%   (상대의 2.25%)
긴 변 896 정규화 제거  median 1.06%
밴드 미제외           median 2.23%
```

### Analysis
- Root cause: 커밋된 자가 있는데 즉석 스크립트로 쟀다. 즉석 스크립트는
  커밋되지 않아 나중에 무엇을 쟀는지 확인할 방법이 없다.
- 7.51% 는 어떤 손잡이 조합으로도 나오지 않는다. 실사진 84장의 최대값이
  5.43% 인데 적어 둔 p10 이 5.44% 였다. 분포 자체가 다른 대상에서 온 값이다.
  각 300장도 성립 불가능하다(숫자 영역을 빼려면 밴드 라벨이 필요한데 84장뿐).
- 두 번째 실패가 더 나쁘다. 수치가 갈렸을 때 검증 가능한 주장(자가 달랐다)을
  검증하지 않고 적었다. 자는 코드이고 읽으면 된다. 지식의 부재가 아니라
  조회하지 않고 말한 것이다.
- Action taken: 7.51% 를 출처 불명으로 폐기. 규칙을 양쪽에 박았다.
  `CLAUDE.md` 수치를 인용하기 전에 자가 이미 있는지 본다, 그리고
  `docs/GLM_TASKS.md` 3.7. 수치 옆에 명령과 n 을 적고, 자가 없어 새로
  만들면 그 스크립트를 커밋한다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[ad-hoc-ruler-beside-the-committed-one]] ·
  [[verify-premises-before-executing]]

## Case 8: 표본이 작은 줄 알면서 단정했고, 저장소의 하드 검사를 안 돌렸다

### Grounding
- Git Context: `hash:36ec17b`
- Evidence: `docs/reports/synth-gm-crop-review.md`
- Confidence: 5/5

### Error
`glm/synth-gm-crop` 을 리뷰하며 n=40 으로 재고 "쿼드 정확 — 캔버스 밖 0장,
유리 밖 1장(4px), 경계 클램프의 반올림으로 보인다" 고 통과시켰다.

다른 세션이 같은 브랜치를 677줄로 리뷰해 둘을 잡았다.
- 가로형에서 밴드 쿼드가 유리를 넘는다. 3/52, 최대 24px.
- `validate_synth_panel.py` 가 하드 실패를 찍는다. clipped 108/120
  (main 은 0/120).

### Fix Code
677줄 판으로 되돌리고 정정 커밋했다. 이후 리뷰에서는 하드 검사를 먼저 돌리고
층(세로/가로)을 고정한 시트를 따로 뽑는다.

```
python validate_synth_panel.py --count 120 --seed 23000
python band_det_worst_sheet.py --layer portrait|wide
```

### Analysis
- Root cause 1: 가로형은 코퍼스의 8~11% 다. n=40 과 12장 몽타주에는 구조적
  으로 안 걸린다. 표본이 작다는 것을 알면서 반올림으로 보인다고 단정했다.
- Root cause 2: 저장소가 가진 자체 하드 검사 스크립트를 존재조차 확인하지
  않았다. 리뷰에서 "무엇이 이미 이 브랜치를 검사하고 있나" 를 묻지 않았다.
- 그리고 그 리뷰 자체가 중복이었다. 다른 세션이 이미 더 낫게 해 둔 것을
  같은 경로에 151줄로 덮어썼다.

### Related Knowledge
- **Anti-Patterns**: [[sampled-uniformity-as-proof]] ·
  [[downscaled-view-as-evidence]] · [[metric-path-not-under-test]]

## Case 9: 단위는 혈당 값과 같은 줄에 놓이지 않는다

### Grounding
- Git Context: `hash:0389cfa` (+ sugarScan-det 미커밋 작업트리)
- Evidence: 기기별 시트 5장×13종(seed 51000/52000), `synth_profiles.py` PROFILES
- Confidence: 4/5

### Discovery
사람이 합성 몽타주를 보고 준 규칙이다 — **액정에서 단위(mg/dL)는 혈당 값과
같은 줄에 놓이지 않는다.** 지금 프로파일은 정반대로 박혀 있다:

- `gmate` `pos="right-mid"`, `caresens_n_premier` `right-mid`,
  `onetouch_ultra` `left-mid`, `accuchek_instant`/`gc_ms_one`/`acura_plus`
  `right-baseline` — 전부 숫자와 같은 줄
- `synth_panel.py` 의 밴드 폭 역산이 그 자리(`_allow`)를 **미리 떼어** 숫자
  필드를 좁히기까지 한다. 같은 줄 배치가 기하에 박혀 있다는 뜻이다.

증상: 단위가 끝자리에 처박히거나(gmate 5장 전부), 숫자 왼쪽 한가운데 뜬다.

### Analysis
- 왜 비자명한가: 프로파일의 `pos` 값에 근거 사진 id 가 붙어 있어 실측으로
  보였다. 실제로는 '단위가 숫자 옆에 있다'와 '단위가 숫자와 같은 baseline 에
  있다'를 구분하지 않은 채 후자로 구현됐다.
- 영향: 밴드 폭 역산·Placer 예약·`_allow` 계산이 전부 이 전제 위에 있다.
  규칙을 적용하려면 프로파일 `pos` 값과 역산 코드를 함께 고쳐야 한다.
- 조치: 아직 없음. 규칙만 기록한다(구두 피드백을 그 자리에서 남기는
  [[distribution-matched-method-wrong]] 체크리스트).

### Related Knowledge
- Anti-Patterns: [[distribution-matched-method-wrong]]

## Case 10: 사람이 아는 기기 속성을 자로 되찾으려 했다 — 극성

### Grounding
- Git Context: `hash:31d070d`
- Evidence: `assets_dev/train/polarity_gt.jsonl`(사람 30장) ·
  `score_polarity_defs.py` · `diag_polarity_bg.py`
- Confidence: 5/5

### Design Decision
합성 프로파일의 극성(음각/양각)을 **측정 파생에서 사람 선언으로** 바꿨다.
`PANEL_ATTRS` 가 `real_baseline.polarity.by_device` 에서 파생하던 것을
`synth_profiles.PROFILE_INVERTED` 12줄(근거 사진 id 동봉)로 교체했다.

### Alternatives Considered
- 자를 더 깎는다 — 배경 기준을 바꿔 사람 정답 30장에 채점했다.
  crop(현행) 19/30 · out(밴드밖) 15/30 · band(밴드안) 26/30 ·
  mode(밴드 봉우리) 26/30 · minority(소수파) 26/30. 최선이 26/30 이고 남은
  오차가 하필 프로파일 기기에 몰렸다(Green Doctor 3/4 반전인데 사진 4장
  전부 정상 · ACURA PLUS 4/4 인데 3장 전부 정상 · Gmate 1/4 인데 4장 전부
  정상). 자를 더 깎을 자리가 아니다.
- 기기명으로 라벨 — 같은 이름 아래 서브모델이 갈린다(ACCU-CHEK Performa
  0/8 vs Performa Nano 8/8, 후자는 백라이트 액정이라 아예 다른 물건이다).
  프로파일 단위로 선언하고 갈리면 프로파일을 쪼갠다.
- **선택**: 프로파일 단위 사람 선언. 극성은 기기의 성질이고 사람이 이미
  안다. 사진에서 달라 보이는 것은 극성이 아니라 대비이고, 대비는 이미 별도
  축으로 잰다.

### Analysis
- Context: 이 코퍼스를 라벨링한 사람이 기기 54종을 눈으로 식별해 두었다.
  그가 아는 사실을 알고리즘으로 복원하려는 시도였다.
- **Trade-offs**: 선언은 새 기기마다 사람 손이 든다. 대신 틀리지 않는다.
  자는 손이 안 들지만 26/30 이고 어디서 틀리는지 예측할 수 없다.
- Reversibility: High — 자(`measure_polarity`)는 남겨 두고 익명 풀의 비율
  산출에만 쓴다.
- 부수 효과: 자의 배경 기준이 GM 크롭 전체 중앙값이라 어두운 베젤 기기를
  통째로 반전으로 세던 계통 오차를 잡았다(정상 25장 중 11장 오검). 자 자체는
  밴드 히스토그램 봉우리로 교체했다(`hash:66a7941`).

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[label-without-visual-ground-truth]] ·
  [[proxy-metric-moves-against-the-goal]]

## Case 11: 검증기가 다른 물건을 나란히 찍어 두 번 오진했다

### Grounding
- Git Context: `hash:904c266`
- Evidence: `validate_synth_panel.py` 종횡비 줄 · seed 31000 n=300
- Confidence: 5/5

### Error
검증기가 합성 **캔버스** w/h 를 실측 **GM 쿼드**(화면) w/h 와 나란히 찍었다.
합성에서 GM 쿼드에 해당하는 것은 캔버스가 아니라 유리다 — 캔버스는 유리에
몸체 마진을 더한 것이라 언제나 더 좁다.

```
캔버스 0.734 · 유리 0.800 · 실측 0.792   (seed 31000 n=300)
```

### Environment Context
- Packages: sugartrain(opencv · numpy)
- Migration Issue: No

### Fix Code
```python
gq = np.asarray([r["glass_quad"] for r in manifest], float)
wh = (gq[:, :, 0].max(1) - gq[:, :, 0].min(1)) / \
     (gq[:, :, 1].max(1) - gq[:, :, 1].min(1))     # 유리 = 실측과 같은 물건
```

### Analysis
- Root cause: 실측 자(`measure_panel_stats.collect_aspect`)는 화면 쿼드를
  재는데, 합성 쪽 대응물을 확인하지 않고 manifest 에 이미 있던 `wh`(캔버스)를
  집어 썼다.
- Why it failed: 두 수치가 **같은 이름**(종횡비)을 갖고 있어 비교가 성립하는
  것처럼 보였다. 분모가 다르다는 사실이 출력 어디에도 없었다.
- How it was fixed: 유리 쿼드(`glass_quad`)를 렌더 산출과 manifest 에 내보내고
  비교를 그것으로 바꿨다. 캔버스 w/h 는 참고로 따로 찍고 실측 대응물이 없다고
  출력에 적는다. 진단이 그 위에 쌓여 있었다 — '합성이 실측보다 좁다' 에서
  '프로파일 12종이 코퍼스 54종을 대표하지 못한다' 까지 갔는데, 유리 기준으로
  재니 0.800 vs 0.792 로 맞았다. 고치면서 밴드 기하 4축을 새로 찍었다 —
  검증기가 그때까지 합성기의 핵심 산출물인 밴드 기하를 아예 안 찍고 있었다.

### Related Knowledge
- Concepts: [[coordinate-frame]] · [[metric-provenance]]
- **Anti-Patterns**: [[unnamed-coordinate-frame]] · [[metric-path-not-under-test]]

## Case 12: 전역 난수 두 곳이 시드 재현성을 깼다

### Grounding
- Git Context: `hash:7a1a453`
- Evidence: seed 31000 n=300 두 실행 manifest md5 `92a5367f84724f62c9b638ffe3dede8d`
  동일 · 이미지 300장 전량 동일
- Confidence: 5/5

### Error
`synth_panel.py gen --count 300 --seed 31000` 을 두 번 돌리면 다른 코퍼스가
나왔다. 그래서 AC 수치를 인용해도 그 명령이 같은 값을 다시 내지 못했다.

### Environment Context
- Packages: numpy · python random
- Migration Issue: No

### Fix Code
```python
_npr = np.random.default_rng(rng.getrandbits(63))   # 광학 노이즈
img = img + _npr.normal(0, rng.uniform(2, 9), img.shape)
...
am = rng.choice(["am", "pm"])                        # 모듈 전역 random 아님
```

### Analysis
- Root cause: 난수원이 셋이었다 — 넘겨받은 `rng`, 모듈 전역 `random`,
  전역 `np.random`. 뒤 둘이 시드 계약 밖이었다.
- Why it failed: 형제 스크립트(`validate_synth_panel`)가 `np.random.seed` 를
  불러 그 경로에서만 재현됐다. 실제 학습 데이터를 굽는 경로(`gen`)는 안
  불렀다. 재현되는 경로만 보고 재현된다고 결론냈다.
- How it was fixed: 두 난수원을 rng 파생으로 바꿨다. 재시도 루프가 rng 소비
  횟수를 데이터(측정 밀도)로 가르는 구조는 그대로 뒀다 — 모든 난수원이 rng
  파생이면 결정성은 유지되고, 전역 난수를 하나라도 들이면 그 구조 때문에
  코퍼스 전체가 갈라진다는 것을 주석으로 못박았다.

### Related Knowledge
- **Anti-Patterns**: [[metric-path-not-under-test]] ·
  [[shared-state-split-by-worktree]]

## Case 13: 두 모델이 서로 다른 합성기를 보고 있었다

### Grounding
- Git Context: `hash:904c266`
- Evidence: `build_profiled_cache.py` · `reader_onnx_spec.md` ·
  삭제된 `synth_profiles.render_profiled`
- Confidence: 5/5

### Discovery
밴드 검출기는 `synth_panel`(기기 12종·극성 사람 선언·단위 아래·형질 고정)을
보고, CTC 리더는 `render_profiled`(극성 `rng<0.5` 동전던지기·기기 개념 없음·
단위가 숫자 옆)을 봤다. 한 저장소에서 두 모델이 다른 세계를 배우고 있었다.

프레이밍도 갈라져 있었다. 실사진 팔은 GM 쿼드를 `BOX_MARGIN` 10% 로 사방
확장해 워프하는데(리더 규격의 프레임 조립 절), 합성 팔은 화면만 320x160 으로
그렸다 — 한 캐시 안에서 실사진과 합성의 프레이밍이 달랐다.

### Analysis
- Why non-obvious: import 그래프만 보면 `render_profiled` 는 패널 렌더러가
  안 부르는 죽은 코드로 보인다. 부르는 쪽이 **다른 모델**이었다. 320x160 을
  죽은 경로로 오진한 것이 그 때문이다 — 그건 리더의 입력 크기였다.
- Impact: 리더를 다시 구우면 오늘 한 작업이 하나도 안 들어간 화면 23,000장을
  학습했을 것이다.
- Action taken: `framed_src_rect`/`frame_crop` 을 `eval_reader` 에서
  `build_cache_v2`(BOX_MARGIN 의 집)로 옮기고 — tensorflow 를 끌고 오지 않게 —
  `synth_panel.reader_view` 가 실사진 팔과 같은 함수를 통과하게 했다.
  `render_profiled` 282줄은 지웠다.

### Related Knowledge
- **Anti-Patterns**: [[duplicated-geometry-implementation]] ·
  [[improvement-parked-outside-the-pipeline]]

## Case 14: 기기 고정을 한 점으로 못박아 검출기가 나빠졌다

### Grounding
- Git Context: `hash:3cdd7a5`
- Artifact: `eval_band_detector.py gate` · `device_layout_stats.py --emit`
- Confidence: 5/5

### Hypothesis
같은 기기가 장마다 같아 보이게 만들면(기기 형질 고정) 합성이 실물에 가까워져
검출기 성적이 오른다.

### Results
사람 밴드 라벨 264장 게이트, 같은 하이퍼파라미터(40 epoch):

| 합성 | det IoU median | p10 | UltraMini(n=30) |
|---|---|---|---|
| 구판 v0 | 0.717 | 0.547 | 0.548 |
| 구판 v0b(재학습) | 0.728 | 0.524 | 0.591 |
| 기기 한 점 고정 v1 | 0.680 | 0.458 | 0.403 |

학습 노이즈는 같은 코퍼스 두 번에서 중앙값 0.011 이다. 0.680 은 그 밖이다.

### Analysis
- Refuted: 기기 일관성을 높이면 합성이 좋아진다는 가정. 사람 눈에는 맞고
  검출기에는 정보 손실이었다.
- Key metrics: 기기당 한 점(중앙값)으로 못박으니 합성 코퍼스가 기기 수만큼의
  점이 되고, 검출기가 그 사이와 바깥(코퍼스 54종)을 못 메웠다.
- 실측이 그 가정을 부정한다 — 같은 기기도 촬영 각도·거리로 흔들린다:
  UltraMini ar p10/p90 1.627/2.463, 세로형은 대개 ±5~10%, 밴드 폭도
  gmate 0.990~1.002 부터 accuchek_instant 0.712~0.904 까지 기기마다 다르다.
- Implications: 기기는 중심을 정하고 촬영이 그 주위를 흔든다. LAYOUTS 를
  (p10, 중앙, p90) 삼각분포로 바꾸고 렌더당 한 번씩 뽑는다. 사람 눈의 같은
  기기로 보인다와 검출기의 공간을 덮는다가 양립한다.
- 기기별 IoU 는 학습 노이즈와 구분되지 않는다(UltraMini 0.548↔0.591, n=4
  기기는 더하다). 신뢰할 수 있는 것은 전체 중앙값과 n>=14 기기뿐이다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[sampled-uniformity-as-proof]] ·
  [[aggregate-hides-stratified-failure]]

## Case 15: 조명이 좌우하는 지표를 합성의 목표로 삼았다 — 밀도

### Grounding
- Git Context: `hash:edff319`
- Evidence: 사람 지적 — 실화면의 밀도체크를 합성화면에 대입하는 건 심각한
  에러다, 조도·광원·그림자에 왜곡된다
- Confidence: 5/5

### Error
실사진의 밴드 밖 엣지 밀도를 합성의 목표로 삼아, 렌더러가 그 값을 맞추려
했다. 두 군데서 그러고 있었다: 밀도가 목표에 못 미치면 최대 3회 다시 그려
가장 밀도 높은 장을 선택했고(여백 분포가 두꺼운 몸체 쪽으로 계통적으로
기울었다), 그래도 모자라면 도트줄·mem·아이콘을 48회 시도로 채워 넣었다.

### Environment Context
- Packages: sugartrain
- Migration Issue: No

### Fix Code
```python
# 렌더는 1회다. 화면에 무엇이 있는지는 기기가 정하고, 밀도는 렌더 뒤에
# 재서 보고만 한다(manifest.density).
return _render_once(value, rng, profile, pid, inverted)
```

### Analysis
- Root cause: 엣지 밀도는 화면 내용이 아니라 촬영 조도·광원·그림자가 크게
  좌우한다. 그 값을 내용으로 맞추려 들면 근거 없는 요소가 화면에 얹힌다.
- Why it failed: 밀도는 잴 수 있고 실측이 있는 축이라 목표로 삼기 쉬웠다.
  그 축이 무엇의 함수인지는 묻지 않았다.
- How it was fixed: 재시도 선택과 채움 필러를 둘 다 지웠다.

### Related Knowledge
- Concepts: [[metric-provenance]]
- **Anti-Patterns**: [[proxy-metric-moves-against-the-goal]] ·
  [[distribution-matched-method-wrong]]

## Case 16: 설계가 측정을 되짚었다 — 상중하 3분할

### Grounding
- Git Context: `hash:2fcae86`
- Artifact: `validate_synth_panel.py --count 150 --seed 23000`
- Confidence: 4/5

### Hypothesis
사람 설계 — 세로 레이아웃이니 혈당을 60~70% 가량 차지하게 하고 상하에
추가정보, 가운데에 혈당정보를 표시한다. 밴드 높이를 실측 분포에서 뽑는 대신
위·아래 정보줄이 쓰는 높이를 빼고 남는 가운데를 값이 채우게 하면 어떤 기하가
나오는가.

### Results
```
밴드/유리(portrait) 합성 w 0.895 h 0.454 cx 0.516 cy 0.437
                   | 실측 w 0.888 h 0.447 cx 0.512 cy 0.407   (n=217)
```

### Analysis
- Confirmed: 설계에서 나온 기하가 사람 라벨과 거의 같다. 지금까지 실측
  분포에서 뽑아 맞추던 값을 레이아웃이 스스로 내놓는다.
- Key metrics: 실측 bh 0.39~0.47 자체가 아래에 두 줄이 오는 화면의 **결과**
  였다. 그 결과값을 입력으로 쓰면 원인을 모른 채 숫자만 맞추게 되고, 아래
  공백이 40%씩 남아도 지표는 통과한다.
- Implications: 측정값을 파라미터로 직접 쓰기 전에 그 값이 무엇의 결과인지
  본다. 결과를 입력으로 쓰면 구조가 사라지고 숫자만 남는다.
- 같은 변경으로 사람이 지적한 세 가지가 함께 풀렸다: 아래 공백, 숫자 크기,
  세그먼트 굵기(숫자가 커지니 획이 따라 굵어졌다 — 마스크 부풀림은 되돌렸다).

### Related Knowledge
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[distribution-matched-method-wrong]]
