# 2026-09-09

## Case 1: G30 밴드 크롭 A/B 가 학습 예산 불일치로 무효였다 — 대조군 60에폭·0.1238 vs 새 팔 12에폭·0.96

### Grounding
- Git Context: `hash:eba39ca` (GLM 보고서) · `hash:7d38c8f` (판정)
- Evidence: `assets_dev/train/training_log.csv`(마지막 파인튜닝 런 60에폭 최종 손실 `0.1238338053226471`, 직전 두 런도 60에폭 `0.13472022116184235` · `0.10210204869508743`) · `training_log_letterbox.csv`(12에폭 `0.643500566482544`) · `docs/reports/G30-band-crop.md`(크롭 팔 12에폭 손실 0.96)
- Confidence: 5/5

### Error
세로형 밴드 크롭 프레이밍 A/B 의 결과표가 "크롭이 나쁘다"로 읽혔다:
완전일치 97.06% → 87.79%, 위험군 0.98% → 8.29%, McNemar p=1.68e-23.

실제로는 두 팔의 **학습 예산이 달랐다.** 대조군 `reader_model/` 은
파인튜닝 재시작 7회가 쌓인 결과로 마지막 런이 60에폭·손실 0.1238 인데,
새 팔은 스크립트 기본값인 12에폭·손실 0.96 에서 멈췄다(보고서에도 하강 중이라
명시). 손실 8배 차이 나는 두 모델을 비교한 표라 프레이밍의 성적이 아니다.

같은 구멍이 직전 G29 letterbox 팔(12에폭·0.64)에도 있었다.

### Environment Context
- `assets_dev/train/ctc_reader_v2.py` 사전학습 40 + 파인튜닝 12 (스크립트 기본값)
- Migration Issue: No

### Fix Code
지시서 쪽을 고쳤다 — `docs/GLM_TASKS.md` §3.5 신설:

```text
A/B 지시서에는
- 대조군의 실제 학습 예산을 로그에서 확인해 숫자로 적는다
  (training_log.csv 의 mtime 이 모델 폴더와 같은지부터 본다)
- 새 팔의 에폭 수를 그 숫자로 명시한다. "기본값 그대로"라고 쓰지 않는다
- 보고서에 양쪽의 최종 손실을 나란히 적게 한다
- 예산 끝에서도 손실이 하강 중이면 그렇게 적고 멈추게 한다
```

대조군 로그가 현 모델의 것인지 확인한 방법:

```bash
ls -lad reader_model training_log.csv   # mtime 이 둘 다 Sep 5 03:46 로 같다
awk -F, 'NR>1 && NF==2' training_log.csv | wc -l   # 파인튜닝 320행 = 재시작 누적
```

### Analysis
- Root cause: 지시서가 "에폭 수를 건드리지 말 것"이라고 적었고, 그 말이
  가리킨 **스크립트 기본값이 대조군이 실제로 받은 예산이 아니었다.**
  대조군은 사람이 재시작을 거듭하며 만든 모델이라 예산이 코드에 없고 로그에만 있다.
- Why it failed: "하이퍼파라미터 고정"은 **두 팔 사이의** 동일성만 보장한다.
  대조군이 코드 밖에서 만들어졌다면 코드를 고정해도 대조군과는 안 맞는다.
  정확도 격차가 프레이밍 효과와 수렴 부족의 합인데 표에서는 분리되지 않는다.
- 이 실험에서 특히 위험했던 이유: 크롭은 세로만 자르므로 가로 해상도
  (= CTC 타임스텝 40)를 건드리지 않는다. G29 가 세운 기전으로는 letterbox 보다
  **유리해야 하는** 팔인데 더 못 맞았다. 기전과 결과가 어긋날 때 결과를 그대로
  믿으면 방향이 맞는 레버를 버린다.
- How it was fixed: 채택/기각 대신 **보류**로 판정하고, 에폭만 60으로 맞춘
  재측정(G31)을 지시서로 냈다. 바뀌는 값이 하나뿐임을 지시서에 못박았다.

### Related Knowledge
- Concepts: [[experiment-budget-parity]]
- Patterns: [[measure-the-premise-not-just-the-claim]] · [[verify-premises-before-executing]]
- **Anti-Patterns**: [[uncontrolled-budget-in-ab-comparison]] · [[aggregate-hides-stratified-failure]]

## Case 2: 세로 지터 0.15 가 파인튜닝을 붕괴시킨다 — 크롭 프레임에는 그만한 여유가 없다

### Grounding
- Git Context: `hash:eba39ca`
- Evidence: `docs/reports/G30-band-crop.md` — 파인튜닝 손실 6.90 → 5.52(3ep) → 5.40(12ep) 동결, TTA 완전일치 15/1122 = 1.34%, 위험군 882 (78.61%). 같은 캐시에서 세로 지터만 0.08 로: 5.94(2ep) → 4.91(4ep) → 2.83(6ep). `_diag/G30/probe_digit_band.py`
- Confidence: 5/5

### Discovery
크롭이 숫자를 프레임에 꽉 채우면 **증강 지터의 여유도 같이 사라진다.**
크롭 프레임에서 숫자 아래 여유의 p5 가 0.072 라 세로 이동 0.15 는 5% 이상의
장에서 획을 자른다(숫자 띠가 검출된 660장 기준, 절단 가능 비율 0.9% → 9.5%).
라벨은 그대로인데 획이 없어지므로 CTC 가 학습할 수 없는 짝이 되고, 모델은
모든 장에 같은 값을 내는 모드 collapse 로 떨어진다.

지시서는 "크롭이 조금 어긋나도 견디게" 하려고 지터를 0.08 → 0.15 로 올리라고
적었다. **의도와 정확히 반대 효과**였다.

### Analysis
- Why non-obvious: 지터를 키우는 것은 보통 정칙화라 안전한 쪽으로 여겨진다.
  여기서는 크롭이 여유를 먼저 없앴기 때문에 같은 값이 파괴적으로 바뀌었다 —
  **증강 강도는 절대값이 아니라 프레임의 여백에 대한 상대값이다.**
- 붕괴가 조용하지 않았던 것이 다행이다: 손실이 5.40 에서 동결되고 완전일치가
  1.34% 로 떨어져 표에서 바로 보인다. 조금만 잘렸다면 정확도가 몇 pp 떨어지고
  "크롭이 별로다"로 잘못 결론났을 것이다.
- Action taken: 0.15 폐기 확정. 재측정(G31)은 0.08 로 간다.
  원인 지정은 **같은 캐시에서 지터만 바꾼 차분 실험**으로 확정됐다 —
  이미지(크롭)와 증강을 분리한 이 설계가 없었으면 "크롭이 망쳤다"가 남았다.

### Related Knowledge
- Concepts: [[experiment-budget-parity]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[augmentation-exceeds-frame-headroom]]

## Case 3: 예산을 맞추자 격차의 6/7 이 사라졌다 — 그래도 기각했다, 오답의 성격이 바뀌어서

### Grounding
- Git Context: `hash:3d0cba0`
- Evidence: `assets_dev/train/training_log_bandcrop_ty008_e60.csv`(60에폭 최종 `0.10946083813905716`) · `training_log.csv`(기준선 마지막 런 60에폭 `0.1238338053226471`) · `assets_dev/train/_diag/G31/ab_report_ty008_e60.json`(`{'b': 15, 'c': 30, 'p': 0.0356978035551946}`) · `docs/reports/G31-bandcrop-epoch-matched.md`
- Confidence: 5/5

### Hypothesis
Case 1 에서 무효로 판정한 밴드 크롭 A/B 를, 파인튜닝 예산만 12 → 60에폭으로
맞춰 다시 잰다. 예산이 원인이었다면 격차가 좁아져야 한다.

### Results
예산은 실제로 맞았다 — 크롭 팔 60에폭 최종 손실 0.1095, 기준선 마지막 런
0.1238. 12·30에폭 지점도 0.9142/0.2754 대 0.5601/0.2668 로 같은 궤적.
**크롭 팔의 학습 손실이 오히려 더 낮다.**

완전일치 87.79% → 95.72% (기준선 97.06%). McNemar p 1.68e-23 → 0.0357.
격차 9.27pp 중 7.93pp 가 에폭 하나로 사라졌다.

오답의 구성:

```text
             완전일치   위험군   blank   안전 실패
기준선        1089       11       3       19
크롭 60에폭   1074       39       0        9
```

독립 재계산으로 보고서 표를 재현했다(reader_preds_bandcrop_ty008_e60.json,
n=1122 → exact 1074 · risky 39 · safe 9 · blank 0).
새로 틀린 30장 중 **25장이 위험군**이고, 크롭 팔 위험군 39건 중 **35건이
기준선에 없던 새 오답**이다(교집합 4건).

### Analysis
- Confirmed: Case 1 의 진단이 맞았다. 예산 불일치가 격차의 대부분을 만들었다.
  §3.5 규약(대조군의 실제 예산을 지시서에 숫자로 적는다)이 값을 했다.
- 그런데 **결론은 뒤집히지 않았다 — 이유만 유효해졌다.** 기각의 근거가
  "덜 학습된 표"에서 "예산 맞춘 표"로 바뀌었다.
- Key metrics: 완전일치 격차는 1.34pp 로 좁은데 **위험군은 0.98% → 3.48%
  로 3.5배**다. 크롭은 오답의 총량보다 **성격**을 바꾼다 — 침묵과 자릿수
  붕괴(22건 → 9건)를 줄이는 대신 자릿수 보존 오독을 28건 늘린다.
  숫자가 프레임을 채워 모델이 자릿수를 확신하게 되고, 확신이 붕괴 대신
  **그럴듯한 틀린 값**으로 나온다.
- Implications: 혈당 앱에서 자릿수 붕괴는 값 검증기와 프레임 합의가 잡지만
  자릿수 보존 오독은 통과한다. 안전 실패를 위험 실패로 바꾸는 교환은
  완전일치가 올라가도 받을 수 없다. **정확도 한 숫자로는 이 교환이
  보이지 않는다** — G31 을 완전일치만 보고 읽으면 "1.34pp 차이"다.
- 에폭을 더 늘리지 않기로 했다. 학습 손실이 이미 기준선보다 낮은데
  홀드아웃이 나쁘다 — 더 태우면 확신이 더 세져 위험군이 늘어날 쪽이다.
- 부수: 가로형 층 57.1% → 28.6%(21장). 가로형 입력은 픽셀 항등(44/44)이라
  재학습 효과다. 학습의 98.2%가 크롭된 세로형이라 가로형 풀프레임이 분포
  밖이 된다. 프레이밍을 한 층에만 적용하면 **손대지 않은 층이 분포 밖으로
  밀려난다.**

### Related Knowledge
- Concepts: [[experiment-budget-parity]] · [[failure-mode-composition]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[accuracy-hides-failure-mode-trade]] · [[aggregate-hides-stratified-failure]]

## Case 4: 정본 문서가 저장소 상태에 대해 한 사실 주장이 조용히 낡아 있었다

### Grounding
- Git Context: `hash:f10074a` (에셋이 커밋된 시점) · 발견은 `hash:289f828` 이후
- Evidence: `git ls-files --error-unmatch assets/models/7seg_classifier.tflite` → 추적됨(609,904 바이트, gitignore 대상 아님). 같은 시점 `CLAUDE.md:341` 은 "`assets/models/7seg_classifier.tflite` 는 아직 저장소에 없다"
- Confidence: 5/5

### Discovery
`CLAUDE.md`(에이전트 지침의 정본)와 `assets/models/README.md` 가 둘 다
"7seg_classifier.tflite 는 아직 없음"이라고 적고 있었는데, 파일은 `f10074a`
에서 이미 커밋돼 있었다. README 는 제목에 "(아직 없음)"을 달아 둔 채 바로
아래 표에 그 파일의 정확한 크기(609,904 바이트)를 적고 있었다 — 문서 안에서
이미 자기모순이었다.

### Analysis
- Why non-obvious: 문서의 **규약**(무엇을 하지 마라)은 어기면 사람이 지적하지만,
  문서의 **사실 주장**(무엇이 있다/없다)은 어겨도 아무도 실패하지 않는다.
  코드가 바뀌면 테스트가 깨지는데, 문서의 사실 주장을 지키는 테스트는 없다.
  에셋을 커밋한 작업은 "에셋 추가"였지 "문서 갱신"이 아니었다.
- Impact: 이 문장을 읽은 에이전트는 앱이 `ScanUnavailable` 로 떨어지는 상태를
  기본으로 가정한다. 실제로는 CNN 엔진이 살아 있으므로 진단이 어긋난다.
- 더 큰 형태: 같은 문서가 **CTC 리더가 앱에 있는지**에 대해서는 아무 말도
  하지 않아, 홀드아웃 97.06% 를 앱 성능으로 오해할 여지가 있었다.
  실제로는 `ocr_bootstrap.dart` 에 `SegmentRuleEngine` 과 7-세그먼트 CNN 만
  등록돼 있고 CTC 리더는 `assets_dev/train/` 밖으로 나온 적이 없다.
- Action taken: 두 문서를 고쳤다. "없다"를 "있다(`f10074a`)"로 바꾸되
  **없어도 동작해야 한다는 설계 불변**은 남겼다(그게 규약이고, 사실 주장이
  아니다). CTC 리더가 아직 앱에 없다는 문장을 명시적으로 추가했다.
- 일반화: 문서에 사실 주장을 적을 때는 **확인 명령을 같이 적는다.**
  "없다"보다 "`git ls-files ...` 로 확인하라"가 낡지 않는다.

### Related Knowledge
- Concepts: [[knowledge-retrieval-shape]]
- Patterns: [[verify-premises-before-executing]] · [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[verification-record-without-subject]]

## Case 5: 진단 작업의 천장을 먼저 쟀다 — 그리고 "왜 안 쟀나"를 사람에게 안 물었다

### Grounding
- Git Context: `hash:96c11ee`
- Evidence: 층별 실측 — 가로형 21/1122(1.9%) · 완전일치 12 · 위험군 3 · 안전 실패 6. `lib/ocr/src/ocr_bootstrap.dart` 등록 엔진 = `SegmentRuleEngine` + `SevenSegCnnEngine` 뿐
- Confidence: 4/5

### Discovery
G32(가로형 검출 실패 13장 진단)를 다음 작업으로 집으려다 천장을 먼저 쟀다.
가로형은 홀드아웃의 **1.9%** 이고, 9건을 전부 맞혀도 완전일치 기여는
**+0.80pp**, 위험군 기여는 **-0.27pp** 다. 작업을 열기 전에 곱한 것은 맞았다.

그런데 같이 적은 두 번째 관찰이 틀렸다. "G29·G30·G31 이 연속으로 홀드아웃
숫자만 다듬고 온디바이스는 0번 쟀다"를 보고 **"잴 수 있는 것만 반복해 재는
편향"이라고 결론냈다.** 사람에게 물어보니 이유는 그게 아니었다 —
**혈당계를 여러 종류 실제로 가지고 있지 않아서** 못 쟀다. 물리적 제약이지
작업 선택의 편향이 아니다.

### Analysis
- Root cause: 커밋 기록과 산출물만 보고 **왜 그렇게 됐는지를 추론했다.**
  기록에 남는 것은 "무엇을 했나"뿐이고 "무엇을 할 수 없었나"는 안 남는다.
  안 남은 자리를 동기로 채우면 사람의 제약이 사람의 실수로 둔갑한다.
- Why it matters: 이건 사람의 판단에 대한 서술이라 틀리면 위키에 **없는
  실수가 기록으로 남는다.** 코드에 대한 틀린 서술은 다음 사람이 코드를 보고
  잡지만, 사람의 동기에 대한 틀린 서술은 대조할 원본이 없다.
- 같은 규칙이 CLAUDE.md 에 이미 있다("증상 보고를 받으면 코드보다 현장을
  먼저 본다", "대상이 사람이어도 같다"). 데이터에 대해서는 지켰는데
  — 층 크기를 직접 셌다 — **사람의 동기에 대해서는 안 물어보고 단정했다.**

### 남는 사실 (제약을 반영해 다시 정리)

기기 다양성과 촬영 경로는 **다른 축**이다:

- **기기 다양성** — 사진 데이터셋(실사진 2,494장)이 이미 여러 기종을 담고
  있다. 손에 없는 기종을 실기기로 재는 것은 지금 불가능하고, 앞으로도
  사진 수집이 현실적인 경로다.
- **촬영 경로** — 라이브 카메라 프레임 · 가이드 박스 ROI 정렬 · 모션 블러는
  정지 사진 홀드아웃이 담지 못한다. 이건 **기기 한 대만 있어도** 잴 수 있고,
  CLAUDE.md 가 "인식이 전혀 안 되면 알고리즘보다 이 정렬을 먼저 의심하라"고
  적어 둔 자리다.

그리고 CTC 리더는 아직 앱에 없다(`ocr_bootstrap.dart` 등록 엔진 둘뿐,
ONNX 경로 미개통). 촬영 경로를 재려면 그게 먼저다.

- 진단 작업(G32)은 그 자체로 가중치도 코드도 바꾸지 않는다. 진단은
  **다음 수정이 정해져 있을 때** 값이 있다 — 이 판단은 유효하다.
- 점검 질문: "이 작업이 끝나면 무엇이 달라지는가"에 "숫자를 알게 된다"만
  답이 나오면, 그 숫자로 무엇을 할지가 이미 정해져 있는지 먼저 확인한다.

### Related Knowledge
- Concepts: [[knowledge-retrieval-shape]]
- Patterns: [[verify-premises-before-executing]] · [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[aggregate-hides-stratified-failure]] · [[metric-path-not-under-test]]

## Case 6: "사람 라벨 0.37" 의 불일치는 라벨 파일이 달랐던 것 — 이 저장소에는 상자가 세 종류다

### Grounding
- Git Context: `hash:9d147c0`
- Evidence: 직접 실측 — `glucose_batch1/1564` band w/h=0.54 · screen w/h=0.37, `glucose_batch1/1565` band w/h=0.84 · screen w/h=0.38. `doc/raw/2026-09-04.md` Case 9 의 인용 "사람 라벨 0.37·0.38" · `docs/reports/G32-wide-gm-failures.md`
- Confidence: 5/5

### Discovery
G32 가 1564·1565 를 조사하면서 "Case 9 가 인용한 사람 라벨 0.37·0.38 이
현재 라벨(0.54·0.84)과도 9/9 백업과도 불일치한다"를 미해결로 남겼다.
라벨이 바뀐 흔적을 찾는 방향으로 읽히는 기록이었다.

바뀐 것은 없었다. **0.37·0.38 은 `screen_boxes.jsonl`(LCD 화면 라벨)의
w/h 이고, G32 가 비교한 0.54·0.84 는 `band_boxes.jsonl`(숫자줄 라벨)이다.**
두 값 모두 지금 그대로 재현된다. Case 9 의 인용은 정확했고 대상이 달랐다.

### Analysis
- Root cause: 두 기록 모두 **"사람 라벨"이라고만 썼다.** 이 저장소에는
  사람이 그린 상자가 세 종류 있고(`screen_boxes.jsonl` 화면 ·
  `band_boxes.jsonl` 숫자줄 · GM 검출기의 `gmscreen_quads.jsonl`) 같은
  사진에서 셋의 종횡비가 전부 다르다. 수식어 없이 "사람 라벨"이라고 쓰면
  어느 것인지 사라지고, 나중에 대조하면 **데이터가 변조된 것처럼 보인다.**
- Why it matters: 미해결로 남은 불일치는 다음 사람에게 "라벨이 언젠가
  덮였다"는 의심을 물려준다. 실제로는 아무 일도 없었는데 백업 대조·복구
  스크립트 같은 비싼 조사를 부른다.
- 이건 [[unnamed-coordinate-frame]] 의 사촌이다. 저쪽은 좌표가 어느 프레임
  기준인지 안 적는 것이고, 이쪽은 **수치가 어느 대상을 잰 것인지** 안 적는 것.
  둘 다 숫자만 남고 출처가 사라져서 나중에 비교가 성립하지 않는다.
- 부수 효과 — 판단이 바뀌었다. 화면 라벨로 보면 1564(0.37)와 1565(0.38)는
  사실상 같은 장면이다. 둘의 차이는 사진 기하가 아니라 **1565 의 숫자줄
  라벨이 느슨하게(대각선에 가깝게) 잡힌 것**이었다. 밴드 라벨만 보고
  "1565 는 1564 와 다르다"고 결론냈으면 틀릴 뻔했다.
- Action taken: `docs/DONE.md` G32 절에 기록. `band_rotation.jsonl` 은
  손대지 않았다 — 유일한 소비자인 밴드 크롭 경로가 기각돼(Case 3) 1565 를
  넣어도 오늘 바뀌는 것이 없다. 규약으로 올린 것: **상자 수치를 인용할 때
  어느 라벨 파일인지 같이 적는다.**

### Related Knowledge
- Concepts: [[coordinate-frame]] · [[knowledge-retrieval-shape]]
- Patterns: [[frame-provenance-binding]] · [[verify-premises-before-executing]]
- **Anti-Patterns**: [[unnamed-coordinate-frame]] · [[verification-record-without-subject]]

## Case 7: 기준선 97.06% 의 분할이 세션이 아니라 이미지였다 — 누수로 인플레이션

### Grounding
- Git Context: `hash:c28c439`
- Artifact: `docs/reports/split-leakage-audit-2026-09-09.md` · `assets_dev/train/audit_split_leakage.py` · `_diag/split_leakage_report.json`(gitignored)
- Confidence: 4/5

### Discovery
`build_cache_v2.py:169-176` 이 실사진 풀을 `RandomState(123)` **이미지 단위**로 셔플해
train/holdout 을 갈랐다. dhash(64비트, EXIF 정규화) 근접쌍(해밍 ≤8)의 연결요소를
세션으로 계측한 결과: 교차 분할 근접쌍 9,068, **홀드아웃 73.7%(827장)가 train 과
같은 장면 성분**, **45.1%(506장)는 같은 장면+같은 표시값**까지 공유. 기존 예측
(reader_preds.json, G29 crc32)을 층별 재집계하면 완전일치가 clean 94.24% → 장면
누수만 97.20% → 값 누수 98.62% 로 **단조 상승** — 누수의 전형적 서명. 위험 오독률은
0.98% → clean 1.69%(약 2배).

### Analysis
- Why non-obvious: "연속 번호+같은 값" 세션 프록시를 먼저 시도했더니 런이 12개뿐이어서
  누수가 0.6% 로 보였다 — **세션은 번호 순서가 아니라 장면**이다(같은 구도에서 값만
  바뀌며 반복 촬영). 픽셀 유사도로 직접 재야만 보였다.
- Impact: 97.06%/0.98% 의 절대 해석 무효(정직한 층 94.24%/1.69%, n=295, SE≈1.4pp).
  G29~G32 의 짝비교 델타는 유지. dhash 회전 취약으로 누수 과소추정(보수적).
- Action taken: 재분할+재학습 카드 「장면 성분 분할 재학습」 등록. 몽타주 눈검증으로
  h0·h7 경계쌍 모두 같은 장면 확인(값은 상이 — GT 일치).

### Related Knowledge
- Concepts: [[experiment-budget-parity]] · [[knowledge-retrieval-shape]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[aggregate-hides-stratified-failure]] · [[metric-path-not-under-test]]

## Case 8: 그룹 분할 재학습 — 누수 없는 정직한 기준선은 96.67%/1.51% 였다

### Grounding
- Git Context: `hash:report-pending`
- Artifact: `docs/reports/grouped-split-rebaseline-2026-09-10.md` · `assets_dev/train/build_grouped_split.py` · `assets_dev/train_grouped/`(gitignored)
- Confidence: 4/5

### Hypothesis
세션 단위로 분할을 고치고 같은 사전학습·같은 60에폭 예산으로 재학습하면, 누수로
인플레이션된 97.06%/0.98% 대신 정직한 기준선이 성립할 것이다.

### Results
교차 성분 0 검증 통과(해밍 ≤8 연결요소 분할, 민감도 ≤6/≤8 기록). 새 기준선:
**완전일치 96.67%(1,278/1,322) · 위험 1.51%(20건) · 득표율거절 6.4%**. 옛 모델의
누수 없는 층 재집계(94.24%/1.69%)보다 2.4pp 위 — 중복 장면 제거로 에폭당 다양성이
올라간 효과 후보(표본 구성이 달라 단정 불가).

### Analysis
- Confirmed/Refuted: 확인 — 세션 무결 조건에서도 모델은 ~96.7% 를 달성한다.
  97.06% 대비 -0.4pp 는 누수 분의 실재였다.
- Key metrics: 위험률 0.98%→1.51%(약 1.5배), 득표율거절 5.2%→6.4%.
- Implications: 이후 모든 A/B·거절 임계 스윕은 train_grouped 기준선 위에서.
  옛 97.06% 인용은 폐기(짝비교 델타는 유효).

### Related Knowledge
- Concepts: [[experiment-budget-parity]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[uncontrolled-budget-in-ab-comparison]] · [[aggregate-hides-stratified-failure]]
