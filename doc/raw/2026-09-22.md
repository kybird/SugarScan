# 2026-09-22

## Case 3: §20.5 규약 가설을 사람 라벨 50장 병렬 채점으로 마감했다

### Grounding
- Git Context: `hash:(이 커밋)`
- Artifact: `rf_band_boxes.jsonl`(사람 라벨 50) · `report_fail_mix.py --gt rfband`
  · BAND_EXP_PLAN §26
- Confidence: 5/5

### Hypothesis

§20.5: Datumo(우리 규약) 98.3% vs Roboflow(READING) 93.4% — 6.6점이 모델
결함인지 라벨 규약 차이인지 가르지 못했다. 같은 사진을 우리 규약으로 다시
그려야 가린다.

### Results

사람이 rand 50장을 우리 규약으로 라벨링(skip 0). 같은 장·같은 8시드에서 정답만
교체해 채점:

- 두 규약 상자 좌표차: 상자폭 기준 중앙 20.1% · p90 37.0% · 최대 45.7%(§23 확인).
- 그런데 판정(통과/일부만/딴 데/미검출)은 **50장 전부·8시드 전부에서 동일**
  (차이 −0.00pt, 95% [−2.25, +2.26]).
- 층 분해: dev 586 전체 94.28±1.1 = rand 50장 98.75 + fail 층 29장 29.31
  (일부만 61.6) + 나머지 ~98. 손실의 대부분이 dev 4.9%(29장)의 잘림에 집중.

### Analysis
- Confirmed/Refuted: **규약 차이 가설 기각(비실패 표본에서)** — 규약 차이는
  실재하나 담음 판정에 닿지 않는다. READING 의 digit_cell 이 K 축소돼 낙관
  쪽이므로 반대 방향 가설("규약 때문에 과소평가")도 지지 안 됨. 갭의 정체는
  29장에 집중된 잘림(모델·데이터 쪽).
- Why non-obvious: 좌표가 20~46% 다른데 판정이 하나도 안 바뀐다는 건 직관에
  반한다 — 판정식의 마진 구조(pad 10%·digit_cell K 축소)가 두 규약의 차이를
  흡수한다. "좌표 다름 ≠ 판정 다름"을 재고 나서야 규약 가설을 닫을 수 있었다.
- 주의점: rand 층은 '실패장 제외 무작위'라 dev 대표가 아니다 — 98.75%를
  전체 성적으로 인용하면 안 된다(§26.1 표에 명시).
- 라벨러(사람) 관찰: 밴드 경계는 지각이 아니라 판단(0.10 마진) — 재현성 자
  여지. 광원·흐릿 사진 혼재 관찰은 B1 열화 축과 같은 방향.

### Related Knowledge
- Concepts: [[metric-provenance]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: 표본의 층 설계(실패 제외)를 잊고 표본 통계를 전체
  성적으로 읽는 것 — 층 분해 없이는 98.75 가 거짓 안전 신호가 된다

## Case 1: except Exception 이 SystemExit 를 못 잡아 백그라운드 초기화가 조용히 죽었다

### Grounding
- Git Context: `hash:22bcd08`
- Evidence: `assets_dev/train/webtool.py` `_atlas_ensure` · 아틀라스 in-process 흡수
  카드 Result(doc/kanban/done/아틀라스-검토-서버를-웹툴-프로세스로-흡수한다.md)
- Confidence: 5/5

### Error
웹툴(8777) 하나가 /atlas 를 in-process 로 서빙하도록 고친 뒤 첫 검증에서
/atlas 가 20분 넘게 "준비 중"(200 진행 안내)만 냈다. 서버 프로세스는 살아
있고 CPU 는 15초에 0.047초(사실상 정지).

### Environment Context
- Packages: 표준 라이브러리 http.server · make_failure_atlas.py(TF 적재)
- Migration Issue: No

### Wrong Code
```python
def run():
    global _ATLAS_STATE, _ATLAS_ERR
    try:
        import make_failure_atlas as mfa
        _ATLAS_STATE = mfa.init_serve_state(HERE, _AtlasArgs())
    except Exception as e:   # SystemExit 는 여기를 지나친다
        _ATLAS_ERR = f"{type(e).__name__}: {e}"
```

### Fix Code
```python
    except BaseException as e:  # SystemExit 포함 — build_cases 는 등가성
        # 검사에서 SystemExit 를 던진다. Exception 만 잡으면 초기화 스레드가
        # 조용히 죽고 /atlas 는 영원히 '준비 중'이 된다(2026-09-21 실제로 겪음).
        import traceback
        traceback.print_exc()
        _ATLAS_ERR = f"{type(e).__name__}: {e}"
```

### Analysis
- Root cause: `make_failure_atlas.build_cases` 는 등가성 검사 실패에서
  `raise SystemExit(...)` 로 종료한다. `SystemExit` 은 `BaseException` 의
  직계라 `except Exception` 에 잡히지 않는다. 백그라운드 스레드에서 잡히지
  못한 `SystemExit` 는 스레드만 죽인다 — 메인 스레프(서버 루프)는 멀쩡하고,
  상태 플래그도 에러도 기록되지 않아 요청은 영원히 "초기화 중" 분기를 탄다.
- Why it failed: 진행 상황을 관측 가능한 신호(산출물 mtime·로그 print)로 보지
  않고 상태 플래그 추측("아직 초기화 중이겠지")으로 읽어 20분을 낭비했다.
  포그라운드 재현 프로브(-u, unbuffered)가 7초 만에 진짜 원인을 드러냈다 —
  같은 초기화 경로라도 스레드가 아닌 메인에서 돌면 SystemExit 가 프로세스를
  죽여 로그에 즉시 보인다.
- How it was fixed: `except BaseException` 으로 바꿔 SystemExit 까지 안내(503,
  원인 명시)로 전이. 수정 후 진행 안내(200) → 20초 내 실패 안내(503) 전이
  확인. 이 실패 안내가 아틀라스 카드의 AC#3(미빌드 안내)을 그대로 채웠다.

### Related Knowledge
- Patterns: [[verify-symptom-before-hypothesis]]
- **Anti-Patterns**: 상태 플래그 추측으로 진행을 판단하는 것 — 스레드가 죽었는지는
  산출물·로그 같은 외부 신호로 본다

## Case 2: 아틀라스 등가성 실패의 정체 — 쿼드 정본 재생성이 캐시와 스택을 갈라놨다

### Grounding
- Git Context: `hash:ef5d6e7`
- Evidence: `data_cache_v2.npz`(9/5 03:46) · `gmscreen_quads.jsonl`(mtime 9/11 21:52,
  untracked) · `git show 3314eaf:assets_dev/train/eval_reader.py`(9/5 기하 복원) ·
  임시 진단 프로브(holdout 20개 크롭 diff)
- Confidence: 5/5

### Discovery

웹툴 /atlas 초기화가 "등가성 검사 실패: glucose_batch1/2411" 로 죽는 원인을 소거법으로
확정했다:

1. **기하·마진은 무죄** — 캐시 굽기 시점(9/5 02:36, 3314eaf)의 eval_reader 프레이밍은
   축정렬 외접 박스 + BOX_MARGIN(0.10 대칭) + warpPerspective 로, 현재
   build_cache_v2.frame_crop 과 알고리즘·값이 동일하다(904c266 은 정본 이동이지
   의미 변경이 아니었다).
2. **쿼드 값이 갈라졌다** — holdout 20개 전부에서 현재 쿼드로 만든 크롭이 캐시 크롭과
   불일치(diff 10~35, ±16px 시프트 탐색으로도 회복 불가 = 이동이 아니라 영역 자체가
   다르다). gmscreen_quads.jsonl 은 9/11 21:52(리더 스크래치 재구축)에 재생성됐고
   **untracked**이라 옛 값은 어디에도 없다. 지금 쿼드는 사람 LCD 라벨(screen_boxes)
   과도 일치하지 않는다(홀드아웃 라벨 보유 3개 중 0개 일치) — 재생성이 단순
   '사람 라벨 우선 병합'이 아니라 전체 재추론이었음을 시사한다.
3. **재결합은 세 방향 모두 막힌다** — (a) 옛 쿼드 부활: 없음(untracked). (b) 캐시
   재굽기(새 경로): 등가성 검사는 통과하지만 검사의 의미(옛 모델이 학습 때 본 크롭과
   같음 증명)가 깨진다 — 옛 preds(9/8)는 옛 쿼드로 추론된 것이라 재현·TTA 표와 공식
   예측이 어긋난다. (c) preds 재생성: 폐기 계보(2026-09-17) 복원이다.

### Analysis
- Why non-obvious: 어젯밤 첫 진단은 "기하가 바뀌어 캐시와 어긋난다"로 읽었는데,
  9/5 코드 복원 비교에서 기하가 같다는 게 드러났다 — **바뀐 건 정본 파일(쿼드)의 값**이었다.
  mtime 과 git 추적 여부(untracked = 복원 불가)를 먼저 봤어야 한 번에 나올 답이었다.
- Impact: /atlas 의 503 은 정확한 상태다. 부활 조건은 새 리더(reader_crnn.py)가 새
  preds 를 내는 것(마일스톤 '합성만으로 사진에서 값까지'). 웹툴 실패 안내 문구에 이
  진단을 명시했다.
- Action taken: 웹툴 503 안내 갱신 · 아틀라스 흡수 카드가 이미 못박은 "데이터 짝"
  사유의 정밀 버전을 이 Case 로 남김 — 아침 리뷰 카드 '리더 기본 캐시의 합성 팔을
  synth_panel 로 굽는다'(캐시 재굽기 허용 여부 판단)에도 이 근거가 쓰인다.

### Related Knowledge
- Patterns: [[frame-provenance-binding]]
- **Anti-Patterns**: 정본 입력 파일을 버전 관리 밖(untracked)에 두고 재생성한 뒤
  옛 짝과의 불일치를 코드에서 찾는 것 — 불일치의 원인은 파일 mtime·추적 여부에서
  먼저 보인다

## Case 4: [폐기] 세트 B 에 추론해 상자와 오차 분포를 낸다

### Grounding
- Evidence: ..\..\doc\kanban\abandoned\세트-B-에-추론해-상자와-오차-분포를-낸다.md
- Confidence: 4/5

### Discovery
이 길은 아니었다 — 카드가 폐기됐다. 폐기 사유(안티패턴 원재료):

폐기 계보 위 카드(2026-09-22 사람 승인): 추론에 쓴 synthband_v0 검출기와 세트 B·C 이미지 모두 2026-09-17/20 폐기·삭제됨. 어젯밤 서브에이전트 재검(카드 Note)이 지목한 대로 — 진짜 질문이었던 '999/1000 합격선'은 죽은 스택 위의 질문이었다. 산출물(_diag/synthband_v0/B.jsonl)과 등가성 논의는 기록으로 남고, 후속은 '리더를 현행 검출기 예측 상자로 학습한다'(승계 카드)로 간다.

### Analysis
- Why non-obvious: 다음 세션이 같은 벽에 다시 부딪히지 않게 하는 기록이다.
- Action taken: 주기 compile에서 안티패턴 승격 후보로 검토할 것.

### Related Knowledge
- **Anti-Patterns**: [[세트-B-에-추론해-상자와-오차-분포를-낸다]]

## Case 4: 리더 본학습 첫 회 — 병목은 학습법이 아니라 검출기 상자 프레이밍이었다

### Grounding
- Git Context: `hash:(이 커밋)`
- Artifact: `reader_crnn_tc_gt/best.pt` · `reader_crnn_tc_pred/best.pt` ·
  `_diag/reader_boxes/{tc,ve}_atone_s0.jsonl` · BAND_EXP_PLAN §27
- Confidence: 5/5

### Hypothesis

배포에선 리더가 검출기 상자를 받는다 — 정답 상자가 아니라 예측 상자로 학습하면
끝단이 오를 것이다(옛 YOLOX 카드의 예측 상자 학습 아이디어를 현행 atone 로 재지정).

### Results

2×2 (VE 1,000 완전일치 %): A(GT학습) GT평가 99.10 / 예측평가 84.20;
B(예측학습) GT평가 98.90 / 예측평가 85.60. atone 게이트(포함)는 VE ≈96.9%
합격 — "담는다"와 "읽힌다" 사이 ~11점 갭.

### Analysis
- Confirmed/Refuted: 예측 상자 학습의 득은 **+1.40pt로 작다**(반박까진 아님 —
  시드 1개 짝비교). 대신 병목의 위치가 옮겨졌다: 리더 학습법이 아니라
  **검출기 상자의 프레이밍 흔들림** — 포함 게이트가 통과시킨 상자를 리더가
  못 읽는다.
- Why non-obvious: 게이트(숫자 칸 포함)가 끝단(읽힘)의 대리 지표라 믿고 여러
  개입을 걸었는데, 2×2 를 재고 나니 대리 갭이 11점이었다. **게이트와 끝단을
  같은 자로 재는 날까지 검출기 실험의 "게이트 승리"는 끝단 승리로 읽지 않는다.**
- Implications: 다음 우선순위는 (1) 끝단 게이트(리더가 읽는가)로 검출기 채점
  (2) 상자→리더 프레이밍 완충. 세트 C 카드는 이 표를 받아 코퍼스·게이트 결정.

### Related Knowledge
- Concepts: [[metric-provenance]]
- Patterns: [[measure-the-premise-not-just-the-claim]]
- **Anti-Patterns**: [[proxy-metric-moves-against-the-goal]] — 포함률 게이트가
  끝단과 11점 갈라진 채 검출기 개입의 판정 기준으로 쓰인 것

## Case 5: TE 재학습으로 합성 작은 밴드 병목을 닫았더니 실사진이 같이 움직이지 않았다

### Grounding
- Git Context: `hash:PENDING`
- Evidence: `docs/BAND_EXP_PLAN.md` §28 · `assets_dev/train/tone2te_report.py` ·
  카드 'atone-레시피로-TE-재학습' (doc/kanban)
- Confidence: 5/5

### 사실
- 사전등록 판정 충족: VE 작음 3분위 합격 Δ+5.80pt, 짝 붓스트랩 95% CI
  [+3.61, +8.21] — 분포 뒤짐. 과대상자 p90 15.62→1.73, 검출실패 전 시드 0.
- 리더 배포 끝단 85.60→96.90(+11.30pt) — §27 의 11pt 갭이 상자만으로 닫힘.
- 같은 팔이 로보플로우 dev 586 에선 통과 93.43→88.48(Δ−4.94pt, 유의).
- 사람 라벨 50장: READING/우리 규약 판정 불변(§26 재확인), Δ−4.49 불유의.

### 교훈
- 웹툴 오버레이는 eval jsonl 의 det=false 행에도 문턱 아래 좌표가 담긴다는
  사실을 몰라서 "상자 없음" 필터가 죽어 있었다(팔 전환 리팩터링에서 det 기준으로
  복구). jsonl 행 규약을 읽고 병합 기준을 정할 것.
- 루프 변수 이름(rarm) 하나로 팔 선택이 조용히 뒤섞였다(arm="B" 사고).
  병합·조인 루프의 변수명은 바깥 이름과 겹치지 않게.
- 합성 도메인 개선과 실사진 성적이 반대로 갈 수 있다는 §22 교훈이 프레이밍
  축에서도 재현 — 도메인 하나에서만 재고 판정하지 않는다.

## Case 6: 세트-C 끝단 평가 — review-arbiter 판정으로 코퍼스·게이트를 정하고 PASS

### Grounding
- Git Context: `hash:PENDING`
- Evidence: `assets_dev/train/e2e_report.py` · `_diag/reader_dump`(18덤프 재생성,
  §28 근거는 backup_s28/ 에 보존) · 카드 '세트-C-로-끝단-평가한다'
- Confidence: 5/5

### 결정(review-arbiter 서브에이전트, 2026-09-22 무인 — 사용자 지시 '판단은
review-arbiter 에게 위임')
- 코퍼스: VE 1,000(unseen, 값 GT 생존 유일). 새 굽기는 구도 판단이 따르고,
  실사진은 값 GT 가 없어 '값까지' 채점 불가 — 기각.
- 게이트: tone2te 상자 s0~3 × 리더 B 값 완전일치 **최저 시드 ≥950/1000**,
  유효조건 B_gt ≥980. 배포 스택(atone×B)은 게이트 밖 인계 수치로 분리.
  95.0% 근거: GT 천장−4pt(잔여 오류 주성분이 리더가 되는 선) · §28.2 도달
  입증(96.90) · 배포 스택과 9.4pt 판별력.

### 결과(e2e_report.py — VE 1,000, 분모 전량)
```
배포 스택 atone×B(참조): 856/851/856/860 — 평균 ~85.6% (미검출 5~13 · 잘림 14~17 · 담고도오독 116~125)
게이트 차량 tone2te×B:    969/969/973/970 — 최저 96.90% (미검출 0 · 잘림 1~4 · 담고도오독 26~29)
천장 B_gt:               989 (98.90%)
판정: PASS
```
- 잘림 과대(area_ratio p90): atone 잘림 최대 131.8배 vs tone2te 1.1~41.5.
- 담고도오독의 과대 p90: atone 25~45배 vs tone2te 3~5배 — 상자 품질 차이가
  오독 버킷에서도 그대로 보인다.

### 면책(기록 의무)
- 표본은 합성(VE)이다 — 실사진 성적이 아니며 일반화 주장이 아니다.
- tone2te 는 배포 팔이 아니다(§28.4 실사진 회귀). 존재 증명 차량이다.
- 소수 팔 비교 위 선택의 승자의 저주(차이 0.5~1.3pt 수준) — 학습 오염 없음.
- 이 숫자를 실사진 성적으로 인용하지 않는다. 배포 스택 끝단 ~85.6% 는
  다음 마일스톤 '검출기와 인식기를 독립 축으로 끌어올린다'의 인계 수치.

### 교훈
- GT 천장 행에 검출기 det 를 섞으면 미검출 장의 우연 ok 가 천장을 깎는다
  (989→978). 경로 비교는 각 경로의 성분만 섞는다.
- arbiter 위임 판정은 '무엇을 재고 · 무엇으로 판정하고 · 무엇을 면책하는가'를
  함께 받아야 밤에 집행할 수 있다.

## Case 7: 오독 31장 분해 — 병목은 과대상자지 잘림이 아니었다

### Grounding
- Git Context: `hash:PENDING`
- Evidence: `assets_dev/train/reader_miss_triage.py` · BAND_EXP_PLAN §30
- Confidence: 5/5

### 사실
- tone2te×B 오독의 65~71%(시드당 19~22장)는 상자가 숫자칸을 담은 채 프레이밍만
  어긋난 장 — 지터 증강 표적. 잘림은 1~4장. 천장(GT 오독) 7~9장.
- 오독 층 면적비 p50 1.90 — 전체 분포(1.07)의 과대 꼬리 층에 오독이 몰린다.

### 교훈
- '검출기 잘림 vs 리더 변형'의 이분은 데이터로 가른다 — 눈으로 추측한 분해와
  실제(잘림 4장)는 달랐다. contains 조인 한 번으로 갈렸다.

## Case 8: 지터 증강 리더 — 천장 유지 조건 아래 상자 오차 강인화 성공

### Grounding
- Git Context: `hash:PENDING`
- Evidence: `assets_dev/train/jitter_report.py` · BAND_EXP_PLAN §31
- Confidence: 5/5

### 사실
- J_gt 99.40%(기준 ≥98 충족) · 배포 조건(tone2te 상자) 96.90→98.60
  (+1.70 CI [+0.90,+2.60]) · atone 상자 +3.00 [+1.90,+4.20].
- 상자가 나쁠수록 이득이 커짐 — 강인성이 검출기 품질과 독립.

### 교훈
- '구조 교체' 이전에 '데이터에 실측 오차를 입히는' 쪽이 먼저다(§27.3 예고가
  맞았다). 리더 학습셋 완전일치 100% 도달 없이(95.91%) 평가 성적이 더 좋아질
  수 있다 — 학습 난이도와 평가 성적은 다른 축이다.

## Case 9: 혼합 학습의 한계와 '딴 데'의 정체 — 보통 장에서의 사전 이동

### Grounding
- Git Context: `hash:PENDING`
- Evidence: `assets_dev/train/mix_corpus.py` · `classify_rf_miss.py --target
  elsewhere` · BAND_EXP_PLAN §32·§33
- Confidence: 5/5

### 사실
- tmix: VE 작음 97.40%(조건① 충족) · 로보플로우 통과 90.87(Δ−2.56 유의 하락,
  조건② 기각). TE 단독(−4.94) 대비 절반 회복.
- '딴 데' 증가분의 64~74%가 극단 신호 없는 보통 장 — 구도/열화/이물 극단층
  집중이 아니다.

### 교훈
- 사전등록 판정이 '절반 성공'을 성공으로 포장하지 않게 한다 — VE 충족만으로
  채택 후보가 되지 않는다(조건 ②가 AND 다).
- 도메인 갭 분해는 증가분(−대조 합집합) 기준으로 봐야 한다. 전체 합집합만
  보면 극단층 비중이 희석돼 보통 장의 이탈이 안 보인다.
