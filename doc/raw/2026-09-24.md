# 2026-09-24

## Case 1: [폐기] 밴드 크롭 대 LCD 전체 A/B — 이번엔 합성도 같은 크롭으로

### Grounding
- Evidence: doc\kanban\abandoned\밴드-크롭-대-LCD-전체-A-B-—-이번엔-합성도-같은-크롭으로.md
- Confidence: 4/5

### Discovery
이 길은 아니었다 — 카드가 폐기됐다. 폐기 사유(안티패턴 원재료):

사람 승인 2026-09-24(권고 채택): (1) OCR_REBUILD_PLAN §4 가 이 카드의 물음을 이미 닫았다 — 프레이밍이 검출기 출력으로 정해지므로 크롭 논쟁이 사라진다. (2) 진행 중인 마일스톤이 그 답을 실물로 만들고 있다(검출기=축정렬 밴드 상자 → 리더 학습). (3) AC#2 의 '같은 사전학습 시작점'이 없다(옛 리더 폐기·새 리더는 실사진 학습 경로 없음). G31 교훈 보존: 크롭을 조이면 침묵·자릿수 붕괴는 줄지만 그럴듯한 오독이 크게 는다(11→39) — 마일스톤의 '세트 C 끝단 평가'가 위험층 1순위로 보는 근거.

### Analysis
- Why non-obvious: 다음 세션이 같은 벽에 다시 부딪히지 않게 하는 기록이다.
- Action taken: 주기 compile에서 안티패턴 승격 후보로 검토할 것.

### Related Knowledge
- **Anti-Patterns**: [[밴드-크롭-대-LCD-전체-A-B-—-이번엔-합성도-같은-크롭으로]]

## Case 2: [폐기] 리더 기본 캐시의 합성 팔을 synth_panel 로 굽는다

### Grounding
- Evidence: doc\kanban\abandoned\리더-기본-캐시의-합성-팔을-synth_panel-로-굽는다.md
- Confidence: 4/5

### Discovery
이 길은 아니었다 — 카드가 폐기됐다. 폐기 사유(안티패턴 원재료):

사람 승인 2026-09-24(권고 채택): 새 리더(reader_crnn)는 npz 캐시를 쓰지 않고 COCO 세트에서 직접 크롭해 학습 — 이 카드가 고치려는 기본 캐시의 소비자가 없다(아무도 안 쓰면 굽는 보람 없음). 디스크 1.3GB×7 벌 유지. 글리프 교체(58c12cc) 뒤 코퍼스 재생성은 새 경로(COCO/synth_panel gen)로 한다.

### Analysis
- Why non-obvious: 다음 세션이 같은 벽에 다시 부딪히지 않게 하는 기록이다.
- Action taken: 주기 compile에서 안티패턴 승격 후보로 검토할 것.

### Related Knowledge
- **Anti-Patterns**: [[리더-기본-캐시의-합성-팔을-synth_panel-로-굽는다]]

## Case 3: 검출기 입력 분포 프레이밍 — 생성기 세대 교체 후 패널 직접 투입 붕괴

### Grounding
- Git Context: `hash:e877827`
- Evidence: _diag/bandft/box_v2a2.jsonl(패널 직접 — IoU 중앙 0.0) · boxfr_*.jsonl(LCD 프레이밍 — 0.79/0.81) · synth_coco/B2 manifest(밴드/캔버스 중앙 0.128 vs 옛 B 0.276)
- Confidence: 5/5

### Error
새 세대 코퍼스(B2)에서 검출기가 붕괴 — synthband_v2_a2 IoU 중앙 0.0000(평균 0.29, >=0.5 20/60). 옛 합성 세트 B 에서는 중앙 0.956 이었다.

### Environment Context
- Packages: YOLOX(D:/tmp) · sugartrain torch
- Migration Issue: No

### Fix Code
frame_panels_for_det.py — 패널을 framed_src_rect(LCD 쿼드+BOX_MARGIN)로 잘라 검출기 분포에 맞춘 뒤 추론, 예측 상자는 det_boxes_to_panel.py 로 패널 좌표로 되돌린다.

### Analysis
- Root cause: 2026-09-20~21 사람 선언(전체 기기 장면·배율·화면 위치)으로 합성 패널이 '전체 사진' 스타일로 바뀌어 밴드/캔버스 면적비가 0.27→0.13. 검출기는 학습 당시의 'LCD 크롭' 분포(0.27)로 학습돼 있어 패널 전체는 분포 밖. 글리프 교체(58c12cc) 탓이 아니었다 — 같은 시드 옛 렌더러 A/B 로 배제.
- Why it failed: 검증 스모크가 '레이아웃 위반 0·숫자 잘림 0'만 보고 밴드 크기 분포는 보지 않았다. 위반 0은 붕괴와 양립한다.
- How it was fixed: 배포 2단 구조(GM 크롭→밴드 검출)와 같은 프레이밍으로 입력을 맞추니 회복(중앙 0.79~0.81). 박스 생성은 이 프레이밍을 거쳐 패널 좌표로 되돌린다.
- 부수 발견: 실촬 파인튜닝(bandft_v1)이 새 세대 합성에서도 합성 전용(v2_a2)보다 우세(중앙 0.814·p10 0.58 vs 0.792·p10 0.00).

### Related Knowledge
- Concepts: [[metric-provenance]]
- Patterns: [[no-real-photo-ruler-for-synth]]
- **Anti-Patterns**: 스모크 자가검사가 위반 카운트만 보고 분포 축을 안 보는 것(위반 0 = 정상이 아니다)
